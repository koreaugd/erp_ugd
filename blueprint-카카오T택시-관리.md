# 카카오T 비즈니스 택시 관리 설계서

> 작성일: 2026-07-24
> 목적: Claude Code 구현 참조용 계획서 (ERP `erp_ugd` 관리자 화면 기능 확장)

---

## 1. 작업 컨텍스트

### 배경 및 목적

UGD는 카카오T 비즈니스(법인택시)를 전 지점에서 사용 중이나, 이용내역 확인·직원 등록·비정상 이용 점검을 모두 카카오T 비즈니스 관리시스템(별도 사이트)에 로그인해서 수작업으로 하고 있다. 월 200건 이상의 택시 이용이 발생하지만 지점별 택시비 집계는 ERP와 분리되어 있어 경비 파악이 늦다.

카카오T 비즈니스 API 연동은 **2026-07-24 실계정 호출로 실증 완료**했다(운영 도메인 `https://b2b-api.kakaomobility.com`, T 비즈 ID `26390514`, 시크릿 토큰 보유, 2026-06월 이용내역 221건 조회 성공). 목표는 관리자 화면(AdminPage)에 **"법인택시" 메뉴를 신설**하여 ① 월별 이용내역 조회·지점별/직원별 집계, ② 이상 이용 자동 점검, ③ 직원 등록·휴직 관리를 ERP 안에서 끝내는 것이다.

### 범위

- **포함**
  - 관리자 사이드바에 "법인택시" 대분류 신설 (하위: 이용내역 · 이상 점검 · 직원 관리)
  - 월별 이용내역 실시간 조회: 상세 리스트(누가·언제·어디서·얼마) + 지점별/직원별 합계
  - 이상 이용 규칙 기반 자동 플래그 (고액 건, 전월 대비 급증, 낮 시간대 이용, 미매핑 부서)
  - 카카오T 직원 등록/휴직/휴직해제/삭제 + 인증 알림톡 발송 (쓰기 작업은 확인 모달 필수)
  - 서버 프록시(Express `server.ts`)를 통한 HMAC-SHA1 서명 및 시크릿 보호
- **제외**
  - 택시 호출 기능 — 카카오가 택시 호출 API를 제공하지 않음 (호출 API는 퀵 전용·별도 계약)
  - 퀵/대리/기차 등 택시 외 서비스 내역 (API상 `vertical_code`로 구분 가능하나 1차 범위에서 제외)
  - 택시비의 손익계산서/월말마감 자동 전기 — 집계 화면 제공까지만, 회계 반영은 기존 흐름 유지
  - 이용내역의 ERP DB 저장 — 실시간 조회 방식 확정(사용자 결정), 저장·백필은 후속 과제

### 입출력 정의

| 항목 | 내용 |
|------|------|
| **입력** | 카카오T 비즈니스 API 응답: `GET /external/v2/orders`(이용내역: 일시·요금·경로·직원명·부서·그룹·결제), `GET /external/v1/groups`(그룹 목록), `GET /external/v2/members/connected`(인증 직원 목록). 관리자가 화면에서 고른 조회 월(YYYY-MM) |
| **출력** | ① 화면 렌더링만 — 월별 상세 리스트, 지점별/직원별 합계 테이블, 이상 플래그 목록, 직원 관리 테이블. ② 쓰기 작업 결과는 카카오 측에 저장(ERP 저장 없음). ③ 엑셀 다운로드(이용내역 월별, xlsx-js-style 기존 패턴 재사용) |
| **트리거** | 관리자가 사이드바 "법인택시" 메뉴 진입 시 당월+전월 조회. 월 변경 셀렉터 조작 시 재조회. 직원 등록/휴직 등 쓰기는 관리자 버튼 클릭 + 확인 모달 통과가 트리거 |

### 제약조건

- **시크릿 보안**: 시크릿 토큰·T 비즈 ID는 `.env`(`KAKAO_T_SECRET`, `KAKAO_T_CORP_ID`)에만 두고 **Express 서버에서만 사용**. React 번들·클라이언트 코드 포함 절대 금지. 운영 배포 환경에도 동일 환경변수 설정 필요.
- **HMAC 서명 함정(실증됨)**: 서명 메시지의 URL에는 **쿼리 파라미터를 제외**해야 한다. 포함 시 90003 "토큰 유효하지 않음" 에러(2026-07-24 실제 발생·해결). 메시지 = `"{nonce}\n{쿼리 제외 URL}\n{METHOD}\n{기업ID}\n{unix초}\n{nonce}"`, 헤더 = `Authorization: Token {b64}` + `x-mob-b2b-corp-id/nonce/timestamp`.
- **API 한도**: 이용내역 조회 기간 최대 1개월, 페이지당 최대 100건(`per=100`) → 서버가 페이지네이션 루프로 전량 수집 후 반환. 월 221건 실측 기준 2~3페이지.
- **지점명 불일치**: `member_department`에 "남산광어" 등 ERP 표준 지점명과 다른 표기가 들어옴(실측). 그룹명(한남점·압구정점·사카바단단·8번대물집·대물섬·본사 등)도 ERP 지점명과 대조 매핑 필요.
- **UI 규칙(AGENTS.md·DESIGN.md 준수)**: 사이드바는 텍스트만·`ugd-nav-item`/`ugd-nav-group` 클래스·`aria-current` 배선. 제목은 텍스트만(아이콘 금지). 관리자 화면은 `--branch-*` 토큰이 안 먹히는 함정 있음 — 관리자용 색 규칙 따를 것.
- **운영 원칙**: 수정 후 `npm run dev`(localhost:3000) 로컬 확인 → 사용자 "배포해" 지시 전 커밋/푸시 금지. 구현 직후 Codex 지독한 리뷰 필수. 마감 시간대 배포 금지.
- **쓰기 작업 안전장치**: 직원 삭제·휴직은 카카오 측 실데이터를 바꾸므로 확인 모달 + 결과 재조회로 반영 확인. 실패 시 화면 상태를 낙관적으로 바꾸지 말 것.

### 용어 정의

| 용어 | 정의 |
|------|------|
| 서버 프록시 | 시크릿을 보관하고 HMAC 서명 후 카카오 API를 대신 호출하는 Express 라우트(`/api/kakao-taxi/*`) |
| 이용내역(order) | 택시 승차 1건. 일시·요금·출발/도착지·직원·그룹·결제 승인 정보를 포함 |
| 그룹 | 카카오T 비즈니스의 조직 단위. UGD에서는 지점 단위로 운영 중(한남점 등 9개, 활성 6개) |
| 이상 플래그 | 규칙 기반 점검에 걸린 이용 건. 자동 차단이 아니라 화면 표시용 표식 |
| 지점 매핑 | `member_department`/`group_name` 표기를 ERP 표준 지점명으로 변환하는 대응표 |

---

## 2. 워크플로우 정의

### 전체 흐름도

```
[관리자 메뉴 진입/월 선택]
   → [Step 1: 서버 프록시 조회 (당월+전월 orders, groups, members)]
   → [Step 2: 정규화·지점 매핑]
   → [Step 3: 집계 (지점별/직원별 합계)]
   → [Step 4: 이상 이용 규칙 점검]
   → [화면 렌더링: 이용내역 탭 | 이상 점검 탭 | 직원 관리 탭]

[직원 관리 쓰기 (등록/휴직/해제/삭제/알림톡)]
   → [확인 모달] → [Step 5: 서버 프록시 쓰기 호출] → [성공: 목록 재조회 / 실패: 에러 표시·상태 유지]
```

### LLM 판단 vs 코드 처리 구분

| LLM이 직접 수행 (구현 시점) | 스크립트/코드로 처리 (런타임) |
|----------------|----------------|
| 지점 매핑표 초안 작성(카카오 그룹·부서 표기 → ERP 표준 지점명 대응, 201 지점명 통합관리 기준 참조) | HMAC-SHA1 서명 생성·헤더 조립 |
| 이상 감지 임계값 기본값 설계(외식업 심야 퇴근이 정상 패턴임을 반영) | 카카오 API 호출·페이지네이션 루프·에러 매핑 |
| 직원 등록 폼 필드 구성(카카오 API 스펙 대조) | 월별/지점별/직원별 합계 집계 |
| 런타임 LLM 호출 없음 — 이 기능은 전부 결정론적 코드로 동작 | 이상 이용 규칙 4종 평가·플래그 부여 |
| | 엑셀 다운로드 생성, 확인 모달·재조회 흐름 |

### 단계별 상세

#### Step 1: 서버 프록시 조회

- **처리 주체**: 스크립트 (`server.ts`의 `/api/kakao-taxi/*` 라우트 + `scripts/…` 아님, 서버 헬퍼 `src/server/kakaoTaxi.ts`를 server.ts에서 import)
- **입력**: 클라이언트 요청 `GET /api/kakao-taxi/orders?month=YYYY-MM` (그 외 `/groups`, `/members`)
- **처리 내용**: month로 start/end 산출 → 쿼리 제외 URL로 HMAC 서명 → 카카오 호출 → `per=100` 페이지네이션으로 전량 수집. 당월과 전월을 각각 호출(전월은 급증 비교용). 동일 month 응답은 서버 메모리에 5분 캐시.
- **출력**: JSON `{ month, orders: [...], prevMonthTotals: {...} }` (시크릿·서명값은 응답에 미포함)
- **성공 기준**: HTTP 200 + `count`와 수집 건수 일치
- **검증 방법**: 스키마 검증 (필수 필드 `service_fare`·`call_time`·`member_name`·`group_name` 존재 확인, count 대조)
- **실패 시 처리**: 자동 재시도 1회(서명 시각 재생성) → 실패 시 클라이언트에 카카오 에러코드 전달, 화면은 "카카오 연동 오류 + 재시도 버튼" 표시. 90003이면 서명 로직 회귀 의심 로그 남김

#### Step 2: 정규화·지점 매핑

- **처리 주체**: 스크립트 (`src/pages/admin/helpers/kakaoTaxi.ts`)
- **입력**: Step 1의 orders 배열
- **처리 내용**: `group_name`·`member_department`를 지점 매핑표로 ERP 표준 지점명으로 변환. 매핑에 없는 표기는 "미매핑"으로 분류(버리지 않음). 일시 문자열 파싱, 금액 합산 필드(`service_fare + toll`) 계산.
- **출력**: 정규화된 이용내역 배열 (원본 필드 + `branchName`, `unmapped` 플래그)
- **성공 기준**: 전 건이 지점명 또는 "미매핑" 중 하나로 분류됨 (소실 0건)
- **검증 방법**: 규칙 기반 (입력 건수 = 출력 건수 검증)
- **실패 시 처리**: 파싱 불가 건은 스킵하지 않고 원본 문자열 그대로 표시 + 콘솔 경고 로그 (스킵 + 로그 금지 — 금액 누락은 집계 왜곡)

#### Step 3: 집계

- **처리 주체**: 스크립트 (`src/pages/admin/helpers/kakaoTaxi.ts`)
- **입력**: Step 2의 정규화 배열
- **처리 내용**: 지점별 합계(건수·금액), 직원별 합계(건수·금액·소속), 월 총계 산출. 전월 직원별 합계도 동일 로직으로 산출.
- **출력**: 집계 객체 (화면 테이블 바인딩용)
- **성공 기준**: 지점별 합계의 총합 = 전체 건 금액 합 (미매핑 포함)
- **검증 방법**: 규칙 기반 (합계 대사식을 코드로 assert, 불일치 시 화면에 경고 배너)
- **실패 시 처리**: 대사 불일치 시에도 화면은 표시하되 "집계 검증 실패" 경고 노출 → 사용자 신고 유도 (조회 전용이라 데이터 훼손 없음)

#### Step 4: 이상 이용 규칙 점검

- **처리 주체**: 스크립트 (`src/pages/admin/helpers/kakaoTaxiAnomaly.ts`)
- **입력**: Step 3의 당월 정규화 배열 + 전월 직원별 합계
- **처리 내용**: 규칙 4종 평가 — R1 고액 건(건당 `service_fare + toll` ≥ 30,000원), R2 급증(직원 당월 합계가 전월의 2배 이상이면서 +50,000원 이상), R3 시간대(출발 06:00~17:00 — 심야 퇴근 택시가 정상 패턴인 업종 특성 반영, 낮 이용을 점검 대상으로), R4 미매핑 부서. 임계값은 상수로 정의하고 화면 상단에서 조정 가능(세션 내 로컬 상태).
- **출력**: 플래그 목록 (건별 사유 태그 포함, 이상 점검 탭에 표시)
- **성공 기준**: 전 건이 규칙 4종 모두에 대해 평가됨
- **검증 방법**: 규칙 기반 (플래그에 사유 태그 필수 — 사유 없는 플래그는 버그)
- **실패 시 처리**: 규칙 평가 예외 발생 시 해당 규칙만 비활성 표시하고 나머지 규칙 결과는 유지 (스킵 + 로그)

#### Step 5: 직원 관리 쓰기

- **처리 주체**: 에이전트 아님 — 관리자 수동 조작 + 스크립트 (서버 프록시 쓰기 라우트)
- **입력**: 관리자가 폼/버튼으로 입력한 직원 정보 또는 대상 직원 id
- **처리 내용**: 확인 모달 통과 후 서버 프록시가 카카오 쓰기 API 호출 — 등록(POST `/external/v1/members`), 휴직(POST `/members/block`)/해제(`/unblock`), 삭제(DELETE `/members/{id}`), 인증 알림톡(POST `/members/{id}/send_tms`). 등록 API의 정확한 요청 필드는 구현 착수 시 api-guide 문서에서 확정.
- **출력**: 성공 시 직원 목록 재조회로 화면 갱신 (낙관적 갱신 금지)
- **성공 기준**: 쓰기 응답 성공 + 재조회 목록에 변경이 실제 반영됨
- **검증 방법**: 규칙 기반 (재조회 결과 대조) + 사람 검토 (확인 모달이 사전 게이트)
- **실패 시 처리**: 에러 메시지 표시 + 화면 상태 원복. 재시도는 관리자 수동 (자동 재시도 금지 — 쓰기 중복 위험)

### 상태 전이

| 상태 | 전이 조건 | 다음 상태 |
|------|----------|----------|
| 조회 대기(idle) | 메뉴 진입/월 변경 | 조회 중(loading) |
| 조회 중(loading) | 서버 응답 성공 | 표시(loaded) |
| 조회 중(loading) | 서버 응답 실패 | 오류(error, 재시도 버튼) |
| 오류(error) | 재시도 클릭 | 조회 중(loading) |
| 직원 활성(enabled) | 휴직 처리 성공 + 재조회 확인 | 휴직(blocked) |
| 휴직(blocked) | 휴직 해제 성공 + 재조회 확인 | 활성(enabled) |
| 미인증 직원 | 인증 알림톡 발송 → 직원이 카카오T 앱에서 인증 | 인증완료(connected) |

---

## 3. 구현 스펙

### 폴더 구조

```
/erp_ugd
  ├── server.ts                                  # /api/kakao-taxi/* 라우트 등록 (기존 파일 확장)
  ├── src/
  │   ├── server/
  │   │   └── kakaoTaxi.ts                       # HMAC 서명·카카오 호출·페이지네이션·캐시 (서버 전용)
  │   └── pages/
  │       ├── AdminPage.tsx                      # 사이드바 "법인택시" 대분류 + 섹션 배선 (기존 파일 확장)
  │       └── admin/
  │           ├── KakaoTaxiSection.tsx           # 하위탭 3개(이용내역·이상 점검·직원 관리) 컨테이너
  │           └── helpers/
  │               ├── kakaoTaxi.ts               # 정규화·지점 매핑표·집계·엑셀 다운로드
  │               └── kakaoTaxiAnomaly.ts        # 이상 이용 규칙 4종
  ├── .env                                       # KAKAO_T_CORP_ID, KAKAO_T_SECRET (커밋 금지)
  └── blueprint-카카오T택시-관리.md               # 본 설계서
```

### CLAUDE.md 핵심 섹션 목록

- 이 기능은 erp_ugd 저장소의 기존 `AGENTS.md` 규칙(로컬 확인 후 배포 승인, Codex 리뷰, 디자인 토큰)을 그대로 따른다 — 신규 CLAUDE.md 작성 없음
- `AGENTS.md`에 추가할 항목: "카카오T API 서명은 쿼리 제외 URL로 — 90003 에러 시 이 규칙 회귀 의심" 한 줄

### 에이전트 구조

**구조 선택**: 단일 에이전트 (Claude Code 단독 구현, 런타임 에이전트 없음)

**선택 근거**: 이 기능은 웹앱 코드 확장이며 런타임에 LLM 판단이 없다. 구현 단계도 서버 라우트 + 화면 1개 섹션 규모로 단일 세션에서 처리 가능. 서브에이전트 분리 기준(컨텍스트 초과·독립 도메인 지식)에 해당하지 않음.

#### 메인 에이전트 (Claude Code)
- **역할**: 서버 프록시 → 헬퍼 → 화면 → 사이드바 배선 순서로 구현, 각 단계 로컬 검증
- **담당 단계**: Step 1~5 전부

### 스킬/스크립트 목록

| 이름 | 유형 | 역할 | 트리거 조건 |
|------|------|------|-----------|
| `src/server/kakaoTaxi.ts` | 스크립트 | HMAC 서명·카카오 API 호출·페이지네이션·5분 캐시 | `/api/kakao-taxi/*` 요청 수신 시 |
| `src/pages/admin/helpers/kakaoTaxi.ts` | 스크립트 | 정규화·지점 매핑·집계·엑셀 다운로드 | 이용내역 응답 수신 시 |
| `src/pages/admin/helpers/kakaoTaxiAnomaly.ts` | 스크립트 | 이상 이용 규칙 4종 평가 | 집계 완료 시 |
| `KakaoTaxiSection.tsx` | 스크립트(컴포넌트) | 하위탭 3개 화면 렌더링·확인 모달 | 사이드바 "법인택시" 진입 시 |
| 신규 스킬 | — | 이 설계에서 신규 Claude 스킬은 정의하지 않음 | — |

### 스킬 생성 규칙

> 이 설계서에 정의된 모든 스킬은 구현 시 반드시 `skill-creator` 스킬(`/skill-creator`)을 사용하여 생성할 것.
> 직접 SKILL.md를 수동 작성하지 말 것 — 규격 불일치 및 트리거 실패의 원인이 됨.
> (본 설계는 신규 스킬 0개이나, 후속 확장에서 스킬을 추가하게 되면 본 규칙을 따른다.)

skill-creator가 보장하는 규격:
1. SKILL.md frontmatter (`name`, `description`) 필수 필드 준수
2. `description`의 트리거 정확도 최적화 (eval 기반 optimization loop)
3. 폴더 구조 (`SKILL.md` + `scripts/` + `references/`) 규격 준수
4. Progressive disclosure: SKILL.md 본문 500줄 이내, 대용량 참조는 `references/`로 분리
5. 테스트 프롬프트 실행 및 품질 검증 완료

### 주요 산출물 파일

| 파일 | 형식 | 생성 단계 | 용도 |
|------|------|----------|------|
| `src/server/kakaoTaxi.ts` | TS | 구현 1단계 | 서버 프록시 (시크릿 보호·서명·호출) |
| `src/pages/admin/helpers/kakaoTaxi.ts` | TS | 구현 2단계 | 정규화·매핑·집계 |
| `src/pages/admin/helpers/kakaoTaxiAnomaly.ts` | TS | 구현 2단계 | 이상 규칙 |
| `src/pages/admin/KakaoTaxiSection.tsx` | TSX | 구현 3단계 | 관리자 화면 섹션 |
| `AdminPage.tsx` 수정분 | TSX | 구현 4단계 | 사이드바 대분류·섹션 배선 |
| 이용내역 엑셀 | XLSX | 런타임(버튼) | 월별 이용내역 다운로드 |
