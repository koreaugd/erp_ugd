# 카카오T 비즈니스 2계정 지원 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ERP 법인택시 화면이 카카오T 비즈니스 계정 2개(기업ID `26390514`, `25648071`)의 이용내역·직원을 하나로 보여주고, 계정 #2 직원도 ERP에서 관리할 수 있게 한다.

**Architecture:** 카카오 호출 계층 최하단에 계정 레지스트리를 두고, 모든 `fetch` 함수가 첫 인자로 `accountKey`를 받는다. 조회 함수는 등록된 계정을 순회해 각 레코드에 `account_key`를 주입한 뒤 합치고, 실패한 계정은 `accountErrors`로 함께 돌려준다(성공분은 보여주고 화면이 빨간 배너로 경고). 캐시 키에 계정을 포함해 계정별로 분리한다. 쓰기 경로는 `accountKey`를 필수로 받아 라우팅한다.

**Tech Stack:** Google Apps Script(`gas/Code.gs`, ES5 스타일 `var`/`function`), Express+TypeScript(`server.ts` — 로컬 dev 미러), React 19 + TypeScript(Vite), Tailwind v4.

## Global Constraints

- **시크릿을 저장소에 넣지 않는다.** 계정 #2 자격증명은 GAS 스크립트 속성(`KAKAO_T_CORP_ID_2` / `KAKAO_T_SECRET_2`)과 로컬 `.env`에만 둔다. 소스·문서·프론트 번들·커밋 메시지 어디에도 값을 쓰지 않는다. 저장소는 아직 public이다.
- **`gas/Code.gs`와 `server.ts`는 같은 로직을 미러링한다.** 한쪽만 고치면 로컬과 운영이 어긋난다. 반드시 같은 커밋에서 함께 수정한다.
- **서명 URL에는 쿼리 파라미터를 넣지 않는다.** 넣으면 카카오가 `90003`("인증 토큰이 유효하지 않습니다")을 반환한다. 실제 요청 URL에만 쿼리를 붙인다.
- **카카오 PUT은 미전송 필드를 공백으로 지운다.** 직원 수정 시 `mobile_phone`·`group_ids`·`name`·`department` 4필드를 항상 함께 보낸다.
- **그룹 제거 PUT은 카카오가 500으로 거부한다.** 기존 `group_ids`에서 항목을 빼는 요청을 만들지 않는다.
- **UI는 `DESIGN.md` §6-0-1 폰트 기준표를 따른다**: 제목·헤더·입력칸·버튼 `text-[11px]`, 데이터셀 `text-[12px]`, **`text-sm` 금지**. 제목에는 아이콘을 넣지 않는다.
- **지점 표준명은 정확히 `사카바단단`·`8번대물집`이다.** 별칭 등록이 필요 없다(확인 완료).
- **검증 명령**: 타입 검사는 `npm run lint`(= `tsc --noEmit`), 순수 로직 검증은 `npx tsx scripts/verify-kakao-multiaccount.mts`. 이 저장소에는 단위 테스트 러너가 없고 `scripts/verify-*.mjs` 검증 스크립트가 관례다.
- **커밋은 하되 푸시·배포는 하지 않는다.** 사용자가 "배포해"라고 지시하기 전에는 `git push`와 GAS `clasp` 재배포를 실행하지 않는다. 배포 직전에는 Codex 지독한 리뷰를 거친다.

---

## File Structure

**수정**
- `src/api/gasClient.ts` (926줄) — 카카오 타입에 `account_key` 추가, 응답에 `accountErrors` 추가, 쓰기 메서드 시그니처에 `accountKey` 추가
- `src/pages/admin/helpers/kakaoTaxi.ts` (139줄) — 지점 판정에 퇴사자 보정표 추가, `memberKey`에 계정 포함, 계정 라벨 헬퍼
- `src/pages/admin/helpers/kakaoTaxiAnomaly.ts` (110줄) — 이상 점검 면제 목록을 `계정|member_id` 쌍으로
- `gas/Code.gs` (1660줄, 카카오 구역 1200~1660) — 계정 레지스트리·서명·조회·쓰기 라우팅
- `server.ts` (1314줄, 카카오 구역 214~560) — 위와 동일 로직 미러
- `src/pages/admin/KakaoTaxiSection.tsx` (1551줄) — 계정 컬럼·필터·실패 배너, 쓰기 시 계정 전달
- `src/pages/branch/tabs/BusinessTaxiTab.tsx` — 지점 화면 계정 통합

**생성**
- `scripts/verify-kakao-multiaccount.mts` — 순수 로직 검증 스크립트(계정 분리 집계, 보정표, memberKey 충돌)

**책임 경계**: 카카오 API 왕복과 계정 라우팅은 백엔드(`Code.gs`/`server.ts`)에만 둔다. 정규화·집계·판정은 `helpers/*.ts` 순수 함수에 두고 화면은 표시만 한다(기존 구조 유지). 지점 필터링은 반드시 백엔드에서 한다 — 타 지점 직원의 이름·전화가 지점 화면에 내려가면 안 된다.

---

### Task 1: 프론트 순수 로직 — 계정 분리 집계와 퇴사자 보정

**Files:**
- Modify: `src/api/gasClient.ts:857-895` (타입에 `account_key` 추가)
- Modify: `src/pages/admin/helpers/kakaoTaxi.ts:12-74`
- Create: `scripts/verify-kakao-multiaccount.mts`

**Interfaces:**
- Consumes: 없음(첫 작업)
- Produces:
  - `KAKAO_TAXI_ACCOUNT_LABEL: Record<string, string>` — `{ acct1: "1계정", acct2: "2계정" }`
  - `accountLabel(key: string): string`
  - `KAKAO_RETIRED_MEMBER_BRANCH: Record<string, string>` — 키는 `` `${account_key}|${member_id}` ``
  - `NormalizedTaxiOrder` 에 `accountKey: string` 필드 추가
  - `memberKey` 형식이 `` `${accountKey}|${member_name}|${member_identifier}` `` 로 변경
  - `KakaoTaxiOrder.account_key`, `KakaoTaxiGroup.account_key`, `KakaoTaxiMember.account_key` (모두 `string`)

- [ ] **Step 1: 검증 스크립트를 먼저 작성한다(실패하는 상태)**

Create `scripts/verify-kakao-multiaccount.mts`:

```typescript
/**
 * 카카오T 2계정 지원 — 순수 로직 검증(읽기 전용, 네트워크 호출 없음).
 *   npx tsx scripts/verify-kakao-multiaccount.mts
 * 실패 시 비정상 종료한다. 배포 전 필수.
 */
import {
  normalizeKakaoTaxiOrders,
  aggregateByBranch,
  memberAmountMap,
  accountLabel,
  type NormalizedTaxiOrder,
} from "../src/pages/admin/helpers/kakaoTaxi";
import type { KakaoTaxiOrder } from "../src/api/gasClient";

let failed = 0;
function check(name: string, cond: boolean, detail = "") {
  if (cond) { console.log(`  OK   ${name}`); return; }
  failed += 1;
  console.error(`  FAIL ${name}${detail ? " — " + detail : ""}`);
}

function order(o: Partial<KakaoTaxiOrder>): KakaoTaxiOrder {
  return {
    id: "x", service_fare: 0, toll: 0, platform_fee: 0,
    call_time: "2026-07-01 12:00:00", departure_time: "2026-07-01 12:00:00", arrival_time: "",
    departure_point: "", arrival_point: "", waypoints: null,
    member_id: "M1", member_name: "홍길동", member_identifier: "홍길동", member_department: "",
    group_id: "", group_name: "", car_model: "", car_number: "", taxi_company_name: "",
    taxi_kind: "", vertical_code: "taxi", vertical_product_name: "", total_distance: 0,
    payment_items: [], account_key: "acct1",
    ...o,
  } as KakaoTaxiOrder;
}

const ERP_BRANCHES = ["사카바단단", "8번대물집", "대물섬 한남점"];

console.log("[1] 계정별 동명이인이 합산되지 않는다");
{
  const rows = normalizeKakaoTaxiOrders([
    order({ id: "a", account_key: "acct1", member_name: "정윤기", member_identifier: "정윤기", member_department: "대물섬 한남점", service_fare: 10000 }),
    order({ id: "b", account_key: "acct2", member_name: "정윤기", member_identifier: "정윤기", member_department: "8번대물집", service_fare: 20000 }),
  ], ERP_BRANCHES);
  const keys = new Set(rows.map((r) => r.memberKey));
  check("동명이인 memberKey 2개", keys.size === 2, [...keys].join(" / "));
  const amounts = memberAmountMap(rows);
  check("각각 따로 집계", amounts.size === 2 && [...amounts.values()].every((v) => v === 10000 || v === 20000));
}

console.log("[2] 퇴사자 보정표가 삭제된 회원의 과거 건을 지점에 귀속시킨다");
{
  const rows = normalizeKakaoTaxiOrders([
    order({ id: "c", account_key: "acct2", member_id: "ZB2L167I", member_name: "김태호", member_identifier: "김태호", member_department: "", service_fare: 9070 }),
  ], ERP_BRANCHES);
  check("보정으로 사카바단단 귀속", rows[0].branchName === "사카바단단", rows[0].branchName);
  check("보정된 건은 미매핑 아님", rows[0].unmapped === false);
}

console.log("[3] 보정표에 없는 빈 부서는 그대로 미매핑");
{
  const rows = normalizeKakaoTaxiOrders([
    order({ id: "d", account_key: "acct2", member_id: "UNKNOWN1", member_department: "", group_name: "기본그룹" }),
  ], ERP_BRANCHES);
  check("미매핑 유지", rows[0].unmapped === true);
  check("표시는 그룹 원문", rows[0].branchName === "기본그룹", rows[0].branchName);
}

console.log("[4] 집계 대사 — 지점별 합계 총합 == 건별 총합");
{
  const rows = normalizeKakaoTaxiOrders([
    order({ id: "e", account_key: "acct2", member_department: "사카바단단", service_fare: 5000, toll: 500 }),
    order({ id: "f", account_key: "acct1", member_department: "대물섬 한남점", service_fare: 3000 }),
  ], ERP_BRANCHES);
  const totals = aggregateByBranch(rows);
  const sum = totals.reduce((a, b) => a + b.amount, 0);
  check("합계 일치", sum === 8500, String(sum));
}

console.log("[5] 계정 라벨");
check("acct1 라벨", accountLabel("acct1") === "1계정");
check("acct2 라벨", accountLabel("acct2") === "2계정");
check("미등록 키는 원문", accountLabel("acct9") === "acct9");

if (failed) { console.error(`\n실패 ${failed}건`); process.exit(1); }
console.log("\n전부 통과");
```

- [ ] **Step 2: 실패하는 것을 확인한다**

Run: `npx tsx scripts/verify-kakao-multiaccount.mts`
Expected: FAIL — `accountLabel` 이 `kakaoTaxi.ts` 에 없어 import 오류로 종료한다.

- [ ] **Step 3: `gasClient.ts` 타입에 `account_key` 를 추가한다**

`src/api/gasClient.ts` — `KakaoTaxiOrder` 인터페이스 끝(`payment_items` 다음)에 추가:

```typescript
  payment_items: KakaoTaxiPaymentItem[];
  /** 어느 카카오T 계정에서 온 건인지 — 백엔드가 주입한다. 계정 2개를 합쳐 보여주므로 필수. */
  account_key: string;
}
```

`KakaoTaxiGroup` 과 `KakaoTaxiMember` 에도 같은 필드를 추가한다:

```typescript
export interface KakaoTaxiGroup {
  id: string;
  name: string;
  status: string; // "enabled" | "deactivated"
  description: string;
  account_key: string;
}

export interface KakaoTaxiMember {
  id: string;
  name: string;
  department: string;
  identifier: string;
  mobile_phone: string;
  status: string; // created | connected | refused | blocked
  confirmed_at: string | null;
  group_ids: string[];
  account_key: string;
}
```

- [ ] **Step 4: `kakaoTaxi.ts` 에 계정 라벨·보정표·계정 인식 정규화를 넣는다**

`src/pages/admin/helpers/kakaoTaxi.ts` — `KAKAO_BRANCH_ALIASES` 선언 아래에 추가:

```typescript
// 카카오T 비즈니스 계정 표시명. 백엔드 KAKAO_TAXI_ACCOUNTS 의 key 와 같게 유지할 것
// ([동기화] gas/Code.gs, server.ts).
export const KAKAO_TAXI_ACCOUNT_LABEL: Record<string, string> = {
  acct1: "1계정",
  acct2: "2계정",
};

export function accountLabel(key: string): string {
  return KAKAO_TAXI_ACCOUNT_LABEL[key] || key || "";
}

// 퇴사자 과거 내역 귀속 보정표 — 키는 `${account_key}|${member_id}`.
// 카카오에서 회원을 삭제하면 그 사람의 과거 주문에서 member_department 가 null 이 된다(실측).
// 이름으로 매칭하면 동명이인을 가를 수 없어(정윤기 사례) 반드시 member_id 로 지정한다.
// 삭제 전에 부서를 채워두면 애초에 필요 없다 — 운영 지침은 "퇴사자 존치"다.
export const KAKAO_RETIRED_MEMBER_BRANCH: Record<string, string> = {
  "acct2|ZB2L167I": "사카바단단", // 김태호, 2026-05 10건 90,700원
};
```

`NormalizedTaxiOrder` 인터페이스에 필드를 추가한다:

```typescript
  /** 어느 카카오T 계정 건인지 — 화면 계정 컬럼·필터와 memberKey 구성에 쓴다 */
  accountKey: string;
  /** 직원 구분 키 — 계정이 다르면 동명이인이므로 계정까지 포함한다 */
  memberKey: string;
```

`resolveBranchName` 을 계정 인식·보정표 적용 형태로 바꾼다:

```typescript
function resolveBranchName(
  order: KakaoTaxiOrder,
  erpBranchNames: Set<string>
): { branchName: string; unmapped: boolean } {
  const candidates = [order.member_department, order.group_name]
    .map((v) => (v || "").trim())
    .filter(Boolean);
  for (const raw of candidates) {
    if (erpBranchNames.has(raw)) return { branchName: raw, unmapped: false };
    const alias = KAKAO_BRANCH_ALIASES[raw];
    if (alias && erpBranchNames.has(alias)) return { branchName: alias, unmapped: false };
  }
  // 부서·그룹으로 못 찾았다 — 카카오에서 삭제된 퇴사자일 수 있으니 보정표를 마지막으로 본다.
  const retired = KAKAO_RETIRED_MEMBER_BRANCH[`${order.account_key}|${order.member_id}`];
  if (retired && erpBranchNames.has(retired)) return { branchName: retired, unmapped: false };
  // 매핑 실패 — 부서 원문(없으면 그룹 원문)을 그대로 보여준다. 숨기면 금액이 사라진 것처럼 보인다.
  return { branchName: candidates[0] || "미지정", unmapped: true };
}
```

`normalizeKakaoTaxiOrders` 의 `return` 객체에서 `accountKey` 를 채우고 `memberKey` 에 계정을 포함시킨다:

```typescript
    const accountKey = String(order.account_key || "acct1");
    return {
      order,
      accountKey,
      branchName,
      unmapped,
      amount: (Number(order.service_fare) || 0) + (Number(order.toll) || 0),
      timeText,
      hour: hourMatch ? Number(hourMatch[1]) : null,
      memberKey: `${accountKey}|${(order.member_name || "").trim()}|${(order.member_identifier || "").trim()}`,
    };
```

`buildOrdersExcelRows` 에 계정 열을 추가한다(`"지점"` 앞):

```typescript
export function buildOrdersExcelRows(rows: NormalizedTaxiOrder[]) {
  return rows.map(({ order, accountKey, branchName, unmapped, amount, timeText }) => ({
    "이용일시": timeText,
    "계정": accountLabel(accountKey),
    "직원": order.member_name,
    "사번": order.member_identifier,
    "지점": branchName + (unmapped ? " (미매핑)" : ""),
    "카카오 그룹": order.group_name,
    "출발지": order.departure_point,
    "도착지": order.arrival_point,
    "요금": Number(order.service_fare) || 0,
    "톨비": Number(order.toll) || 0,
    "합계": amount,
    "차종": order.vertical_product_name || order.taxi_kind,
    "차량번호": order.car_number,
    "택시회사": order.taxi_company_name,
    "구분": verticalLabel(order.vertical_code),
  }));
}
```

- [ ] **Step 5: 검증 스크립트가 통과하는지 확인한다**

Run: `npx tsx scripts/verify-kakao-multiaccount.mts`
Expected: PASS — 전 항목 `OK`, 마지막 줄 `전부 통과`

- [ ] **Step 6: 타입 검사**

Run: `npm run lint`
Expected: `account_key` 를 아직 안 채우는 백엔드 쪽에서 타입 오류가 날 수 있다. **`src/` 안에서 나는 오류만 이 시점에 고친다.** `server.ts` 오류는 Task 4에서 해소되므로 남아 있어도 된다. 어떤 오류가 남았는지 기록해 둔다.

- [ ] **Step 7: 커밋**

```bash
git add src/api/gasClient.ts src/pages/admin/helpers/kakaoTaxi.ts scripts/verify-kakao-multiaccount.mts
git commit -m "feat(kakao): 이용내역 정규화에 계정 구분·퇴사자 귀속 보정 추가"
```

---

### Task 2: 이상 점검 면제 목록을 계정 인식으로

**Files:**
- Modify: `src/pages/admin/helpers/kakaoTaxiAnomaly.ts:34-41,69,92`
- Modify: `scripts/verify-kakao-multiaccount.mts` (검증 추가)

**Interfaces:**
- Consumes: Task 1의 `NormalizedTaxiOrder.accountKey`, `memberKey`
- Produces: `isAnomalyExempt(accountKey: string, memberId: string | null | undefined): boolean`

- [ ] **Step 1: 검증을 먼저 추가한다**

`scripts/verify-kakao-multiaccount.mts` 의 import 에 추가:

```typescript
import { flagTaxiOrders, isAnomalyExempt, DEFAULT_TAXI_THRESHOLDS } from "../src/pages/admin/helpers/kakaoTaxiAnomaly";
```

`[5] 계정 라벨` 블록 아래, `if (failed)` 위에 추가:

```typescript
console.log("[6] 이상 점검 면제는 계정+id 쌍으로만 적용된다");
{
  check("계정1 서광엽 면제", isAnomalyExempt("acct1", "JE1T6UC2") === true);
  check("계정2의 같은 id 는 면제 아님", isAnomalyExempt("acct2", "JE1T6UC2") === false);
  const rows = normalizeKakaoTaxiOrders([
    order({ id: "g", account_key: "acct1", member_id: "JE1T6UC2", member_department: "본사", service_fare: 90000 }),
    order({ id: "h", account_key: "acct2", member_id: "JE1T6UC2", member_department: "사카바단단", service_fare: 90000 }),
  ], [...ERP_BRANCHES, "본사"]);
  const flagged = flagTaxiOrders(rows, DEFAULT_TAXI_THRESHOLDS);
  check("고액 표식은 계정2 건에만", flagged.length === 1 && flagged[0].row.accountKey === "acct2",
    flagged.map((f) => f.row.accountKey).join(","));
}
```

- [ ] **Step 2: 실패 확인**

Run: `npx tsx scripts/verify-kakao-multiaccount.mts`
Expected: FAIL — `isAnomalyExempt` 가 인자 1개짜리라 타입 오류로 종료한다.

- [ ] **Step 3: 면제 목록과 판정 함수를 계정 인식으로 바꾼다**

`src/pages/admin/helpers/kakaoTaxiAnomaly.ts:34-41` 을 교체:

```typescript
// 이상 점검 제외 대상 — 본사 운영진은 업무 특성상 고액/낮 시간대 이용이 정상 패턴이라
// 표식이 소음만 만든다(사용자 지시 2026-07-27). 이용내역 표에는 그대로 나온다 — 점검 표식만 제외.
// [동명이인 함정] 이름으로 거르면 지점에 같은 이름 직원이 생겼을 때 그 직원까지 조용히 점검에서
// 빠진다(사번=이름 관례라 identifier 로도 구분 불가) — 카카오 직원 고유 id 로 지정한다(운영 실측).
// [계정 함정] member_id 는 8자리 단문자열이라 계정끼리 겹칠 수 있다 — `계정|id` 쌍으로 지정한다.
// 카카오에서 삭제 후 재등록하면 id 가 바뀌어 제외가 풀리고 점검 표에 다시 나타난다(눈에 보이는
// 실패라 안전한 방향 — 그때 이 목록의 id 만 갱신하면 된다).
export const TAXI_ANOMALY_EXEMPT_MEMBERS = new Set([
  "acct1|JE1T6UC2", // 서광엽
  "acct1|OGKR9ACV", // 이미림
]);

export function isAnomalyExempt(accountKey: string, memberId: string | null | undefined): boolean {
  return TAXI_ANOMALY_EXEMPT_MEMBERS.has(`${String(accountKey || "").trim()}|${String(memberId || "").trim()}`);
}
```

- [ ] **Step 4: 호출부 2곳을 고친다**

`flagTaxiOrders` 안(기존 69행):

```typescript
    if (isAnomalyExempt(row.accountKey, row.order.member_id)) continue; // 본사 운영진 제외
```

`detectMemberSurges` 안(기존 92행)도 똑같이:

```typescript
    if (isAnomalyExempt(row.accountKey, row.order.member_id)) continue; // 본사 운영진 제외
```

- [ ] **Step 5: 통과 확인**

Run: `npx tsx scripts/verify-kakao-multiaccount.mts`
Expected: PASS — `[6]` 블록 포함 전부 `OK`

- [ ] **Step 6: 커밋**

```bash
git add src/pages/admin/helpers/kakaoTaxiAnomaly.ts scripts/verify-kakao-multiaccount.mts
git commit -m "fix(kakao): 이상 점검 면제 목록을 계정+member_id 쌍으로"
```

---

### Task 3: GAS 계정 레지스트리와 조회 통합

**Files:**
- Modify: `gas/Code.gs:1200-1451` (레지스트리·서명·조회), `gas/Code.gs:1477-1530` (그룹 조회 호출부)

**Interfaces:**
- Consumes: 없음(백엔드 독립)
- Produces:
  - `kakaoTaxiAccounts(): Array<{key: string, label: string, corpId: string, secret: string}>` — 속성이 등록된 계정만
  - `kakaoTaxiFetch(accountKey, method, path, query, body)`
  - `kakaoTaxiFetchAllPages(accountKey, path, baseQuery, listKey)`
  - `getKakaoTaxiOrders(month, forceRefresh)` → `{ month, count, orders, accountErrors }`
  - `getKakaoTaxiGroups(forceRefresh)` → `{ groups, accountErrors }` (**반환 형태 변경**)
  - `getKakaoTaxiMembers(forceRefresh)` → `{ count, members, accountErrors }`
  - `kakaoTaxiAccountGroups(accountKey, forceRefresh): Array<group>` — 쓰기 경로 전용, 단일 계정 실시간 조회

- [ ] **Step 1: 계정 레지스트리와 자격증명을 넣는다**

`gas/Code.gs:1222-1229` 의 `kakaoTaxiCredentials` 를 아래로 교체한다:

```javascript
// 카카오T 비즈니스 계정 레지스트리.
// 계정 #1 은 기존 속성 이름을 그대로 쓴다 — 속성 재등록 없이 기존 동작이 유지된다.
// [동기화] server.ts 의 KAKAO_TAXI_ACCOUNTS, src/pages/admin/helpers/kakaoTaxi.ts 의 계정 라벨.
var KAKAO_TAXI_ACCOUNTS = [
  { key: "acct1", label: "1계정", corpIdProp: "KAKAO_T_CORP_ID", secretProp: "KAKAO_T_SECRET" },
  { key: "acct2", label: "2계정", corpIdProp: "KAKAO_T_CORP_ID_2", secretProp: "KAKAO_T_SECRET_2" }
];

// 스크립트 속성이 등록된 계정만 돌려준다. 코드가 먼저 배포되고 속성 등록이 늦어도
// 기존 계정만으로 지금과 똑같이 동작한다(배포 순서 사고 방지).
function kakaoTaxiAccounts() {
  var out = [];
  for (var i = 0; i < KAKAO_TAXI_ACCOUNTS.length; i++) {
    var acc = KAKAO_TAXI_ACCOUNTS[i];
    var corpId = PROPERTIES.getProperty(acc.corpIdProp);
    var secret = PROPERTIES.getProperty(acc.secretProp);
    if (corpId && secret) out.push({ key: acc.key, label: acc.label, corpId: corpId, secret: secret });
  }
  if (!out.length) {
    throw new Error("카카오T 연동 정보가 등록되지 않았습니다. Apps Script 스크립트 속성에 KAKAO_T_CORP_ID / KAKAO_T_SECRET 를 등록해주세요.");
  }
  return out;
}

function kakaoTaxiCredentials(accountKey) {
  var accounts = kakaoTaxiAccounts();
  for (var i = 0; i < accounts.length; i++) {
    if (accounts[i].key === accountKey) return accounts[i];
  }
  throw new Error("등록되지 않은 카카오T 계정입니다: " + accountKey);
}
```

- [ ] **Step 2: 서명·요청 함수에 계정을 전달한다**

`gas/Code.gs` 의 `kakaoTaxiBuildRequest`·`kakaoTaxiFetch`·`kakaoTaxiFetchAllPages` 시그니처를 바꾼다:

```javascript
function kakaoTaxiBuildRequest(accountKey, method, path, query, body) {
  const cred = kakaoTaxiCredentials(accountKey);
  // [주의] 서명 URL에는 쿼리 파라미터를 넣지 않는다.
  // 넣으면 카카오가 90003("인증 토큰이 유효하지 않습니다")을 돌려준다 — 2026-07-24 실계정에서 확인.
  const signUrl = KAKAO_TAXI_BASE + path;
  const nonce = String(Math.floor(Math.random() * 100000));
  const timestamp = String(Math.floor(Date.now() / 1000));
  const message = nonce + "\n" + signUrl + "\n" + method + "\n" + cred.corpId + "\n" + timestamp + "\n" + nonce;
  const token = Utilities.base64Encode(
    Utilities.computeHmacSignature(Utilities.MacAlgorithm.HMAC_SHA_1, message, cred.secret)
  );
  const request = {
    url: KAKAO_TAXI_BASE + path + (query ? "?" + query : ""),
    method: method.toLowerCase(),
    muteHttpExceptions: true,
    headers: {
      "Authorization": "Token " + token,
      "x-mob-b2b-corp-id": cred.corpId,
      "x-mob-b2b-nonce": nonce,
      "x-mob-b2b-timestamp": timestamp
    }
  };
  if (body) {
    request.contentType = "application/json";
    request.payload = JSON.stringify(body);
  }
  return request;
}

function kakaoTaxiFetch(accountKey, method, path, query, body) {
  const req = kakaoTaxiBuildRequest(accountKey, method, path, query, body);
  const res = UrlFetchApp.fetch(req.url, req);
  return kakaoTaxiParseResponse(res);
}

function kakaoTaxiFetchAllPages(accountKey, path, baseQuery, listKey) {
  const pageQuery = function (page) {
    return (baseQuery ? baseQuery + "&" : "") + "per=100&page=" + page;
  };
  const first = kakaoTaxiFetch(accountKey, "GET", path, pageQuery(1), null);
  const firstBatch = (first && first[listKey]) || [];
  const reportedCount = (first && typeof first.count === "number") ? first.count : firstBatch.length;
  const items = firstBatch.slice();
  const totalPages = Math.min(50, Math.ceil(reportedCount / 100));
  if (firstBatch.length >= 100 && totalPages > 1) {
    const requests = [];
    for (let page = 2; page <= totalPages; page++) {
      requests.push(kakaoTaxiBuildRequest(accountKey, "GET", path, pageQuery(page), null));
    }
    const responses = UrlFetchApp.fetchAll(requests);
    for (let r = 0; r < responses.length; r++) {
      // 한 페이지라도 실패하면 전체를 실패로 처리한다 — 일부 누락본을 정상 자료처럼 돌려주지 않는다.
      const parsed = kakaoTaxiParseResponse(responses[r]);
      const batch = (parsed && parsed[listKey]) || [];
      for (let j = 0; j < batch.length; j++) items.push(batch[j]);
    }
  }
  return { count: reportedCount, items: items };
}
```

- [ ] **Step 3: 계정 순회 헬퍼를 넣는다**

`kakaoTaxiFetchAllPages` 아래에 추가한다:

```javascript
// 등록된 계정을 순회하며 collect(account) 를 부르고 결과를 합친다.
// 한 계정이 실패해도 나머지는 살린다 — 대신 실패 사실을 accountErrors 로 올려보내
// 화면이 "이 집계에 빠진 계정이 있다"고 빨간 배너로 알린다(조용한 금액 축소 방지).
// 전부 실패하면 기존처럼 오류를 던진다.
function kakaoTaxiCollect(collect) {
  var accounts = kakaoTaxiAccounts();
  var items = [];
  var errors = [];
  for (var i = 0; i < accounts.length; i++) {
    var acc = accounts[i];
    try {
      var got = collect(acc) || [];
      for (var j = 0; j < got.length; j++) {
        got[j].account_key = acc.key;   // 화면 계정 컬럼·필터·memberKey 의 근거
        items.push(got[j]);
      }
    } catch (e) {
      errors.push({ key: acc.key, label: acc.label, message: String((e && e.message) || e) });
    }
  }
  if (errors.length === accounts.length) {
    throw new Error("카카오T 조회에 실패했습니다: " + errors[0].message);
  }
  return { items: items, accountErrors: errors };
}
```

- [ ] **Step 4: 이용내역 조회를 계정 통합으로 바꾼다**

`gas/Code.gs` 의 `kakaoTaxiOrdersCacheKey` 와 `getKakaoTaxiOrders` 를 교체한다:

```javascript
function kakaoTaxiOrdersCacheKey(accountKey, month) {
  // 세대 번호 포함 — 직원 쓰기 시 kakaoTaxiInvalidateOrdersCache 가 번호를 올려 전 월 캐시를 무효화한다.
  // 계정별로 따로 캐싱한다 — 한쪽이 실패해도 다른 쪽 캐시가 살아 있고, 100KB 상한도 분산된다.
  return "kakao_taxi_orders_v2_" + kakaoTaxiOrdersCacheVersion() + "_" + accountKey + "_" + month;
}

// 월별 이용내역 전량 수집. 조회 기간은 카카오 제한(최대 1개월)에 맞춰 항상 한 달 단위다.
function getKakaoTaxiOrders(month, forceRefresh) {
  const range = kakaoTaxiMonthRange(month); // 형식 검증 겸용 — 캐시 키에 이상값이 섞이지 않게 먼저 부른다
  var cache = null;
  try { cache = CacheService.getScriptCache(); } catch (e) { cache = null; }
  const nowMonth = Utilities.formatDate(new Date(), "Asia/Seoul", "yyyy-MM");

  const collected = kakaoTaxiCollect(function (acc) {
    const cacheKey = kakaoTaxiOrdersCacheKey(acc.key, month);
    if (cache && !forceRefresh) {
      const cached = cache.get(cacheKey);
      if (cached) {
        try { return JSON.parse(cached); } catch (e) { /* 손상 시 아래에서 재조회 */ }
      }
    }
    const res = kakaoTaxiFetchAllPages(
      acc.key,
      "/external/v2/orders",
      "start_date=" + range.start + "&end_date=" + range.end,
      "orders"
    );
    // 건수가 어긋나거나 id 중복/누락이 있는 스냅샷은 캐시하지 않는다 — 캐시하면 '새로고침' 전까지 경고가 반복된다.
    if (cache && kakaoTaxiSnapshotComplete(res.items, res.count)) {
      const ttl = month < nowMonth ? KAKAO_ORDERS_CACHE_TTL_CLOSED : KAKAO_ORDERS_CACHE_TTL_CURRENT;
      // 캐시 값은 최대 100KB — 초과하면 put 이 던지므로 실패는 무시(그 달만 실시간 조회로 동작).
      try { cache.put(cacheKey, JSON.stringify(res.items), ttl); } catch (e) {}
    }
    return res.items;
  });

  // count 는 수집본 기준. 계정별 카카오 보고 건수와의 대사는 kakaoTaxiSnapshotComplete 가 이미 했다.
  return {
    month: month,
    count: collected.items.length,
    orders: collected.items,
    accountErrors: collected.accountErrors
  };
}
```

> **주의**: 캐시에 담는 값이 `result` 객체에서 **배열(`res.items`)로 바뀌었다.** 캐시 키의 `_v1_` → `_v2_` 변경이 옛 형식과의 충돌을 막는다.

- [ ] **Step 5: 그룹·직원 조회를 계정 통합으로 바꾼다**

`getKakaoTaxiGroups` 와 `getKakaoTaxiMembers` 를 교체하고, 쓰기 경로용 단일 계정 조회를 새로 만든다:

```javascript
function kakaoTaxiGroupsCacheKey(accountKey) {
  return KAKAO_GROUPS_CACHE_KEY + "_" + accountKey;
}

// 쓰기 경로 전용 — 특정 계정의 그룹만 실시간으로 받는다(캐시 미사용).
// 방금 만든/바꾼 그룹을 못 찾아 오등록되는 것을 막기 위해 조회용 캐시를 쓰지 않는다.
function kakaoTaxiAccountGroups(accountKey) {
  const groups = kakaoTaxiFetch(accountKey, "GET", "/external/v1/groups", null, null) || [];
  for (var i = 0; i < groups.length; i++) groups[i].account_key = accountKey;
  return groups;
}

function getKakaoTaxiGroups(forceRefresh) {
  var cache = null;
  try { cache = CacheService.getScriptCache(); } catch (e) { cache = null; }
  const collected = kakaoTaxiCollect(function (acc) {
    const key = kakaoTaxiGroupsCacheKey(acc.key);
    if (cache && !forceRefresh) {
      const cached = cache.get(key);
      if (cached) {
        try { return JSON.parse(cached); } catch (e) { /* 손상 시 재조회 */ }
      }
    }
    const groups = kakaoTaxiFetch(acc.key, "GET", "/external/v1/groups", null, null) || [];
    if (cache) {
      try { cache.put(key, JSON.stringify(groups), KAKAO_GROUPS_CACHE_TTL); } catch (e) {}
    }
    return groups;
  });
  return { groups: collected.items, accountErrors: collected.accountErrors };
}

function kakaoTaxiMembersCacheKey(accountKey) {
  return KAKAO_MEMBERS_CACHE_KEY + "_" + accountKey;
}

function getKakaoTaxiMembers(forceRefresh) {
  var cache = null;
  try { cache = CacheService.getScriptCache(); } catch (e) { cache = null; }
  const collected = kakaoTaxiCollect(function (acc) {
    const key = kakaoTaxiMembersCacheKey(acc.key);
    if (cache && !forceRefresh) {
      var cached = cache.get(key);
      if (cached) {
        try { return JSON.parse(cached); } catch (e) { /* 손상 시 재조회 */ }
      }
    }
    const res = kakaoTaxiFetchAllPages(acc.key, "/external/v2/members/connected", "", "members");
    // 캐시 값은 최대 100KB — 인원이 많아 초과하면 put 이 던지므로, 캐싱 실패는 무시하고 그대로 반환.
    if (cache) {
      try { cache.put(key, JSON.stringify(res.items), KAKAO_MEMBERS_CACHE_TTL); } catch (e) {}
    }
    return res.items;
  });
  return { count: collected.items.length, members: collected.items, accountErrors: collected.accountErrors };
}
```

- [ ] **Step 6: 캐시 무효화를 계정 인식으로 바꾼다**

`kakaoTaxiInvalidateMembersCache` 를 교체한다:

```javascript
// 특정 계정의 직원 캐시를 지운다. accountKey 를 안 주면 전 계정을 지운다(안전 쪽).
function kakaoTaxiInvalidateMembersCache(accountKey) {
  try {
    var cache = CacheService.getScriptCache();
    if (accountKey) {
      cache.remove(kakaoTaxiMembersCacheKey(accountKey));
    } else {
      for (var i = 0; i < KAKAO_TAXI_ACCOUNTS.length; i++) {
        cache.remove(kakaoTaxiMembersCacheKey(KAKAO_TAXI_ACCOUNTS[i].key));
      }
    }
  } catch (e) {}
  // 직원 정보(특히 부서=지점)를 고치면 이용내역의 지점/직원 집계도 달라진다 — 카카오가 이용내역에
  // 조회 시점의 직원 정보(member_department 등)를 실어 주기 때문. 이용내역 캐시도 함께 무효화해
  // "부서를 고쳤는데 집계가 그대로"인 상황을 막는다. 모든 직원 쓰기 경로가 이 함수를 지난다.
  kakaoTaxiInvalidateOrdersCache();
}
```

- [ ] **Step 7: 남은 단일 계정 전제 호출부를 고친다**

아래 함수들이 `kakaoTaxiFetch` 를 계정 없이 부르고 있다. **Task 8에서 계정 라우팅을 완성할 때까지 임시로 `"acct1"` 을 넘긴다**(현행 동작 유지):

- `registerKakaoTaxiMember` — `kakaoTaxiFetch("acct1", "POST", "/external/v1/members", null, body)`
- `updateKakaoTaxiMember` — `kakaoTaxiFetch("acct1", "PUT", ...)`
- `setKakaoTaxiMemberBlocked` — `kakaoTaxiFetch("acct1", "POST", path, ...)`
- `deleteKakaoTaxiMember` — `kakaoTaxiFetch("acct1", "DELETE", ...)`
- `sendKakaoTaxiMemberTms` — `kakaoTaxiFetch("acct1", "POST", ...)`

그리고 반환 형태가 바뀐 `getKakaoTaxiGroups` 의 내부 호출부 2곳을 고친다:

`kakaoTaxiGroupIdForBranch` (기존 1522행):

```javascript
  const groups = kakaoTaxiAccountGroups("acct1") || [];
```

`submitBranchKakaoRegister` 안의 중복 안내 부분(기존 1554행):

```javascript
  var allGroups = (getKakaoTaxiGroups() || {}).groups || [];
```

`getKakaoTaxiBranchMembers` 안(기존 1489행)도 형태는 그대로지만 전 계정 결과를 받게 된다:

```javascript
  const all = getKakaoTaxiMembers(forceRefresh).members || [];
```

- [ ] **Step 8: 구조 검증**

Run: `grep -n "kakaoTaxiFetch(" gas/Code.gs`
Expected: 모든 호출의 첫 인자가 계정 키(`acc.key` 또는 `"acct1"`)다. 하나라도 `kakaoTaxiFetch("GET"` / `kakaoTaxiFetch("POST"` 처럼 HTTP 메서드로 시작하면 고친다.

Run: `grep -n "getKakaoTaxiGroups()" gas/Code.gs`
Expected: 남은 호출이 모두 `.groups` 를 꺼내 쓴다.

- [ ] **Step 9: 커밋**

```bash
git add gas/Code.gs
git commit -m "feat(kakao/gas): 계정 레지스트리 도입, 조회 3종 2계정 통합"
```

---

### Task 4: server.ts 로컬 프록시 미러

**Files:**
- Modify: `server.ts:214-320` (자격증명·서명), `server.ts:450-520` (지점 필터·그룹 매핑), 조회 액션 분기

**Interfaces:**
- Consumes: Task 3이 정한 반환 형태(`accountErrors`, `account_key`)
- Produces: GAS와 동일한 액션 응답 — 로컬 dev와 운영의 화면 코드가 같아야 한다

- [ ] **Step 1: `.env.example` 에 계정 #2 항목을 추가한다**

`.env.example` 에 아래 두 줄을 추가한다(**값은 비워 둔다 — 실제 시크릿 금지**):

```
KAKAO_T_CORP_ID_2=
KAKAO_T_SECRET_2=
```

- [ ] **Step 2: 계정 레지스트리를 옮긴다**

`server.ts` 의 카카오 자격증명 블록(기존 289-292행 주변)을 교체한다:

```typescript
// [동기화] gas/Code.gs 의 KAKAO_TAXI_ACCOUNTS 와 같게 유지할 것.
// 계정 #1 은 기존 환경변수 이름을 그대로 쓴다 — .env 재작성 없이 기존 동작이 유지된다.
const KAKAO_TAXI_ACCOUNTS = [
  { key: "acct1", label: "1계정", corpIdEnv: "KAKAO_T_CORP_ID", secretEnv: "KAKAO_T_SECRET" },
  { key: "acct2", label: "2계정", corpIdEnv: "KAKAO_T_CORP_ID_2", secretEnv: "KAKAO_T_SECRET_2" },
] as const;

interface KakaoAccount { key: string; label: string; corpId: string; secret: string }

function kakaoTaxiAccounts(): KakaoAccount[] {
  const out: KakaoAccount[] = [];
  for (const acc of KAKAO_TAXI_ACCOUNTS) {
    const corpId = process.env[acc.corpIdEnv];
    const secret = process.env[acc.secretEnv];
    if (corpId && secret) out.push({ key: acc.key, label: acc.label, corpId, secret });
  }
  if (!out.length) {
    throw new Error("카카오T 연동 정보가 없습니다. .env 에 KAKAO_T_CORP_ID / KAKAO_T_SECRET 를 등록해주세요.");
  }
  return out;
}

function kakaoTaxiCredentials(accountKey: string): KakaoAccount {
  const found = kakaoTaxiAccounts().find((a) => a.key === accountKey);
  if (!found) throw new Error(`등록되지 않은 카카오T 계정입니다: ${accountKey}`);
  return found;
}

// 등록된 계정을 순회해 합치고, 실패한 계정은 accountErrors 로 알린다. 전부 실패면 던진다.
// gas/Code.gs 의 kakaoTaxiCollect 와 같은 규칙.
async function kakaoTaxiCollect<T extends Record<string, unknown>>(
  collect: (acc: KakaoAccount) => Promise<T[]>
): Promise<{ items: T[]; accountErrors: Array<{ key: string; label: string; message: string }> }> {
  const accounts = kakaoTaxiAccounts();
  const items: T[] = [];
  const accountErrors: Array<{ key: string; label: string; message: string }> = [];
  for (const acc of accounts) {
    try {
      const got = await collect(acc);
      for (const row of got) {
        (row as Record<string, unknown>).account_key = acc.key;
        items.push(row);
      }
    } catch (e) {
      accountErrors.push({ key: acc.key, label: acc.label, message: String((e as Error)?.message || e) });
    }
  }
  if (accountErrors.length === accounts.length) {
    throw new Error(`카카오T 조회에 실패했습니다: ${accountErrors[0].message}`);
  }
  return { items, accountErrors };
}
```

- [ ] **Step 3: 서명·조회 함수에 계정을 전달한다**

`server.ts` 의 카카오 fetch 함수 시그니처를 `(accountKey, method, path, query, body)` 로 바꾸고, 헤더의 `x-mob-b2b-corp-id` 와 서명 메시지의 기업ID가 `kakaoTaxiCredentials(accountKey)` 값을 쓰도록 고친다. 서명 URL에는 쿼리를 넣지 않는다.

이용내역·그룹·직원 조회 함수는 `kakaoTaxiCollect` 로 감싸 GAS와 같은 형태를 돌려준다:

```typescript
// 이용내역 — 로컬은 캐시 없이 매번 실시간 조회한다(GAS 의 ScriptCache 최적화는 운영 전용).
async function kakaoTaxiOrders(month: string) {
  const range = kakaoTaxiMonthRange(month);
  const collected = await kakaoTaxiCollect(async (acc) =>
    (await kakaoTaxiFetchAllPages(acc.key, "/external/v2/orders",
      `start_date=${range.start}&end_date=${range.end}`, "orders")).items
  );
  return { month, count: collected.items.length, orders: collected.items, accountErrors: collected.accountErrors };
}

async function kakaoTaxiGroups() {
  const collected = await kakaoTaxiCollect(async (acc) =>
    (await kakaoTaxiFetch(acc.key, "GET", "/external/v1/groups", null, null)) || []
  );
  return { groups: collected.items, accountErrors: collected.accountErrors };
}

async function kakaoTaxiMembers() {
  const collected = await kakaoTaxiCollect(async (acc) =>
    (await kakaoTaxiFetchAllPages(acc.key, "/external/v2/members/connected", "", "members")).items
  );
  return { count: collected.items.length, members: collected.items, accountErrors: collected.accountErrors };
}
```

쓰기 함수(등록·수정·차단·삭제·알림톡)는 Task 8까지 임시로 `"acct1"` 을 넘긴다.

- [ ] **Step 4: 타입 검사**

Run: `npm run lint`
Expected: PASS — 오류 0건. Task 1에서 기록해 둔 `server.ts` 오류가 여기서 전부 사라져야 한다.

- [ ] **Step 5: 실계정 스모크(로컬)**

`.env` 에 계정 #2 값을 넣고 로컬 서버를 띄운다.

Run: `npm run dev`
브라우저에서 `localhost:3000` 관리자 > 법인택시 > 이용내역을 연다.
Expected: 2026-07 이용내역에 계정 #1·#2 건이 함께 나온다. **기준값** — 계정 #2가 사카바단단 86건 1,392,700원 / 8번대물집 20건 225,300원(7월 28일 기준, 이후 증가 가능).

- [ ] **Step 6: 커밋**

```bash
git add server.ts .env.example
git commit -m "feat(kakao/server): 로컬 프록시에 계정 레지스트리·조회 통합 미러"
```

---

### Task 5: gasClient 응답 타입과 쓰기 시그니처

**Files:**
- Modify: `src/api/gasClient.ts:792-843` (메서드), `:884-922` (타입)

**Interfaces:**
- Consumes: Task 3·4의 응답 형태
- Produces:
  - `KakaoTaxiAccountError { key: string; label: string; message: string }`
  - `KakaoTaxiOrdersResult` 에 `accountErrors: KakaoTaxiAccountError[]`
  - `getKakaoTaxiGroups(...)` → `Promise<{ groups: KakaoTaxiGroup[]; accountErrors: KakaoTaxiAccountError[] }>`
  - `getKakaoTaxiMembers(...)` → `Promise<{ count: number; members: KakaoTaxiMember[]; accountErrors: KakaoTaxiAccountError[] }>`
  - 쓰기 메서드 5종이 `accountKey: string` 을 받는다

- [ ] **Step 1: 타입을 추가한다**

`src/api/gasClient.ts` 의 `KakaoTaxiOrdersResult` 위에 추가:

```typescript
/** 계정 하나가 실패했을 때 — 성공한 계정 데이터는 그대로 오고, 화면이 이 목록으로 경고 배너를 띄운다 */
export interface KakaoTaxiAccountError {
  key: string;
  label: string;
  message: string;
}

export interface KakaoTaxiOrdersResult {
  month: string;
  count: number; // 수집본 건수
  orders: KakaoTaxiOrder[];
  accountErrors: KakaoTaxiAccountError[];
}
```

- [ ] **Step 2: 조회 메서드 반환 타입을 바꾼다**

```typescript
  async getKakaoTaxiGroups(adminPinHash: string, forceRefresh?: boolean): Promise<{ groups: KakaoTaxiGroup[]; accountErrors: KakaoTaxiAccountError[] }> {
    return callApi("getKakaoTaxiGroups", { adminPinHash, forceRefresh });
  },

  async getKakaoTaxiMembers(adminPinHash: string, forceRefresh?: boolean): Promise<{ count: number; members: KakaoTaxiMember[]; accountErrors: KakaoTaxiAccountError[] }> {
    return callApi("getKakaoTaxiMembers", { adminPinHash, forceRefresh });
  },
```

- [ ] **Step 3: 쓰기 메서드에 `accountKey` 를 넣는다**

```typescript
  // [주의] 쓰기는 계정을 반드시 지정한다. 기본값을 두면 엉뚱한 계정에 삭제·수정이 나갈 수 있다.
  async updateKakaoTaxiMember(memberId: string, member: KakaoTaxiMemberUpdateInput, adminPinHash: string, accountKey: string): Promise<KakaoTaxiMember> {
    return callApi("updateKakaoTaxiMember", { memberId, member, adminPinHash, accountKey });
  },

  async blockKakaoTaxiMember(memberIds: string[], adminPinHash: string, accountKey: string): Promise<Array<{ id: string; status_code: number; status_msg: string }>> {
    return callApi("blockKakaoTaxiMember", { memberIds, adminPinHash, accountKey });
  },

  async unblockKakaoTaxiMember(memberIds: string[], adminPinHash: string, accountKey: string): Promise<Array<{ id: string; status_code: number; status_msg: string }>> {
    return callApi("unblockKakaoTaxiMember", { memberIds, adminPinHash, accountKey });
  },

  async deleteKakaoTaxiMember(memberId: string, adminPinHash: string, accountKey: string): Promise<{ success: boolean }> {
    return callApi("deleteKakaoTaxiMember", { memberId, adminPinHash, accountKey });
  },

  async sendKakaoTaxiMemberTms(memberId: string, adminPinHash: string, accountKey: string): Promise<{ success: boolean }> {
    return callApi("sendKakaoTaxiMemberTms", { memberId, adminPinHash, accountKey });
  },

  async registerKakaoTaxiMember(member: KakaoTaxiMemberInput, adminPinHash: string, accountKey: string): Promise<KakaoTaxiMember> {
    return callApi("registerKakaoTaxiMember", { member, adminPinHash, accountKey });
  },
```

- [ ] **Step 4: 타입 검사로 호출부를 찾아낸다**

Run: `npm run lint`
Expected: FAIL — `KakaoTaxiSection.tsx` 의 쓰기 호출들이 인자 부족으로 오류. **이 오류 목록이 Task 6의 작업 목록이다.** 출력을 기록해 둔다.

- [ ] **Step 5: 커밋(타입 오류가 남은 상태 — Task 6에서 해소)**

```bash
git add src/api/gasClient.ts
git commit -m "feat(kakao/client): 응답에 accountErrors, 쓰기 메서드에 accountKey 추가"
```

---

### Task 6: 관리자 화면 — 계정 컬럼·필터·실패 배너

**Files:**
- Modify: `src/pages/admin/KakaoTaxiSection.tsx` (조회 105-160행 주변, 직원 표 1480-1540행 주변, 이용내역 표)

**Interfaces:**
- Consumes: Task 1의 `accountLabel`·`NormalizedTaxiOrder.accountKey`, Task 5의 `accountErrors`·쓰기 시그니처
- Produces: 없음(최종 소비자)

- [ ] **Step 1: 조회 결과의 실패 목록을 상태로 받는다**

`KakaoTaxiSection.tsx` 의 상태 선언부(50-70행 주변)에 추가:

```typescript
  const [accountErrors, setAccountErrors] = useState<KakaoTaxiAccountError[]>([]);
  const [accountFilter, setAccountFilter] = useState("all");
```

이용내역 로드(105-130행 주변)에서 응답의 `accountErrors` 를 담고, 직원 로드(152-155행 주변)에서 `getKakaoTaxiGroups`·`getKakaoTaxiMembers` 의 새 반환 형태(`.groups` / `.members`)를 꺼내 쓰도록 고친다. 두 로드의 실패 목록은 합쳐서 `setAccountErrors` 한다.

- [ ] **Step 2: 실패 배너를 렌더한다**

이용내역·직원 관리·이상 점검 뷰의 표 위(기존 `ERROR_BANNER` 를 쓰는 자리 근처)에 추가한다:

```tsx
{accountErrors.length > 0 && (
  <div className={ERROR_BANNER}>
    {accountErrors.map((e) => e.label).join(", ")} 조회 실패 — 아래 집계에 해당 계정 내역이 빠져 있습니다.
    잠시 후 새로고침해주세요. ({accountErrors[0].message})
  </div>
)}
```

- [ ] **Step 3: 이용내역 표에 계정 열을 넣는다**

표 헤더의 `직원` 열 앞에 `<th className="text-[11px] font-bold ...">계정</th>` 을 추가하고, 각 행에 대응 셀을 넣는다:

```tsx
<td className="text-[12px]">{accountLabel(row.accountKey)}</td>
```

`colSpan` 을 쓰는 빈 상태 행이 있으면 숫자를 1 늘린다.

- [ ] **Step 4: 계정 필터를 넣는다**

지점 필터(`branchFilter`) 옆에 같은 스타일로 추가한다:

```tsx
<select
  value={accountFilter}
  onChange={(e) => setAccountFilter(e.target.value)}
  className="text-[11px] border rounded px-2 py-1"
>
  <option value="all">전체 계정</option>
  <option value="acct1">1계정</option>
  <option value="acct2">2계정</option>
</select>
```

행 필터링에 조건을 추가한다:

```typescript
  const rows = useMemo(
    () => baseRows.filter((r) => accountFilter === "all" || r.accountKey === accountFilter),
    [baseRows, accountFilter]
  );
```

> 필터는 `normalizeKakaoTaxiOrders` 결과에 적용한다. 집계(`aggregateByBranch`·`aggregateByMember`)는 이미 `rows` 를 받으므로 자동으로 따라간다.

- [ ] **Step 5: 직원 표에 계정 열을 넣고 쓰기 호출에 계정을 전달한다**

직원 관리 표 헤더에 `계정` 열을 추가하고 각 행에 `{accountLabel(m.account_key)}` 를 넣는다.

Task 5에서 기록해 둔 타입 오류 목록의 호출부를 전부 고친다:

```tsx
() => gasClient.unblockKakaoTaxiMember([m.id], adminPinHash, m.account_key)
() => gasClient.blockKakaoTaxiMember([m.id], adminPinHash, m.account_key)
() => gasClient.deleteKakaoTaxiMember(m.id, adminPinHash, m.account_key)
```

직원 수정 저장(기존 542행 주변)과 알림톡 발송(387·425·796행 주변), 신청 승인 시 등록(785행 주변)·삭제(842행 주변)도 같은 방식으로 계정을 넘긴다. **수정 대상의 계정은 그 행의 `account_key` 를 쓴다** — 화면 어딘가에 고정값을 박지 않는다.

신규 등록 폼(397-430행 주변)은 계정 선택이 필요하다. 그룹 드롭다운이 이미 계정별 그룹을 모두 담고 있으므로, **선택한 그룹의 `account_key` 를 등록 계정으로 쓴다**:

```tsx
const selectedGroup = membersData?.groups.find((g) => g.id === regGroupId);
if (!selectedGroup) { setRegError("그룹을 선택해주세요."); return; }
const created = await gasClient.registerKakaoTaxiMember(input, adminPinHash, selectedGroup.account_key);
```

그룹 드롭다운의 표시명에 계정을 덧붙여 어느 계정 그룹인지 보이게 한다:

```tsx
<option key={g.id} value={g.id}>{`${g.name} (${accountLabel(g.account_key)})`}</option>
```

- [ ] **Step 6: 타입 검사**

Run: `npm run lint`
Expected: PASS — 오류 0건

- [ ] **Step 7: 로컬 화면 확인**

Run: `npm run dev`
`localhost:3000` 관리자 > 법인택시에서 확인한다:
- 이용내역에 계정 열이 보이고, 계정 필터가 동작한다
- 2026-07 전체 집계에 사카바단단·8번대물집이 나온다
- 직원 관리에 계정 열이 보이고, 계정 #2 직원(정승화·이윤지·정윤기·김지혜·최우석·이태현·백승윤)이 목록에 있다
- 이상 점검에 계정 #2 건이 섞여 나온다

**화면을 사용자에게 보여주고 확인받는다. 여기서 멈춘다 — 푸시·배포하지 않는다.**

- [ ] **Step 8: 커밋**

```bash
git add src/pages/admin/KakaoTaxiSection.tsx
git commit -m "feat(kakao/admin): 계정 컬럼·필터·조회 실패 배너, 쓰기에 계정 라우팅"
```

---

### Task 7: 지점 화면 계정 통합

**Files:**
- Modify: `src/pages/branch/tabs/BusinessTaxiTab.tsx`
- Modify: `gas/Code.gs` `getKakaoTaxiBranchMembers` (기존 1477-1495행), `server.ts` `kakaoTaxiBranchMembers` (기존 459행 주변)

**Interfaces:**
- Consumes: Task 3·4의 전 계정 직원 목록
- Produces: 없음(최종 소비자)

- [ ] **Step 1: 백엔드 지점 필터가 전 계정을 대상으로 하는지 확인한다**

`getKakaoTaxiBranchMembers` 는 `getKakaoTaxiMembers(forceRefresh).members` 를 필터하므로 Task 3 이후 이미 전 계정을 본다. **부서(`department`) 기준 필터라 계정 #2 직원도 지점명으로 잡힌다.** 코드 변경 없이 동작하는지 확인만 한다.

Run: `grep -n "getKakaoTaxiMembers(forceRefresh).members" gas/Code.gs`
Expected: `getKakaoTaxiBranchMembers` 안에서 1건 매칭

> **지점 화면에 계정을 노출하지 않는다.** 지점 입장에서 계정 구분은 의미가 없고, 자기 지점 사람만 보이면 충분하다. 계정 열은 관리자 화면에만 둔다.

- [ ] **Step 2: 지점 화면 타입 오류를 고친다**

Run: `npm run lint`
`BusinessTaxiTab.tsx` 에서 `KakaoTaxiMember.account_key` 추가나 쓰기 시그니처 변경으로 오류가 나면 고친다. 지점 화면의 쓰기는 `submitBranchKakaoRegister` 뿐이며, 이 액션은 지점명으로 계정을 정하므로(Task 8) 프론트에서 계정을 넘기지 않는다.

Expected: PASS — 오류 0건

- [ ] **Step 3: 로컬 확인**

Run: `npm run dev`
지점 PIN으로 **사카바단단** 로그인 → 비즈니스택시 탭.
Expected: 등록된 인원에 정승화·이윤지·김지혜·최우석·이태현·백승윤 6명이 보인다.
**8번대물집**으로 로그인하면 정윤기 1명만 보인다. **다른 지점 사람이 섞여 보이면 안 된다.**

- [ ] **Step 4: 커밋**

```bash
git add src/pages/branch/tabs/BusinessTaxiTab.tsx
git commit -m "feat(kakao/branch): 지점 화면 등록 인원 2계정 통합"
```

---

### Task 8: 지점→계정 매핑과 쓰기 라우팅 완성 (2단계)

**Files:**
- Modify: `gas/Code.gs` (쓰기 함수 5종, `kakaoTaxiGroupIdForBranch`, `submitBranchKakaoRegister`, doPost 분기 96-140행)
- Modify: `server.ts` (같은 로직 미러)
- Modify: `scripts/verify-kakao-multiaccount.mts` (매핑표 검증 추가)

**Interfaces:**
- Consumes: Task 3·4의 계정 레지스트리
- Produces:
  - `KAKAO_ACCOUNT_BY_BRANCH: Record<string, string>` — 미기재 지점은 `acct1`
  - `kakaoTaxiAccountForBranch(branchName: string): string`
  - `kakaoTaxiGroupIdForBranch(branchName)` → `{ accountKey, groupId }`

- [ ] **Step 1: 매핑표 검증을 먼저 추가한다**

`scripts/verify-kakao-multiaccount.mts` 에 추가한다(프론트 상수를 기준으로 검증):

```typescript
import { KAKAO_ACCOUNT_BY_BRANCH, kakaoTaxiAccountForBranch } from "../src/pages/admin/helpers/kakaoTaxi";

console.log("[7] 지점→계정 매핑");
check("사카바단단은 2계정", kakaoTaxiAccountForBranch("사카바단단") === "acct2");
check("8번대물집은 2계정", kakaoTaxiAccountForBranch("8번대물집") === "acct2");
check("그 외 지점은 1계정", kakaoTaxiAccountForBranch("대물섬 한남점") === "acct1");
check("빈 지점명도 1계정", kakaoTaxiAccountForBranch("") === "acct1");
check("매핑표에 acct2 만 기재", Object.values(KAKAO_ACCOUNT_BY_BRANCH).every((v) => v === "acct2"));
```

Run: `npx tsx scripts/verify-kakao-multiaccount.mts`
Expected: FAIL — `kakaoTaxiAccountForBranch` 가 없어 import 오류

- [ ] **Step 2: 프론트 상수에 매핑표를 넣는다**

`src/pages/admin/helpers/kakaoTaxi.ts` 의 `KAKAO_RETIRED_MEMBER_BRANCH` 아래에 추가:

```typescript
// 지점 → 카카오T 계정. 미기재 지점은 계정 #1(기본값)이다.
// 계정 #2(기업ID 25648071)는 사카바단단·8번대물집 두 지점 인원만 쓴다(2026-07-28 확인).
// [동기화] gas/Code.gs, server.ts 의 같은 이름 상수와 세 곳을 같게 유지할 것.
// [주의] 계정 #1 에도 '사카바단단'·'8번대물집' 이름의 그룹이 껍데기로 남아 있다 —
// 이 표가 없으면 지점 자동등록이 그 껍데기 그룹을 찾아 엉뚱한 계정에 등록한다.
export const KAKAO_ACCOUNT_BY_BRANCH: Record<string, string> = {
  "사카바단단": "acct2",
  "8번대물집": "acct2",
};

export function kakaoTaxiAccountForBranch(branchName: string): string {
  return KAKAO_ACCOUNT_BY_BRANCH[String(branchName || "").trim()] || "acct1";
}
```

Run: `npx tsx scripts/verify-kakao-multiaccount.mts`
Expected: PASS

- [ ] **Step 3: GAS 쓰기 함수 5종에 계정을 받게 한다**

`gas/Code.gs` — Task 3에서 `"acct1"` 로 임시 고정한 자리를 인자로 바꾼다:

```javascript
function registerKakaoTaxiMember(accountKey, member) {
  const m = member || {};
  const norm = kakaoTaxiNormalizeContact(m);
  if (!m.identifier || !norm.phone || !norm.groupIds.length) {
    throw new Error("직원 등록에는 사번(identifier)·휴대전화번호·그룹이 모두 필요합니다.");
  }
  const body = {
    identifier: String(m.identifier),
    mobile_phone: norm.phone,
    group_ids: norm.groupIds
  };
  if (m.name) body.name = String(m.name);
  if (m.department) body.department = String(m.department);
  const res = kakaoTaxiFetch(accountKey, "POST", "/external/v1/members", null, body);
  kakaoTaxiInvalidateMembersCache(accountKey);
  return res;
}

function updateKakaoTaxiMember(accountKey, memberId, member) {
  if (!memberId) throw new Error("수정할 직원이 지정되지 않았습니다.");
  const m = member || {};
  const norm = kakaoTaxiNormalizeContact(m);
  if (!norm.phone || !norm.groupIds.length) {
    throw new Error("직원 수정에는 휴대전화번호와 그룹이 모두 필요합니다.");
  }
  // [함정] 카카오 수정 API 는 name/department 를 보내지 않으면(null) 공백으로 지워버린다.
  // 그래서 화면이 기존 값을 채워 보내는 것을 전제로, 여기서도 4개 필드를 항상 모두 보낸다.
  // 전화번호가 실제로 바뀐 경우 카카오가 새 번호로 인증 알림톡을 자동 발송한다(문서 명시).
  const body = {
    mobile_phone: norm.phone,
    group_ids: norm.groupIds,
    name: m.name ? String(m.name) : "",
    department: m.department ? String(m.department) : ""
  };
  const res = kakaoTaxiFetch(accountKey, "PUT", "/external/v1/members/" + encodeURIComponent(memberId), null, body);
  kakaoTaxiInvalidateMembersCache(accountKey);
  return res;
}
```

`setKakaoTaxiMemberBlocked(accountKey, memberIds, blocked)`, `deleteKakaoTaxiMember(accountKey, memberId)`, `sendKakaoTaxiMemberTms(accountKey, memberId)` 도 같은 방식으로 첫 인자를 추가하고, 내부 `kakaoTaxiFetch`·`kakaoTaxiInvalidateMembersCache` 에 넘긴다. `setKakaoTaxiMemberBlocked` 의 무효화는 **부분 성공을 숨기지 않도록 `throw` 전에** 호출한다(기존 규칙 유지).

- [ ] **Step 4: doPost 분기에서 계정을 검증해 넘긴다**

`gas/Code.gs:96-140` 의 카카오 액션 분기에 계정 추출 헬퍼를 추가하고 쓰기 액션에 적용한다:

```javascript
// 쓰기 액션 전용 — 계정을 반드시 명시받는다. 기본값을 두면 엉뚱한 계정에 삭제·수정이 나간다.
function requireKakaoAccountKey(accountKey) {
  var key = String(accountKey || "").trim();
  if (!key) throw new Error("대상 카카오T 계정이 지정되지 않았습니다. 화면을 새로고침한 뒤 다시 시도해주세요.");
  kakaoTaxiCredentials(key); // 등록된 계정인지 확인 — 아니면 던진다
  return key;
}
```

분기 예:

```javascript
      case "updateKakaoTaxiMember":
        requireKakaoTaxiAdmin(requestData.adminPinHash);
        result = updateKakaoTaxiMember(
          requireKakaoAccountKey(requestData.accountKey),
          requestData.memberId,
          requestData.member
        );
        break;
```

`registerKakaoTaxiMember`·`blockKakaoTaxiMember`·`unblockKakaoTaxiMember`·`deleteKakaoTaxiMember`·`sendKakaoTaxiMemberTms` 도 같은 방식으로 고친다.

- [ ] **Step 5: 지점 자동등록이 매핑표로 계정을 고르게 한다**

`gas/Code.gs` 에 매핑표를 추가한다(별칭표 바로 아래):

```javascript
// 지점 → 카카오T 계정. 미기재 지점은 계정 #1(기본값).
// [동기화] server.ts, src/pages/admin/helpers/kakaoTaxi.ts 와 세 곳을 같게 유지할 것.
var KAKAO_ACCOUNT_BY_BRANCH = {
  "사카바단단": "acct2",
  "8번대물집": "acct2"
};

function kakaoTaxiAccountForBranch(branchName) {
  return KAKAO_ACCOUNT_BY_BRANCH[String(branchName || "").trim()] || "acct1";
}
```

`kakaoTaxiGroupIdForBranch` 를 계정 인식으로 바꾼다:

```javascript
// 지점명 → { 계정, 활성(enabled) 그룹 id }. 지점 자동등록은 지점이 그룹을 직접 고르지 않으므로
// 백엔드가 정한다. 못 찾으면 groupId 가 null → 호출부가 거부(오등록 방지).
// [주의] 계정 #1 에도 사카바단단·8번대물집 이름의 껍데기 그룹이 있으므로 반드시 매핑표가 정한
// 계정 안에서만 찾는다. 계정 전체를 뒤지면 엉뚱한 계정에 등록된다.
function kakaoTaxiGroupIdForBranch(branchName) {
  const target = String(branchName || "").trim();
  const accountKey = kakaoTaxiAccountForBranch(target);
  if (!target) return { accountKey: accountKey, groupId: null };
  // [쓰기 경로] 조회용 캐시(최대 5분 묵음)를 쓰면 방금 만든/바꾼 그룹을 못 찾아 오등록될 수 있으니
  // 항상 실시간 조회한다.
  const groups = kakaoTaxiAccountGroups(accountKey) || [];
  var fallback = null;
  for (var i = 0; i < groups.length; i++) {
    var g = groups[i];
    if (String(g.status) !== "enabled") continue;
    var gn = String(g.name || "").trim();
    if (gn === target || KAKAO_TAXI_BRANCH_ALIASES[gn] === target) return { accountKey: accountKey, groupId: g.id };
    fallback = fallback === null ? g.id : fallback;
  }
  // 계정 #2 처럼 지점명 그룹이 없고 활성 그룹이 '기본그룹' 하나뿐이면 그 그룹에 넣는다.
  // 지점 판정은 부서(department=지점명) 우선이라 집계는 정상 동작한다.
  var enabledCount = 0;
  for (var k = 0; k < groups.length; k++) if (String(groups[k].status) === "enabled") enabledCount++;
  if (enabledCount === 1 && fallback) return { accountKey: accountKey, groupId: fallback };
  return { accountKey: accountKey, groupId: null };
}
```

`submitBranchKakaoRegister` 의 호출부를 고친다:

```javascript
  var resolved = kakaoTaxiGroupIdForBranch(branchName);
  var groupId = resolved.groupId;
  var accountKey = resolved.accountKey;
  if (!groupId) throw new Error("이 지점에 해당하는 카카오T 그룹을 찾지 못했습니다. 관리자에게 문의해주세요.");
```

그리고 등록 호출을 `registerKakaoTaxiMember(accountKey, {...})`, 알림톡을 `sendKakaoTaxiMemberTms(accountKey, member.id)` 로 바꾼다.

- [ ] **Step 6: 중복 전화 검사를 전 계정으로 확대한다**

`submitBranchKakaoRegister` 의 중복 검사(기존 1551-1565행)에서 `getKakaoTaxiMembers()` 는 Task 3 이후 이미 전 계정을 돌려준다. 안내 문구에 계정을 덧붙인다:

```javascript
  var existing = (getKakaoTaxiMembers().members) || [];
  var allGroups = (getKakaoTaxiGroups() || {}).groups || [];
  for (var j = 0; j < existing.length; j++) {
    if (String(existing[j].mobile_phone || "").replace(/[^0-9]/g, "") === cleanPhone) {
      var ex = existing[j];
      var where = String(ex.department || "").trim();
      if (!where && ex.group_ids && ex.group_ids.length) {
        for (var g = 0; g < allGroups.length; g++) {
          if (allGroups[g].id === ex.group_ids[0] && allGroups[g].account_key === ex.account_key) {
            where = String(allGroups[g].name || ""); break;
          }
        }
      }
      if (!where) where = "다른 지점";
      throw new Error((ex.name || cleanName) + " 님(" + cleanPhone + ")은 이미 '" + where + "'에 등록돼 있습니다. 같은 번호는 중복 등록할 수 없습니다. 전입한 직원이라면 관리자에게 소속(그룹) 변경을 요청해주세요.");
    }
  }
```

> 그룹 id 대조에 `account_key` 를 함께 본다 — 계정이 다르면 같은 그룹 id 가 존재할 수 있다.

- [ ] **Step 7: server.ts 에 같은 변경을 미러한다**

`server.ts` 의 쓰기 함수 5종·`kakaoTaxiGroupIdForBranch`·`kakaoTaxiSubmitBranchRegister`·중복 검사·액션 분기에 Step 3~6과 동일한 변경을 적용한다. 매핑표 상수도 같은 값으로 둔다.

- [ ] **Step 8: 검증**

Run: `npx tsx scripts/verify-kakao-multiaccount.mts`
Expected: PASS — `[1]`~`[7]` 전부 `OK`

Run: `npm run lint`
Expected: PASS — 오류 0건

Run: `grep -n "kakaoTaxiFetch(" gas/Code.gs server.ts`
Expected: `"acct1"` 하드코딩이 **하나도 남아 있지 않다.** 모두 인자로 받은 계정을 쓴다.

- [ ] **Step 9: 로컬 동작 확인**

Run: `npm run dev`
- 관리자 > 법인택시 > 직원 관리에서 **계정 #2 직원의 부서를 바꿔 저장** → 저장 성공, 목록 갱신, 이용내역 집계도 새 지점으로 바뀐다
- 관리자에서 **계정 #1 직원**을 수정 → 계정 #1에 반영된다(서로 섞이지 않는다)
- 지점 화면(사카바단단)에서 **이용신청**에 이미 등록된 번호를 넣으면 "이미 '사카바단단'에 등록돼 있습니다" 안내가 뜬다

**사용자에게 화면을 보여주고 확인받는다.**

- [ ] **Step 10: 커밋**

```bash
git add gas/Code.gs server.ts src/pages/admin/helpers/kakaoTaxi.ts scripts/verify-kakao-multiaccount.mts
git commit -m "feat(kakao): 지점→계정 매핑과 쓰기 계정 라우팅, 중복 검사 전 계정 확대"
```

---

### Task 9: 배포 준비 — 문서·리뷰·자격증명 등록 절차

**Files:**
- Modify: `AGENTS.md` (카카오 2계정 운영 규칙 한 줄)
- Modify: `docs/superpowers/specs/2026-07-28-카카오T-2계정-지원-설계.md` (구현 완료 표시)

**Interfaces:**
- Consumes: Task 1~8 전부
- Produces: 없음

- [ ] **Step 1: 운영 규칙을 `AGENTS.md` 에 적는다**

카카오 관련 절이 있으면 그 아래, 없으면 말미에 추가한다:

```markdown
### 카카오T 비즈니스 2계정
- 계정 #1(기존)과 계정 #2가 있다. 계정 #2 = 사카바단단·8번대물집 인원 전용.
- 자격증명은 GAS 스크립트 속성(`KAKAO_T_CORP_ID_2`/`KAKAO_T_SECRET_2`)·로컬 `.env` 에만. 저장소 금지.
- 지점→계정 매핑(`KAKAO_ACCOUNT_BY_BRANCH`)은 `gas/Code.gs`·`server.ts`·`src/pages/admin/helpers/kakaoTaxi.ts` 세 곳을 같게 유지한다.
- **퇴사자를 카카오에서 삭제하지 말 것.** 삭제하면 그 사람 과거 주문의 `member_department` 가 null 이 되어 지점 비용에서 빠진다. 이미 지운 건은 `KAKAO_RETIRED_MEMBER_BRANCH` 에 `계정|member_id` 로 추가한다.
- 카카오 코드를 고쳤으면 앱 푸시만으로 끝나지 않는다 — `gas/Code.gs` 는 clasp 재배포까지 해야 반영된다.
```

- [ ] **Step 2: 설계 문서에 구현 완료를 표시한다**

`docs/superpowers/specs/2026-07-28-카카오T-2계정-지원-설계.md` 말미에 추가:

```markdown
## 구현

계획: `docs/superpowers/plans/2026-07-28-카카오T-2계정-지원.md`
```

- [ ] **Step 3: 전체 검증을 다시 돌린다**

Run: `npx tsx scripts/verify-kakao-multiaccount.mts && npm run lint`
Expected: 둘 다 PASS

- [ ] **Step 4: Codex 지독한 리뷰**

카카오 관련 변경 전체(`gas/Code.gs`·`server.ts`·`gasClient.ts`·`kakaoTaxi.ts`·`kakaoTaxiAnomaly.ts`·`KakaoTaxiSection.tsx`·`BusinessTaxiTab.tsx`)를 대상으로 Codex 리뷰를 요청한다. 백그라운드로 돌리면 Monitor 하트비트를 함께 걸어 3분마다 상태를 보고한다.

특히 확인받을 것:
- 계정 하나가 실패했을 때 나머지 집계가 조용히 축소되지 않는가
- 쓰기 액션이 계정 없이 통과할 수 있는 경로가 남아 있지 않은가
- 캐시 무효화가 계정별로 정확한가(부서를 고쳤는데 집계가 그대로인 경우가 없는가)
- 지점 화면에 타 지점·타 계정 직원 정보가 새지 않는가

지적 사항을 전부 반영한 뒤 다시 검증을 돌린다.

- [ ] **Step 5: 커밋**

```bash
git add AGENTS.md docs/superpowers/specs/2026-07-28-카카오T-2계정-지원-설계.md
git commit -m "docs(kakao): 2계정 운영 규칙과 구현 계획 링크"
```

- [ ] **Step 6: 배포 — 사용자 지시를 받은 뒤에만**

**여기서 멈추고 사용자에게 배포 여부를 묻는다.** "배포해" 지시를 받으면 아래 두 가지를 **모두** 수행한다(한쪽만으로는 동작하지 않는다 — 핵심이 `Code.gs`다):

1. **GAS 스크립트 속성 등록** — Apps Script 편집기 > 프로젝트 설정 > 스크립트 속성에 `KAKAO_T_CORP_ID_2` / `KAKAO_T_SECRET_2` 를 등록한다. **속성을 먼저 등록해도 안전하다**(코드가 없으면 무시된다). 안 하면 계정 #2가 조용히 빠진 채 계정 #1만 나온다.
2. **배포 순서**
   - 먼저 clasp clone 으로 운영본을 받아 저장소 직전 커밋과 diff 해 드리프트가 없는지 확인한다
   - `clasp push --force` → `clasp create-version "카카오T 2계정 지원"` → `clasp redeploy AKfycby__e1D6... -V <n> -d "..."` (/exec URL 유지)
   - `git push` (GitHub Actions → GitHub Pages)
   - **마감 시간대에는 배포하지 않는다.**

배포 후 운영에서 확인한다: 관리자 > 법인택시 이용내역에 계정 #2 건이 나오는지, 지점 화면(사카바단단)에 6명이 보이는지.

---

## Self-Review

**스펙 커버리지**

| 설계 문서 항목 | 담당 태스크 |
|---|---|
| §1 계정 레지스트리 | Task 3(GAS), Task 4(server) |
| §2 조회 통합·계정별 캐시 | Task 3, Task 4 |
| §3 부분 실패 처리 | Task 3(`kakaoTaxiCollect`), Task 5(타입), Task 6(배너) |
| §4 지점→계정 매핑 | Task 8 |
| §5 중복·동명이인(`memberKey`·면제목록) | Task 1, Task 2, Task 8(중복 전화) |
| §5-1 퇴사자 귀속 보정 | Task 1 |
| §6 쓰기 경로 라우팅 | Task 5(시그니처), Task 6(관리자), Task 8(백엔드) |
| §7 화면(계정 컬럼·필터·배너) | Task 6, Task 7 |
| 배포 단계·GAS 절차 | Task 9 |

**타입 일관성 확인**
- `account_key`(백엔드·API 응답, snake_case) ↔ `accountKey`(프론트 정규화 결과·함수 인자, camelCase) — 경계가 `normalizeKakaoTaxiOrders` 한 곳이다
- `isAnomalyExempt(accountKey, memberId)` — Task 2에서 정의, Task 2 내 두 호출부에서만 사용
- `kakaoTaxiGroupIdForBranch` 반환이 `string | null` → `{accountKey, groupId}` 로 바뀐다 — 호출부는 `submitBranchKakaoRegister` 뿐이며 Task 8 Step 5에서 함께 고친다
- `getKakaoTaxiGroups` 반환이 배열 → `{groups, accountErrors}` 로 바뀐다 — 호출부는 GAS 내부 1곳(Task 3 Step 7), `KakaoTaxiSection.tsx` 1곳(Task 6 Step 1)

**남은 판단 사항**
- 계정 #2에 활성 그룹이 나중에 2개 이상 생기면 `kakaoTaxiGroupIdForBranch` 의 단일 그룹 fallback이 작동하지 않고 등록이 거부된다. 그때는 계정 #2에 지점명 그룹을 만들거나 매핑표를 그룹 단위로 확장해야 한다 — 지금은 거부(오등록 방지)가 안전한 기본값이다.
