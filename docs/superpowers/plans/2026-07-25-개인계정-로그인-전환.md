# 개인 계정 로그인 전환 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PIN 공통 로그인을 "구글/이메일 개인 계정 + 로그인 후 PIN 게이트" 방식으로 전환하고, 계정별 탭/지점 권한과 작성자 자동 기록을 붙인다.

**Architecture:** Firebase Auth(주 인스턴스)로 개인 로그인 → Firestore `users/{uid}` 프로필 로드/생성 → 보조 Auth("gate" named app)로 (지점/관리자 계정, PIN) 쌍 검증 → 세션에 pinHash 보관해 기존 GAS·카카오 경로 무수정 유지. 탭 권한은 UI 차단(레지스트리+내비게이션 가드), 데이터 규칙은 `users` 컬렉션만 신설.

**Tech Stack:** React 19 + TypeScript, Firebase JS SDK v12 (Auth/Firestore, Spark 요금제 — Cloud Functions 없음), 기존 GAS 백엔드.

**설계서:** `docs/superpowers/specs/2026-07-25-개인계정-로그인-전환-design.md` (v6) — 모든 태스크는 이 설계서를 따른다. 충돌 시 설계서가 우선.

## Global Constraints

- **커밋·푸시·배포 금지** — 사용자가 "배포해"라고 말하기 전까지. 각 태스크는 코드 수정+검증까지만. (AGENTS.md·CLAUDE.md 상시 규칙)
- 검증 게이트: 이 저장소는 테스트 프레임워크가 없다. 각 태스크의 게이트는 `npm run lint`(tsc --noEmit) 통과 + 해당 태스크의 수동 확인 절차. 최종적으로 `npm run dev`(localhost:3000) 화면 확인 + Codex 지독한 리뷰.
- 신규 가입 기본값(코드·규칙 완전 일치 필수): `role:"branch"`, `allowedTabs:["daily.settle"]`, `allowedBranches:"all"`, `status:"active"`, `reviewedByAdmin:false`
- 게이트 오류 정책: `auth/wrong-password`·`auth/invalid-credential`·`auth/user-not-found`·`auth/too-many-requests` → GAS 폴백 **금지**. `auth/network-request-failed`만 GAS `verifyPin` 폴백(+branchName/role 비교).
- 기존 PIN 로그인 플로우는 전환 기간 동안 **동작 그대로 보존** (하위호환 회귀 금지).
- UI 수정 시 DESIGN.md 규칙 적용 (관리자 화면은 `--branch-*` 토큰 사용 불가 주의).
- 세션 키: `erp_ugd_session_v2` (구 키 `erp_ugd_session`은 읽지 않고 삭제).

---

### Task 0: Firebase 콘솔 사전 작업 (수동 — 코드 아님)

**이 태스크는 사용자(또는 구현자가 사용자 승인 하에)가 Firebase 콘솔에서 수행한다. Task 3 이후의 실기기 확인에 필요하므로 초반에 해 둔다.**

- [ ] **Step 1:** Firebase 콘솔 → Authentication → Sign-in method → **Google** 제공업체 활성화 (지원 이메일 = 프로젝트 소유자 메일)
- [ ] **Step 2:** 같은 화면에서 **이메일/비밀번호** 제공업체 활성화 (이메일 링크 로그인은 끔)
- [ ] **Step 3:** Authentication → Settings → 승인된 도메인에 운영 도메인과 `localhost`가 있는지 확인
- [ ] **Step 4:** 첫 관리자 부트스트랩은 **Task 2 완료 후** 진행 (§Task 2 Step 5 참조 — uid를 알아야 함)

---

### Task 1: Firestore 규칙 — `users` 컬렉션 신설

**Files:**
- Modify: `firestore.rules`

**Interfaces:**
- Produces: `users/{uid}` 컬렉션 규칙 (본인 create=기본값만, 개인 관리자만 update/delete·본인 제외), `isPersonalAdmin()` 함수. Task 2의 클라이언트 코드가 이 규칙을 전제로 동작.

- [ ] **Step 1: 규칙 수정** — `firestore.rules`의 `isAdmin()` 함수 아래에 `isPersonalAdmin()`을 추가하고, `public_branches` match 앞에 `users` match를 추가:

```
    // 개인 계정 관리자 판정 — users 문서 기반. status까지 확인한다(정지된 관리자 배제).
    function isPersonalAdmin() {
      return signedIn()
        && exists(/databases/$(database)/documents/users/$(request.auth.uid))
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin'
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.status == 'active';
    }

    // 개인 계정 프로필. PIN 로그인 계정은 이 컬렉션을 사용하지 않는다.
    match /users/{uid} {
      // 본인 문서 읽기 + 개인 관리자 전체 읽기
      allow read: if signedIn() && (request.auth.uid == uid || isPersonalAdmin());
      // 본인 생성은 "신규 기본값 그대로"일 때만 — 셀프 권한 상향 차단
      allow create: if signedIn() && request.auth.uid == uid
        && request.resource.data.role == "branch"
        && request.resource.data.allowedTabs is list
        && request.resource.data.allowedTabs.size() == 1
        && request.resource.data.allowedTabs[0] == "daily.settle"
        && request.resource.data.allowedBranches == "all"
        && request.resource.data.status == "active"
        && request.resource.data.reviewedByAdmin == false;
      // 수정·삭제는 개인 관리자만, 본인 문서는 관리자라도 불가(셀프 정지해제·권한조작 차단).
      // 주의: PIN 관리자(admin@ugd-erp.example)는 여기서 의도적으로 배제 — isAdmin() 쓰지 말 것.
      allow update, delete: if isPersonalAdmin() && request.auth.uid != uid;
    }
```

- [ ] **Step 2: 기존 관리자 판정 확장** — `public_branches`, `settings`, `edit_logs`의 `isAdmin()` 사용처 3곳을 `(isAdmin() || isPersonalAdmin())`으로 교체. 예 (`public_branches`):

```
    match /public_branches/{branchId} {
      allow read: if true;
      allow create, update, delete: if isAdmin() || isPersonalAdmin();
    }
```

`settings`의 `create, update, delete`와 `edit_logs`의 `update, delete`도 동일 패턴으로 교체한다. **`users` match에는 절대 `isAdmin()`을 넣지 않는다.**

- [ ] **Step 3: 규칙 배포 승인 요청** — 규칙은 배포해야 효력이 생기고, 배포 전에는 Task 2 이후의 실기기 확인이 전부 permission-denied로 실패한다. 사용자에게 보고: "firestore.rules에 users 컬렉션 규칙을 추가했습니다. 이후 태스크의 실기기 확인을 위해 규칙만 먼저 배포가 필요합니다 (`firebase deploy --only firestore:rules`). 배포할까요?" — **승인 없이는 배포하지 않는다.** 승인 전이라면 이후 태스크의 "실기기 확인" 단계를 보류 목록에 적어두고 코드 작업만 진행.

---

### Task 2: `users` 프로필 API + gateAuth 검증 모듈

**Files:**
- Create: `src/api/userProfile.ts`
- Create: `src/api/gateAuth.ts`

**Interfaces:**
- Consumes: `firebase-applet-config.json`(기존), `hashPin`(`src/utils/hashPin.ts`), `gasClient.verifyPin(pinHash): Promise<BranchSetting>`(기존), `LoginBranch`(`src/api/firebaseAuth.ts`)
- Produces:
  - `interface UserProfile { uid: string; name: string; email: string; role: "admin" | "branch"; allowedTabs: string[] | "all"; allowedBranches: string[] | "all"; status: "active" | "suspended"; createdAt: string; reviewedByAdmin: boolean; }`
  - `loadOrCreateUserProfile(user: { uid, displayName, email }): Promise<UserProfile>` — 없으면 기본값 create, 실패 시 throw
  - `verifyGatePin(target: { kind: "admin" } | { kind: "branch"; branch: LoginBranch }, pin: string): Promise<string>` — 통과 시 pinHash 반환, 실패 시 throw

- [ ] **Step 1: `src/api/userProfile.ts` 작성**

```ts
// src/api/userProfile.ts
// 개인 계정 프로필(users/{uid}) 로드·생성. PIN 로그인 세션은 이 모듈을 쓰지 않는다.
import { getApps, getApp, initializeApp } from "firebase/app";
import { doc, getDoc, setDoc, getFirestore } from "firebase/firestore";
import firebaseConfig from "../../firebase-applet-config.json";

const app = getApps().length ? getApp() : initializeApp(firebaseConfig);
const db = getFirestore(app, firebaseConfig.firestoreDatabaseId);

export interface UserProfile {
  uid: string;
  name: string;
  email: string;
  role: "admin" | "branch";
  allowedTabs: string[] | "all";
  allowedBranches: string[] | "all";
  status: "active" | "suspended";
  createdAt: string;
  reviewedByAdmin: boolean;
}

// 신규 가입 기본값 — firestore.rules의 create 조건과 글자 단위로 일치해야 한다.
const NEW_PROFILE_DEFAULTS = {
  role: "branch" as const,
  allowedTabs: ["daily.settle"],
  allowedBranches: "all" as const,
  status: "active" as const,
  reviewedByAdmin: false
};

/**
 * users/{uid} 로드. 없으면 기본값으로 생성한다.
 * 생성 실패 시 throw — 호출부(useAuth)는 반드시 signOut 후 오류를 안내한다(설계서 §4: 유령 세션 금지).
 */
export async function loadOrCreateUserProfile(user: { uid: string; displayName: string | null; email: string | null }): Promise<UserProfile> {
  const ref = doc(db, "users", user.uid);
  const snapshot = await getDoc(ref);
  if (snapshot.exists()) {
    return { uid: user.uid, ...(snapshot.data() as Omit<UserProfile, "uid">) };
  }
  const profile: Omit<UserProfile, "uid"> = {
    name: user.displayName || (user.email ? user.email.split("@")[0] : "이름없음"),
    email: user.email || "",
    createdAt: new Date().toISOString(),
    ...NEW_PROFILE_DEFAULTS
  };
  await setDoc(ref, profile);
  return { uid: user.uid, ...profile };
}
```

- [ ] **Step 2: `src/api/gateAuth.ts` 작성**

```ts
// src/api/gateAuth.ts
// 로그인 후 PIN 게이트 검증 — 보조 Firebase Auth("gate" named app) 사용.
// 절대 주 Auth 인스턴스로 검증하지 말 것: loginWithBranchPin/loginWithAdminPin을 재사용하면
// 개인 계정 세션이 지점/관리자 가짜 계정으로 바뀐다(설계서 §4 gateAuth 규격).
import { getApps, initializeApp } from "firebase/app";
import { initializeAuth, inMemoryPersistence, signInWithEmailAndPassword, signOut, type Auth } from "firebase/auth";
import firebaseConfig from "../../firebase-applet-config.json";
import { hashPin } from "../utils/hashPin";
import { gasClient } from "./gasClient";
import type { LoginBranch } from "./firebaseAuth";

const ADMIN_EMAIL = "admin@ugd-erp.example";
const firebasePassword = (pin: string) => `ugd-${pin.trim()}`;

// initializeAuth는 같은 앱에 두 번 호출하면 오류 — 모듈 스코프 싱글턴으로 1회만.
let gateAuthSingleton: Auth | null = null;
function getGateAuth(): Auth {
  if (gateAuthSingleton) return gateAuthSingleton;
  const gateApp = getApps().find((a) => a.name === "gate") ?? initializeApp(firebaseConfig, "gate");
  gateAuthSingleton = initializeAuth(gateApp, { persistence: inMemoryPersistence });
  return gateAuthSingleton;
}

export type GateTarget = { kind: "admin" } | { kind: "branch"; branch: LoginBranch };

/**
 * PIN 게이트 검증. 통과 시 세션에 보관할 pinHash를 반환한다.
 * 오류 정책(설계서 §4 — 브루트포스 우회 차단):
 *  - 자격증명 오류/시도초과 → 즉시 실패, GAS 폴백 금지
 *  - 네트워크 오류만 GAS verifyPin 폴백(+지점 branchName/관리자 role 필수 비교)
 */
export async function verifyGatePin(target: GateTarget, pin: string): Promise<string> {
  const pinHash = await hashPin(pin);
  const email = target.kind === "admin" ? ADMIN_EMAIL : target.branch.loginEmail;
  const gateAuth = getGateAuth();
  try {
    await signInWithEmailAndPassword(gateAuth, email, firebasePassword(pin));
    return pinHash;
  } catch (error: any) {
    const code = String(error?.code || "");
    if (code === "auth/too-many-requests") {
      throw new Error("시도가 너무 많습니다. 잠시 후 다시 시도해 주세요.");
    }
    if (code === "auth/network-request-failed") {
      // 네트워크 장애만 GAS 폴백 — 현행 useAuth 폴백과 동일한 비교를 강제한다.
      const setting = await gasClient.verifyPin(pinHash);
      if (target.kind === "admin" && setting.role !== "admin") throw new Error("관리자 PIN이 올바르지 않습니다.");
      if (target.kind === "branch" && setting.branchName !== target.branch.branchName) throw new Error("PIN이 올바르지 않습니다.");
      return pinHash;
    }
    // wrong-password / invalid-credential / user-not-found 등 자격증명 오류: 폴백 없이 실패.
    throw new Error("PIN이 올바르지 않습니다. 다시 확인해 주세요.");
  } finally {
    // 성공/실패와 무관하게 gate 인스턴스의 인증 상태를 비운다.
    void signOut(gateAuth).catch(() => {});
  }
}
```

- [ ] **Step 3: 타입 게이트** — 실행: `npm run lint` / 기대: 오류 0
- [ ] **Step 4: 단독 동작 확인(브라우저 콘솔)** — Task 5에서 화면으로 검증하므로 여기서는 lint까지만. (규칙 미배포 상태면 `loadOrCreateUserProfile`은 permission-denied — 예상된 동작)
- [ ] **Step 5: 첫 관리자 부트스트랩(사용자와 함께)** — 사용자가 본인 구글 계정으로 최초 로그인하면 Firebase 콘솔 Authentication 사용자 목록에서 해당 uid 확인 → Firestore `users/{uid}` 문서를 콘솔에서 직접 수정: `role: "admin"`, `allowedTabs: "all"`. (규칙상 본인 create는 branch 기본값만 가능하므로, 가입 후 콘솔에서 승격하는 순서)

---

### Task 3: `useAuth` 개편 — UserSession v2 + 개인 로그인 경로

**Files:**
- Modify: `src/hooks/useAuth.ts` (전면 개편)
- Modify: `src/contexts/AuthContext.tsx` (타입 배선)

**Interfaces:**
- Consumes: Task 2의 `loadOrCreateUserProfile`, `verifyGatePin`, `UserProfile`; 기존 `loginWithBranchPin`/`loginWithAdminPin`/`logoutFirebase`(`firebaseAuth.ts`), `gasClient.verifyPin`, `hashPin`
- Produces (AuthContext로 노출 — 이후 태스크 전부가 소비):

```ts
export interface UserSession {
  loginType: "personal" | "pin";
  uid?: string;                       // personal만
  name: string;                       // personal=profile.name, pin=branchName("직원"/"관리자")
  email?: string;
  role: "admin" | "branch";
  branchName: string;
  brand: string;
  allowedTabs: string[] | "all";      // pin 로그인은 "all"
  allowedBranches: string[] | "all";  // pin 로그인은 "all"
  pinHash: string;
}
// 반환 훅 시그니처(추가분):
// loginWithGoogle(): Promise<boolean>          — 구글 팝업 → 프로필 로드 → gate 대기 상태로 전환
// loginWithEmail(email, pw): Promise<boolean>
// signUpWithEmail(name, email, pw): Promise<boolean>
// sendPasswordReset(email): Promise<void>
// pendingGate: { profile: UserProfile } | null — 개인 인증됐지만 게이트 미통과 상태
// completeGate(target: GateTarget, pin: string): Promise<boolean> — 게이트 통과 시 세션 확정
// login(branch, pin): 기존 PIN 로그인 그대로 유지
```

- [ ] **Step 1: 세션 키 승격 및 legacy 폐기** — `useAuth.ts` 상단 상수 교체:

```ts
const SESSION_KEY = "erp_ugd_session_v2";   // v2: loginType 필수. 구 세션과 절대 혼용 금지(설계서 §4)
const LEGACY_SESSION_KEY = "erp_ugd_session";
```

복구 useEffect에서: `sessionStorage.removeItem(LEGACY_SESSION_KEY);`를 먼저 실행하고, v2 세션은 `parsedSession.loginType`이 있을 때만 인정(없으면 삭제).

- [ ] **Step 2: 개인 로그인 함수 3종 + 비밀번호 재설정 추가** — 주 Auth 인스턴스에 `browserSessionPersistence` 적용:

```ts
const startPersonalLogin = useCallback(async (mode: "google" | "email-in" | "email-up",
  params?: { name?: string; email?: string; password?: string }): Promise<boolean> => {
  setLoading(true); setError(null);
  const { getAuth, setPersistence, browserSessionPersistence, signInWithPopup, GoogleAuthProvider,
    signInWithEmailAndPassword, createUserWithEmailAndPassword, updateProfile, signOut } = await import("firebase/auth");
  const auth = getAuth();
  try {
    await setPersistence(auth, browserSessionPersistence);
    if (mode === "google") {
      await signInWithPopup(auth, new GoogleAuthProvider());
    } else if (mode === "email-in") {
      await signInWithEmailAndPassword(auth, params!.email!, params!.password!);
    } else {
      const credential = await createUserWithEmailAndPassword(auth, params!.email!, params!.password!);
      await updateProfile(credential.user, { displayName: params!.name! });
      // 인증 메일은 보내되 로그인 조건으로 삼지 않는다(설계서 §4 — 스팸함 문제로 비강제)
      const { sendEmailVerification } = await import("firebase/auth");
      void sendEmailVerification(credential.user).catch(() => {});
    }
    const firebaseUser = auth.currentUser!;
    let profile;
    try {
      const { loadOrCreateUserProfile } = await import("../api/userProfile");
      profile = await loadOrCreateUserProfile(firebaseUser);
    } catch (profileError) {
      await signOut(auth);   // 유령 세션 금지: 프로필 없으면 앱 진입 불가(설계서 §4)
      throw new Error("가입 처리에 실패했습니다. 다시 로그인해 주세요.");
    }
    if (profile.status === "suspended") {
      await signOut(auth);
      throw new Error("정지된 계정입니다. 관리자에게 문의해 주세요.");
    }
    setPendingGate({ profile });   // 게이트 화면으로 — 아직 user 세션은 만들지 않는다
    return true;
  } catch (err: any) {
    setError(translateAuthError(err));
    return false;
  } finally { setLoading(false); }
}, []);
```

`translateAuthError`는 같은 파일의 작은 헬퍼: `auth/popup-closed-by-user`→"로그인 창이 닫혔습니다", `auth/email-already-in-use`→"이미 가입된 이메일입니다", `auth/weak-password`→"비밀번호는 6자 이상이어야 합니다", `auth/invalid-credential`·`auth/wrong-password`→"이메일 또는 비밀번호가 올바르지 않습니다", 그 외→`err.message`.
`sendPasswordReset(email)`은 `sendPasswordResetEmail(getAuth(), email)` 단순 위임.

- [ ] **Step 3: `completeGate` 구현** — 게이트 통과 시에만 세션 확정:

```ts
const completeGate = useCallback(async (target: GateTarget, pin: string): Promise<boolean> => {
  if (!pendingGate) return false;
  setLoading(true); setError(null);
  try {
    const { verifyGatePin } = await import("../api/gateAuth");
    const pinHash = await verifyGatePin(target, pin);
    const profile = pendingGate.profile;
    const branchSetting = target.kind === "branch"
      ? { branchName: target.branch.branchName, brand: target.branch.brand || target.branch.branchName, role: "branch" as const }
      : { branchName: "관리자", brand: "본사", role: "admin" as const };
    const session: UserSession = {
      loginType: "personal", uid: profile.uid, name: profile.name, email: profile.email,
      role: profile.role, branchName: branchSetting.branchName, brand: branchSetting.brand,
      allowedTabs: profile.role === "admin" ? "all" : profile.allowedTabs,
      allowedBranches: profile.allowedBranches, pinHash
    };
    sessionStorage.setItem(SESSION_KEY, JSON.stringify(session));
    if (target.kind === "branch") {
      sessionStorage.setItem(SELECTED_BRANCH_KEY, JSON.stringify(branchSetting));
      setSelectedBranchState(branchSetting);
    }
    localStorage.setItem(ATTEMPTS_KEY, "0"); setFailedAttempts(0);
    setPendingGate(null); setUser(session);
    return true;
  } catch (err: any) {
    const next = failedAttempts + 1;
    setFailedAttempts(next); localStorage.setItem(ATTEMPTS_KEY, String(next));
    setError(err.message || "PIN이 올바르지 않습니다.");
    return false;
  } finally { setLoading(false); }
}, [pendingGate, failedAttempts]);
```

- [ ] **Step 4: 기존 PIN `login`은 세션 객체만 v2 스키마로 채워 유지** — `login()` 내부의 `session` 생성부를 다음으로 교체(검증 로직·폴백은 그대로):

```ts
const session: UserSession = {
  loginType: "pin",
  name: branchSetting.branchName || "직원",
  role: (branchSetting.role as "admin" | "branch") || "branch",
  branchName: branchSetting.branchName || "직원",
  brand: branchSetting.brand || "",
  allowedTabs: "all", allowedBranches: "all",
  pinHash
};
```

- [ ] **Step 5: 새로고침 복구(개인)** — 복구 useEffect에서 v2 세션의 `loginType === "personal"`이면 `onAuthStateChanged`를 1회 구독해 Firebase Auth 사용자가 없으면(브라우저 재시작 등) 세션을 버리고 로그아웃 상태로 둔다. Auth가 살아 있으면 `users/{uid}`를 재로드해 `status === "suspended"`면 signOut+세션 삭제(다음 진입 차단, 설계서 §9). 프로필 재로드 실패(오프라인)는 세션 유지(이미 게이트 통과한 세션이므로).
- [ ] **Step 6: `logout`** — 기존 코드에 v2 키 삭제 추가(`SESSION_KEY`가 이미 v2), `pendingGate` null 초기화. `logoutFirebase()` 호출 유지(주 인스턴스 signOut — 30분 유휴 로그아웃 경로도 이걸 타므로 별도 작업 불필요).
- [ ] **Step 7: `AuthContext.tsx`에 신규 멤버 노출** — `AuthContextType`에 `loginWithGoogle`, `loginWithEmail`, `signUpWithEmail`, `sendPasswordReset`, `pendingGate`, `completeGate` 추가(구현은 useAuth 반환값 그대로 전달).
- [ ] **Step 8: 게이트** — 실행: `npm run lint` / 기대: 오류 0. `user.pinHash` 소비처(AdminPage:149, KakaoTaxiSection:44, BusinessTaxiTab:38)가 그대로 컴파일되는지 확인(`pinHash`는 v2에서도 필수 string).

---

### Task 4: LoginPage 개편 — 개인 로그인 UI + PIN 병행

**Files:**
- Modify: `src/pages/LoginPage.tsx` (전면 개편)

**Interfaces:**
- Consumes: Task 3의 `loginWithGoogle`, `loginWithEmail`, `signUpWithEmail`, `sendPasswordReset`, `pendingGate`, 기존 `login`(PIN)
- Produces: 첫 화면 = [구글로 시작하기] + 이메일 폼(로그인/가입 전환) + 하단 접이식 [기존 PIN으로 로그인]. `pendingGate`가 생기면 Task 5의 게이트 화면으로 넘어간다(같은 페이지 내 단계 전환).

- [ ] **Step 1: 화면 상태 설계** — `mode: "personal" | "pin"` (기본 "personal"), `emailForm: "signin" | "signup" | "reset"`. 기존 PIN UI(현행 JSX 전체)는 `mode === "pin"`일 때 그대로 렌더 — **삭제 금지**(전환 기간 병행, 설계서 §11). 참고: 현행 파일의 `hidden` 처리된 중복 PIN input 블록(LoginPage.tsx:128-145)은 죽은 코드이므로 이번에 제거.
- [ ] **Step 2: 개인 로그인 JSX** — UGD 타이틀 아래:

```tsx
{mode === "personal" && !pendingGate && (
  <div className="space-y-4">
    <button type="button" onClick={() => void loginWithGoogle()} disabled={loading}
      className="flex w-full items-center justify-center gap-2 rounded-xl border border-black px-4 py-4 text-sm font-bold hover:bg-zinc-50">
      구글로 시작하기
    </button>
    <div className="text-center text-xs font-bold text-zinc-400">또는 이메일로</div>
    <form className="space-y-3" onSubmit={handleEmailSubmit}>
      {emailForm === "signup" && (
        <input value={name} onChange={(e) => setName(e.target.value)} required placeholder="이름"
          className="w-full rounded-xl border border-black px-4 py-3 text-sm font-bold" />
      )}
      <input type="email" value={email} onChange={(e) => setEmail(e.target.value)} required placeholder="이메일"
        className="w-full rounded-xl border border-black px-4 py-3 text-sm font-bold" />
      {emailForm !== "reset" && (
        <input type="password" value={password} onChange={(e) => setPassword(e.target.value)} required placeholder="비밀번호"
          autoComplete={emailForm === "signup" ? "new-password" : "current-password"}
          className="w-full rounded-xl border border-black px-4 py-3 text-sm font-bold" />
      )}
      <button type="submit" disabled={loading}
        className="w-full rounded-xl bg-black px-4 py-4 text-sm font-bold text-white hover:bg-zinc-800 disabled:opacity-50">
        {emailForm === "signin" ? "로그인" : emailForm === "signup" ? "가입하기" : "재설정 메일 보내기"}
      </button>
    </form>
    <div className="flex justify-between text-xs font-bold text-zinc-500">
      <button type="button" className="underline underline-offset-4"
        onClick={() => setEmailForm(emailForm === "signup" ? "signin" : "signup")}>
        {emailForm === "signup" ? "로그인으로 돌아가기" : "이메일로 가입하기"}
      </button>
      <button type="button" className="underline underline-offset-4" onClick={() => setEmailForm("reset")}>비밀번호 찾기</button>
    </div>
    <button type="button" onClick={() => setMode("pin")} className="w-full text-xs font-bold text-zinc-400 underline underline-offset-4">
      기존 PIN으로 로그인
    </button>
  </div>
)}
```

`handleEmailSubmit`: `emailForm`에 따라 `loginWithEmail`/`signUpWithEmail`/`sendPasswordReset` 호출(재설정은 성공 시 "메일을 보냈습니다. 스팸함도 확인해 주세요." 안내 후 signin 폼 복귀 — 네이버/다음 스팸함 문제 고지, 설계서 §4).
- [ ] **Step 3: 자동 이동 유지** — 기존 `useEffect(user → navigate)` 그대로. `pendingGate`가 있으면 Task 5의 `<GateStep />` 렌더.
- [ ] **Step 4: 게이트** — `npm run lint` 통과. `npm run dev` → localhost:3000에서 (a) 첫 화면에 구글/이메일/PIN 접이식 3요소 노출 (b) "기존 PIN으로 로그인" 클릭 시 현행 PIN 플로우 그대로 (c) 관리자 PIN 로그인 회귀 확인.

---

### Task 5: 게이트 단계 화면 — 지점 선택 → PIN 입력

**Files:**
- Create: `src/pages/login/GateStep.tsx`
- Modify: `src/pages/LoginPage.tsx` (GateStep 렌더 분기 — Task 4 Step 3 자리)

**Interfaces:**
- Consumes: Task 3의 `pendingGate`, `completeGate`, `failedAttempts`; `getFirebaseLoginBranches()`(기존 — `LoginBranch[]` 반환, `public_branches` 조회)
- Produces: `<GateStep profile={pendingGate.profile} />` — role=branch면 허용 지점 선택(1개면 자동 선택) 후 지점 공통 PIN 입력, role=admin이면 관리자 PIN만. 통과 시 `completeGate`가 세션을 확정해 기존 라우팅(`/admin`·`/branch-confirm`)이 그대로 동작.

- [ ] **Step 1: `GateStep.tsx` 작성**

```tsx
// src/pages/login/GateStep.tsx
// 개인 로그인 성공 후 회사 게이트: 지점 선택(지점 role) → PIN 검증.
// 검증은 gateAuth(보조 인스턴스)로만 — 주 세션을 건드리지 않는다.
import React, { useEffect, useMemo, useState } from "react";
import { Lock } from "lucide-react";
import { useAuthContext } from "../../contexts/AuthContext";
import LoadingSpinner from "../../components/LoadingSpinner";
import type { LoginBranch } from "../../api/firebaseAuth";
import type { UserProfile } from "../../api/userProfile";

export default function GateStep({ profile }: { profile: UserProfile }) {
  const { completeGate, loading, error, failedAttempts, logout } = useAuthContext();
  const [pin, setPin] = useState("");
  const [branches, setBranches] = useState<LoginBranch[]>([]);
  const [selected, setSelected] = useState<LoginBranch | null>(null);
  const [loadingBranches, setLoadingBranches] = useState(false);
  const isAdmin = profile.role === "admin";

  useEffect(() => {
    if (isAdmin) return;
    setLoadingBranches(true);
    import("../../api/firebaseAuth")
      .then(({ getFirebaseLoginBranches }) => getFirebaseLoginBranches())
      .then((list) => {
        const allowed = profile.allowedBranches === "all"
          ? list
          : list.filter((b) => (profile.allowedBranches as string[]).includes(b.branchName));
        setBranches(allowed);
        if (allowed.length === 1) setSelected(allowed[0]);   // 허용 1개면 자동 선택(설계서 §4)
      })
      .catch(() => setBranches([]))
      .finally(() => setLoadingBranches(false));
  }, [isAdmin, profile.allowedBranches]);

  const canSubmit = useMemo(() => !!pin && (isAdmin || !!selected) && !loading, [pin, isAdmin, selected, loading]);

  const handleSubmit = async (event: React.FormEvent) => {
    event.preventDefault();
    if (!canSubmit) return;
    const ok = await completeGate(isAdmin ? { kind: "admin" } : { kind: "branch", branch: selected! }, pin);
    if (ok) setPin("");
  };

  return (
    <form className="space-y-4" onSubmit={handleSubmit}>
      <p className="text-center text-sm font-bold text-zinc-700">
        {profile.name}님, {isAdmin ? "관리자 PIN을 입력해 주세요." : "지점을 선택하고 PIN을 입력해 주세요."}
      </p>
      {!isAdmin && (loadingBranches ? (
        <div className="flex justify-center py-5"><LoadingSpinner size="sm" /></div>
      ) : (
        <select value={selected?.branchName || ""} required
          onChange={(e) => setSelected(branches.find((b) => b.branchName === e.target.value) || null)}
          className="w-full rounded-xl border border-black bg-white px-4 py-4 text-center text-sm font-bold outline-hidden">
          <option value="">지점을 선택하세요</option>
          {branches.map((b) => <option key={b.branchName} value={b.branchName}>{b.branchName}</option>)}
        </select>
      ))}
      <div className="relative">
        <input type="password" inputMode="text" autoComplete="off" required value={pin}
          onChange={(e) => { const v = e.target.value; if (/^[a-zA-Z0-9]*$/.test(v) && v.length <= 12) setPin(v); }}
          placeholder="PIN" aria-label="PIN" disabled={loading}
          className="w-full rounded-xl border border-black px-4 py-4 pl-11 text-center font-mono text-xl font-bold tracking-widest outline-hidden focus:ring-1 focus:ring-black disabled:bg-zinc-100" />
        <Lock className="absolute left-4 top-1/2 h-5 w-5 -translate-y-1/2 text-zinc-500" />
      </div>
      {failedAttempts > 0 && <p className="text-center text-xs font-bold text-rose-600">실패 횟수: {failedAttempts}</p>}
      <button type="submit" disabled={!canSubmit}
        className="w-full rounded-xl bg-black px-4 py-4 text-sm font-bold text-white hover:bg-zinc-800 disabled:opacity-50">
        {loading ? <LoadingSpinner size="sm" light /> : "입장하기"}
      </button>
      <button type="button" onClick={logout} className="w-full text-xs font-bold text-zinc-400 underline underline-offset-4">
        다른 계정으로 로그인
      </button>
    </form>
  );
}
```

(오류 표시는 LoginPage의 기존 error 배너를 재사용 — GateStep에서 별도 렌더하지 않음.)
- [ ] **Step 2: LoginPage에 분기 배선** — `{pendingGate && <GateStep profile={pendingGate.profile} />}`, `pendingGate`가 있으면 개인/PIN 폼은 렌더하지 않는다.
- [ ] **Step 3: 게이트** — `npm run lint` 통과. 실기기 확인(규칙 배포 후): (a) 신규 구글 가입 → 게이트 화면 → 올바른 지점 PIN → 지점 진입 (b) 틀린 PIN → 오류+failedAttempts 증가, GAS 폴백 호출 없음(Network 탭에서 GAS 요청 부재 확인) (c) 관리자 프로필(부트스트랩 계정) → 관리자 PIN → /admin 진입 (d) **지점 계정에 관리자 PIN 입력 → 거부** (e) 게이트 통과 후 DevTools Application 탭에서 gate 인스턴스의 IndexedDB/localStorage 인증 흔적 없음 (f) 주 세션(개인 계정)이 유지됨.

---

### Task 6: 탭 권한 — 레지스트리 + 내비게이션 가드

**Files:**
- Create: `src/pages/branch/tabRegistry.ts`
- Modify: `src/pages/BranchConfirmPage.tsx` (지점 목록 필터 :75-107, ActiveWorkspace props :189-205, 사이드바 :540-620, 초기 라우팅)

**Interfaces:**
- Consumes: Task 3의 `UserSession.allowedTabs`/`allowedBranches`
- Produces:

```ts
// tabRegistry.ts
export type PermKey = string; // "dashboard" | "daily.settle" | ... | "annualLeave"
export interface TabNavTarget { mainCategory: string; activeTab?: string; monthlyTab?: string; }
export const BRANCH_TAB_REGISTRY: Record<PermKey, TabNavTarget>;
export function isTabAllowed(allowedTabs: string[] | "all", key: PermKey): boolean;
export function firstAllowedKey(allowedTabs: string[] | "all"): PermKey; // "all"이면 "dashboard"
```

- [ ] **Step 1: `tabRegistry.ts` 작성** — 설계서 §5 표의 17개 키 전수:

```ts
// src/pages/branch/tabRegistry.ts
// 지점 화면 탭 권한의 단일 출처(설계서 §5). 키 = "대분류.서브탭", 서브탭은 현행 state 값 그대로.
// 새 탭을 만들면 여기에 등록해야 화면에 나타난다.
export type PermKey =
  | "dashboard"
  | "daily.settle" | "daily.orders" | "daily.liquorInventory" | "daily.officeWorkLog"
  | "daily.overtimeLog" | "daily.partTimeLog" | "daily.roster"
  | "monthly.fullTimeSalary" | "monthly.purchaseSales" | "monthly.partTimeSalary"
  | "monthly.cashManagement" | "monthly.cashExpenses" | "monthly.cardExpenses"
  | "laborContract" | "businessTaxi" | "annualLeave";

export interface TabNavTarget { mainCategory: string; activeTab?: string; monthlyTab?: string; }

export const BRANCH_TAB_REGISTRY: Record<PermKey, TabNavTarget> = {
  "dashboard": { mainCategory: "dashboard" },
  "daily.settle": { mainCategory: "daily", activeTab: "settle" },
  "daily.orders": { mainCategory: "daily", activeTab: "orders" },
  "daily.liquorInventory": { mainCategory: "daily", activeTab: "liquorInventory" },
  "daily.officeWorkLog": { mainCategory: "daily", activeTab: "officeWorkLog" },
  "daily.overtimeLog": { mainCategory: "daily", activeTab: "overtimeLog" },
  "daily.partTimeLog": { mainCategory: "daily", activeTab: "partTimeLog" },
  "daily.roster": { mainCategory: "daily", activeTab: "roster" },
  "monthly.fullTimeSalary": { mainCategory: "monthly", monthlyTab: "fullTimeSalary" },
  "monthly.purchaseSales": { mainCategory: "monthly", monthlyTab: "purchaseSales" },
  "monthly.partTimeSalary": { mainCategory: "monthly", monthlyTab: "partTimeSalary" },
  "monthly.cashManagement": { mainCategory: "monthly", monthlyTab: "cashManagement" },
  "monthly.cashExpenses": { mainCategory: "monthly", monthlyTab: "cashExpenses" },
  "monthly.cardExpenses": { mainCategory: "monthly", monthlyTab: "cardExpenses" },
  "laborContract": { mainCategory: "laborContract" },
  "businessTaxi": { mainCategory: "businessTaxi" },
  "annualLeave": { mainCategory: "annualLeave", activeTab: "annualLeave" }
};

export function isTabAllowed(allowedTabs: string[] | "all", key: PermKey): boolean {
  return allowedTabs === "all" || allowedTabs.includes(key);
}

// 허용 목록의 첫 화면 — 기본값 계정(daily.settle만)이 대시보드로 떨어지는 모순 방지(설계서 §5)
export function firstAllowedKey(allowedTabs: string[] | "all"): PermKey {
  if (allowedTabs === "all") return "dashboard";
  const keys = Object.keys(BRANCH_TAB_REGISTRY) as PermKey[];
  return keys.find((k) => allowedTabs.includes(k)) || "daily.settle";
}
```

- [ ] **Step 2: 지점 목록 필터** — `BranchConfirmPage.tsx` fetchBranches(:86-97)에서 GAS/폴백 목록 확정 직후:

```ts
if (user.allowedBranches !== "all") {
  filtered = filtered.filter((b: any) => (user.allowedBranches as string[]).includes(b.branchName));
}
```

(개인 로그인은 GateStep에서 이미 지점이 확정돼 이 화면을 거의 안 거치지만, PIN 세션과 selectBranch 재선택 경로를 위해 필터는 유지.)
- [ ] **Step 3: ActiveWorkspace 배선** — `WorkspaceProps`에 `allowedTabs: string[] | "all"` 추가, 호출부(:189)에서 `allowedTabs={user.allowedTabs}` 전달.
- [ ] **Step 4: `navigateTo` 가드 단일화** — ActiveWorkspace 내부에 추가하고 사이드바 onClick 전부 교체:

```ts
const navigateTo = (key: PermKey) => {
  if (!isTabAllowed(allowedTabs, key)) return;   // 허용 밖 전환은 무시(사이드바에서 이미 숨겨짐)
  const target = BRANCH_TAB_REGISTRY[key];
  // 주류재고 이탈 확인(기존 openDailySubTab 로직 흡수)
  if (activeTab === "liquorInventory" && target.activeTab !== "liquorInventory" && (window as any).__ugdLiquorInventoryDirty) {
    if (!window.confirm("저장하지 않은 주류 재고 입력값이 있습니다. 저장하지 않고 이동할까요?")) return;
    (window as any).__ugdLiquorInventoryDirty = false;
  }
  setMainCategory(target.mainCategory as any);
  if (target.activeTab) setActiveTab(target.activeTab as BranchDailyTab);
  if (target.monthlyTab) setMonthlyTab(target.monthlyTab as any);
};
```

교체 대상: 대시보드 버튼→`navigateTo("dashboard")`, `openDailySubTab(tab.id)`→`navigateTo(("daily." + tab.id) as PermKey)`, 월말 버튼→`navigateTo(("monthly." + tab.id) as PermKey)`, 근로계약서(:596)→`navigateTo("laborContract")`, 비즈니스택시(:605)→`navigateTo("businessTaxi")`, 연차관리(:613)→`navigateTo("annualLeave")`. 기존 `openDailySubTab` 함수는 삭제(다른 참조가 있으면 `navigateTo` 위임으로 교체).
- [ ] **Step 5: 사이드바 필터** — `dailySubTabs`/`monthlySubTabs` 정의 뒤에 `.filter((t) => isTabAllowed(allowedTabs, ("daily."+t.id) as PermKey))`(월말은 "monthly." 접두사) 적용, 대시보드·근로계약서·비즈니스택시·연차관리 버튼은 `isTabAllowed(...)`일 때만 렌더.
- [ ] **Step 6: 초기 라우팅** — ActiveWorkspace 마운트 시(useEffect, 1회) `navigateTo(firstAllowedKey(allowedTabs))` 호출 — `daily.settle`만 허용된 계정이 대시보드가 아닌 일일마감정산에서 시작.
- [ ] **Step 7: 게이트** — `npm run lint` 통과. dev 확인: (a) PIN 로그인(allowedTabs "all") → 사이드바·이동 현행과 완전 동일(회귀 없음) (b) 신규 개인 계정 → 사이드바에 일일마감정산만, 첫 화면 일일마감정산, 근로계약서/비즈니스택시/연차관리/월말 버튼 부재.

---

### Task 7: 작성자 자동 기록 — uid 병행 저장 + 입력칸 잠금

**Files:**
- Modify: `src/api/gasClient.ts` (:36-50 MasterDaily 타입, :318-323 updateDaily 시그니처)
- Modify: `src/api/firebaseDirect.ts` (:127-150 firebaseSubmitDaily, :218-263 firebaseUpdateDaily)
- Modify: `src/pages/branch/helpers/dailyOps.ts` (actor 필수화)
- Modify: `src/pages/branch/tabs/DailySettleTab.tsx` (:534 부근 writer state, :1324 submittedBy, 확정 버튼 옆 작성자 노출)

**Interfaces:**
- Consumes: Task 3의 `UserSession`(`loginType`, `uid`, `name`)
- Produces: `MasterDaily.submittedByUid?: string`, `MasterDaily.modifiedByUid?: string`; `updateDaily(..., modifiedBy?, modifiedByUid?)`; `updateDailyMetadata(recordId, updater, actor: { name: string; uid?: string })` — **actor 기본값 제거, 필수 인자**

- [ ] **Step 1: 타입 확장** — `gasClient.ts` MasterDaily에 `submittedByUid?: string;`, `modifiedByUid?: string;` 추가. `updateDaily` 시그니처에 `modifiedByUid?: string` 파라미터 추가해 `firebaseUpdateDaily`로 전달.
- [ ] **Step 2: 저장 경로 uid 기입** — `firebaseDirect.ts`:
  - `firebaseSubmitDaily`(:140-147): `savedMaster`에 `submittedByUid: master.submittedByUid || ""`, 수정 시 `modifiedByUid: existing.exists() ? master.submittedByUid || "" : ""` 추가. **기존 `modifiedBy: ... || "branch"` fallback 문자열은 삭제** — `master.submittedBy`를 그대로 쓰고 비면 빈 문자열(거짓 작성자 금지, 설계서 §7).
  - `firebaseUpdateDaily`(:232, :258): `modifiedBy: modifiedBy || "관리자"` → `modifiedBy: modifiedBy || ""`로 교체, `modifiedByUid` 병행 기입. edit_logs 기록부(:252-263)에도 `modifiedByUid` 추가.
- [ ] **Step 3: edit_logs 실패 표면화** — `firebaseUpdateDaily`의 edit_logs 기록 실패 catch(현행 console.warn)에서 호출부로 실패 여부를 반환(`{ editLogFailed: true }`)하거나 전역 토스트 콜백 호출. 구현 단순화: 반환 객체에 플래그를 추가하고, 호출부(DailySettleTab 등 저장 핸들러)에서 `editLogFailed`면 기존 toast 유틸로 "수정이력 기록에 실패했습니다. 저장은 완료됐습니다." 표시. 본문 저장은 막지 않는다(설계서 §7).
- [ ] **Step 4: `dailyOps.ts` actor 필수화** —

```ts
export const updateDailyMetadata = async (
  recordId: string,
  updater: (metadata: any, detail: DailySettleDetail) => { metadata: any; staff?: any[]; expenses?: any[]; masterPatch?: any } | void,
  actor: { name: string; uid?: string }   // 기본값 제거 — 안 넘기면 컴파일 오류(거짓 작성자 방지)
) => { ... gasClient.updateDaily(recordId, masterPatch, ..., actor.name, actor.uid); };
```

호출부 전수 수정: `Grep "updateDailyMetadata(" src/` 로 찾은 모든 호출에 actor 전달 — 지점 화면은 `{ name: user.loginType === "personal" ? user.name : branchName, uid: user.uid }`, 관리자 화면은 `{ name: user.loginType === "personal" ? user.name : "관리자", uid: user.uid }`.
- [ ] **Step 5: DailySettleTab 작성자 잠금** — `writer` state 초기화(:534 부근)와 신규 작성 시: `user.loginType === "personal"`이면 `writer = user.name`으로 강제하고 입력칸 `disabled` + 저장 payload(:1324)에 `submittedBy: writerName, submittedByUid: user.uid || ""`. PIN 세션은 현행 수기 입력 유지. 확정 버튼 근처에 고정 노출(공용기기 오염 방지, 설계서 §7):

```tsx
<span className="text-sm font-bold text-zinc-700">작성자: {user.loginType === "personal" ? user.name : writerName || "-"}</span>
```

- [ ] **Step 6: 게이트** — `npm run lint` 통과. dev 확인: (a) 개인 계정으로 일일마감 저장 → Firestore 문서에 `submittedBy`=계정 이름, `submittedByUid`=uid (b) PIN 세션 저장 → 현행과 동일 동작(수기 작성자, uid 빈 값) (c) 확정 후 수정 → `modifiedBy`/`modifiedByUid` 기록, edit_logs에도 동일.

---

### Task 8: 관리자 — 계정 관리 탭 + 신규 가입 알림

**Files:**
- Create: `src/pages/admin/AccountManagementSection.tsx`
- Modify: `src/pages/AdminPage.tsx` (:91 adminSection 유니언에 `"accounts"` 추가, 사이드바 인사 그룹에 버튼, 본문 렌더 분기, 대시보드 알림 :517-545)
- Modify: `src/api/userProfile.ts` (관리자용 목록/수정 함수 추가)

**Interfaces:**
- Consumes: Task 2의 `UserProfile`; Task 6의 `BRANCH_TAB_REGISTRY`(탭 체크박스 라벨), `getFirebaseLoginBranches`(지점 다중선택 목록)
- Produces: `listUserProfiles(): Promise<UserProfile[]>`, `updateUserProfile(uid: string, patch: Partial<Omit<UserProfile, "uid" | "createdAt" | "email">>): Promise<void>` (userProfile.ts에 추가)

- [ ] **Step 1: userProfile.ts에 관리자 함수 추가**

```ts
export async function listUserProfiles(): Promise<UserProfile[]> {
  const { collection, getDocs } = await import("firebase/firestore");
  const snapshot = await getDocs(collection(db, "users"));
  return snapshot.docs
    .map((d) => ({ uid: d.id, ...(d.data() as Omit<UserProfile, "uid">) }))
    .sort((a, b) => (b.createdAt || "").localeCompare(a.createdAt || ""));
}

export async function updateUserProfile(uid: string, patch: Partial<Omit<UserProfile, "uid" | "createdAt" | "email">>): Promise<void> {
  const { updateDoc } = await import("firebase/firestore");
  await updateDoc(doc(db, "users", uid), patch);   // 규칙: 개인 관리자만, 본인 문서 불가
}
```

- [ ] **Step 2: `AccountManagementSection.tsx` 작성** — 구성: 상단 신규(`!reviewedByAdmin`) 배지 카운트, 계정 테이블(이름/이메일/역할/허용 탭 요약(n개 또는 "전체")/허용 지점 요약/상태/가입일), 행 클릭 → 편집 패널: 역할 select(admin/branch), 상태 select(active/suspended), 탭 체크박스 17개(+"전체 허용" 토글 → `"all"`), 지점 다중 체크(+"전체" 토글), [저장]=`updateUserProfile` + "확인 처리"=`{ reviewedByAdmin: true }`. **본인 uid 행은 편집 버튼 비활성** + "본인 계정은 다른 관리자 또는 콘솔에서 수정" 안내(규칙과 일치). 스타일은 SalaryChangeHistoryTab 등 기존 관리자 탭 준용(관리자 화면 — `--branch-*` 토큰 금지, bg-rose 계열 상태색 주의: 메모리 `erp_salary_change_history_tab` 참고).
- [ ] **Step 3: AdminPage 배선** — :91 유니언에 `"accounts"` 추가, 사이드바 인사 그룹에 `<button onClick={() => setAdminSection("accounts")}>계정 관리</button>`(기존 버튼 마크업 패턴 복제), 본문에 `{adminSection === "accounts" && <AccountManagementSection currentUid={user.loginType === "personal" ? user.uid : undefined} />}`.
- [ ] **Step 4: 신규 가입 알림** — AdminPage 대시보드 알림 로드(:517-545)에 `listUserProfiles()` 병렬 추가(개인 관리자 세션에서만 — PIN 관리자는 users 읽기 권한 없음: `user.loginType === "personal"` 조건), `newSignups = profiles.filter(p => !p.reviewedByAdmin).length`, 기존 "정산 변경 N건" 버튼 옆에 같은 마크업으로 "신규 가입 N건" 버튼 → `setAdminSection("accounts")`. PIN 관리자 세션이면 이 알림 자체를 스킵(권한 없음 오류 방지).
- [ ] **Step 5: 게이트** — `npm run lint` 통과. dev 확인(개인 관리자 계정): (a) 계정 목록 표시·신규 배지 (b) 다른 계정의 탭/지점/역할/상태 수정 → 대상 계정 재로그인 시 반영 (c) 본인 행 편집 비활성 (d) 정지 계정 로그인 → 차단 문구 (e) PIN 관리자 로그인 → 계정 관리 진입 시 "개인 관리자 계정 전용" 안내(users 읽기 거부 처리).

---

### Task 9: admin 이메일 가드 교체 + 평문 PIN 정리

**Files:**
- Modify: `src/api/firebaseDirect.ts` (:716-718 이메일 가드)
- Modify: `ERP_ACCESS.md` (평문 PIN 삭제)

**Interfaces:**
- Consumes: Task 3의 `UserSession.role`/`loginType`
- Produces: `firebaseDirect`의 지점목록 갱신 가드가 `isAdminSession` 인자를 받도록 시그니처 변경(설계서 §6 — API 레이어는 세션을 모르므로 호출부가 명시 전달)

- [ ] **Step 1: 가드 교체** — :716-718의 해당 함수(지점 목록 갱신)에 `isAdminSession: boolean` 파라미터 추가:

```ts
if (currentUser?.email !== "admin@ugd-erp.example" && !isAdminSession) return; // 전환기: 이메일 || 개인 admin
```

`Grep '"admin@ugd-erp.example"' src/` 로 클라이언트 전수 확인 — firebaseDirect 외 추가 발견 시 동일 패턴(호출부 인자 전달)으로 교체하고 목록을 완료 보고에 기재. 호출부는 `user.role === "admin"`을 전달.
- [ ] **Step 2: `ERP_ACCESS.md` 정리** — Branch PIN/Admin PIN 두 줄 삭제, "PIN은 관리자 화면에서 관리(코드·문서에 평문 저장 금지)" 한 줄로 대체. git 이력에 남는 점은 완료 보고에 명시하고 **PIN 값 자체 교체를 사용자에게 권고**(설계서 §12).
- [ ] **Step 3: 게이트** — `npm run lint` 통과.

---

### Task 10: 통합 검증 + Codex 리뷰 + 완료 보고

**Files:** (수정 없음 — 검증 전용)

- [ ] **Step 1: 전체 빌드** — 실행: `npm run lint` && `npm run build` / 기대: 오류 0
- [ ] **Step 2: 회귀 시나리오(dev, localhost:3000)** — 설계서 §15 전체:
  1. PIN 지점 로그인 → 지점선택 → 전 탭 접근·일일마감 저장(수기 작성자) — 현행과 동일
  2. PIN 관리자 로그인 → /admin 전 기능 — 현행과 동일
  3. 신규 구글 가입 → 게이트(지점 PIN) → 일일마감만 → 저장 시 작성자 자동
  4. 이메일 가입(이름 입력) → 동일 + 비밀번호 재설정 메일
  5. 게이트 오류 정책: 틀린 PIN에 GAS 폴백 없음(Network 탭), 역할 교차 거부
  6. 개인 관리자: 게이트(관리자 PIN) → getDailyList·카카오 택시 화면이 세션 pinHash로 동작
  7. 지점 개인 계정: 비즈니스택시 탭(권한 부여 후) 동작 — pinHash 호환 확인
  8. 계정 관리: 권한 편집 반영, 정지 차단, 신규 배지
  9. legacy 세션: 구 키 브라우저 → 재로그인 유도
  10. 30분 유휴 로그아웃이 개인 세션에서 Firebase signOut까지 타는지(`useIdleLogout` → `logout` → `logoutFirebase`)
- [ ] **Step 3: Codex 지독한 리뷰** — 수정 파일 전체 대상(상시 지침 P0). 지적 반영 후 재리뷰 CLEAN까지.
- [ ] **Step 4: 완료 보고 후 정지** — "수정 완료, 로컬 확인 방법 안내. 배포할까요?"까지만 말하고 **멈춘다**(커밋·푸시 금지). 배포 시 안내 사항: firestore.rules 배포(`--only firestore:rules`) 필요, 마감시간대 배포 금지, `clean:false` 유지.
