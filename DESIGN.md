# UGD ERP 디자인 기준

> **[강제사항] 이 문서는 참고서가 아니라 지킬 규칙이다.**
> 지점 화면을 만들거나 고칠 때는 **작업 전에 이 문서를 먼저 읽고, 여기 적힌 토큰·수치·규칙을 예외 없이 그대로 적용**한다.
> 여기 적힌 값과 다르게 만들면 그건 버그다 — "더 나아 보여서" 바꾸지 않는다. 값을 바꿔야 할 이유가 생기면 **먼저 이 문서를 고쳐 합의한 뒤** 코드에 반영한다.
> 코드의 실제 렌더 값과 이 문서가 어긋나면, 둘 중 무엇이 맞는지 확인해 **문서와 코드를 같은 값으로 맞춘다**(죽은 CSS 규칙에 속지 말 것 — 뒤 블록이 이긴다).

이 문서는 UGD ERP **지점 화면**의 디자인 규칙을 값 단위로 못 박은 기준서입니다.
직접 눈으로 뜯어 분석한 세 탭 — **일일마감정산 · 발주관리 · 주류재고** — 이 이 규칙들의 기준 예시입니다.

> **관리자 화면을 만들거나 고친다면 이 문서를 읽은 뒤 [`DESIGN_ADMIN.md`](./DESIGN_ADMIN.md)도 반드시 함께 읽는다.**
> 관리자는 이 문서의 규칙을 그대로 상속하되 **색 값만 연하게 치환**하며, 그 치환 때문에
> **여기 적힌 색 지침을 곧이곧대로 따르면 오히려 규칙을 어기게 되는 경우**가 있다
> (예: §11의 실패색 `bg-rose-50`이 관리자에서는 "완료·긍정" 허니색으로 바뀐다 — `DESIGN_ADMIN.md` §2-1).

읽는 순서: 먼저 2번(렌더링 원리)을 이해해야 나머지 수치가 왜 그런지 이해됩니다.

---

## 1. 디자인 방향

지점 운영자가 매일 반복해서 쓰는 업무 콘솔입니다. 마케팅 페이지가 아니라, 좁은 시간 안에 입력하고 확인하는 화면입니다.
인상은 **부드러운 회보라 배경 + 선명한 검정 섹션 테두리(1px) + 바닐라색 알약 제목 + 작고 밀도 있는 표 + 의미가 있을 때만 쓰는 상태색**으로 정리합니다.

---

## 2. 렌더링 원리 — 이걸 모르면 색이 안 먹는다

지점 화면의 최종 외형은 **두 겹**으로 결정됩니다.

1. **탭 컴포넌트(.tsx)** — 인라인 Tailwind 클래스로 **구조와 초안 색**을 만든다. 예: `bg-white p-4 rounded-2xl border border-gray-100 shadow-sm`.
2. **`src/index.css`의 `.branch-redesign` 스코프** — 그 위에 **실제 배포 외형을 !important로 덮어쓴다.** 지점 화면은 최상위에 `.branch-redesign` 클래스가 붙어 있어, 여기 규칙이 항상 마지막 승자다.

그래서 **탭에 무슨 색을 적든, 최종 색은 `.branch-redesign`가 정한다.** 규칙:

- 탭에서 `bg-blue-50`, `bg-emerald-50`, `bg-amber-50`, `bg-white` 같은 **Tailwind 표준 색**을 쓰면 → `.branch-redesign`가 자동으로 **디자인 토큰**으로 치환한다(아래 매핑 표). 그러니 **새 화면도 표준 Tailwind 색으로 칠하면 자동으로 팔레트에 맞는다.**
- **커스텀 hex(`bg-[#202A5A]` 등)나 전용 클래스(`order-cat-food` 등)는 치환을 타지 않는다.** 특정 색을 꼭 유지해야 할 때만 이 방식을 쓴다.
- 색을 새로 만들지 말 것. 아래 토큰 6개 안에서 해결한다.

Tailwind → 토큰 자동 치환(지점 화면 기준):

| 탭에서 쓴 클래스 | 실제 렌더 색 |
| --- | --- |
| `bg-blue-50/100`, `bg-sky-50`, `bg-indigo-50`, `bg-slate-50~100`, `bg-gray-50~100` | `--branch-alice` (#E6EBF3) |
| `bg-emerald-50/100`, `bg-green-50/100` | `--branch-honey` (#E0EBDC) |
| `bg-amber-50`, `bg-orange-50`, `bg-yellow-50`, `bg-rose-50`, `bg-red-50` | `--branch-vanilla` (#F4F2CC) |
| `bg-[#2E6DB4]`, `bg-blue-*`, `bg-emerald-*`, `bg-rose-*`, `bg-slate-700`, `bg-zinc-900` (버튼) | `--branch-black` (#212121) + 밝은 글자 |
| `border-gray-100/200`, `border-slate-100/200`, `border-blue-100`, `border-*-200` | 검정 또는 연회색 테두리(문맥별) |
| `text-blue-700`, `text-emerald-700`, `text-amber-700` 등 진한 색 글자 | `--branch-black` |
| `text-gray-500~800`, `text-slate-*`, `text-zinc-*` | `rgba(33,33,33,0.66)` |

**따라서 버튼은 색으로 의미를 구분하지 않는다** — 파랑·초록·빨강 버튼 모두 검정으로 렌더된다. 의미 구분은 위치·라벨·아이콘으로 한다.

---

## 3. 색상 토큰

`.branch-redesign`에 정의된 6색이 전부입니다(`src/index.css`).

| 이름 | 값 | 용도 |
| --- | --- | --- |
| `--branch-ghost` | `#F6F5FA` | 화면 전체 배경, 섹션 카드 바탕 |
| `--branch-alice` | `#E6EBF3` | 사이드바, **표 헤더(thead)·제목 밴드 짝색**, 부드러운 패널, 지점이동 상태 |
| `--branch-honey` | `#E0EBDC` | 완료·확정·긍정, **주류 재고/월초 칸**, 기존직원 |
| `--branch-vanilla` | `#F4F2CC` | **제목 밴드·섹션 제목 알약**, 공지·미확인·주의, 신규입사 |
| `--branch-black` | `#212121` | 본문 글자, **섹션 테두리(1px)**, 활성 버튼 배경 |

> **[개정 2026-08-04] 세 색을 관리자 팔레트와 같은 연한 값으로 통일했다**(사용자 지시 — 제목 밴드에 쓰던 값을 팔레트로 승격).
> 옛 값: 엘리스 `#D8DFE9` · 허니듀 `#CFDECA` · 바닐라 `#EFF0A3` (밝기 +7~9%p·채도 소폭 감소로 셋 다 같은 정도로 연해짐).
> 단 **상태 칩**(월말마감 제출완료/수정중, 근로계약서 색 사다리, 택시 상태 등)은 §11·DESIGN_ADMIN §2-1 "원래 채도 유지" 원칙대로 **옛 진한 값을 hex 로 유지**한다 — 연한 바탕 위에서 상태가 한눈에 갈려야 하기 때문.

상태·경고 전용 색(토큰 밖, 그대로 hex로 씀):

| 값 | 용도 |
| --- | --- |
| `#C93A3A` 테두리 / `#FDE2E2` 배경 / `#8F1F1F`·`#b91c1c` 글자 | 검증 오류, 필수 미입력, 현금 차이 불일치 |
| `#2E6DB4` | 포커스 링·활성 셀 아웃라인 등 **잔존 액션 블루**(버튼 배경으로는 검정으로 치환됨) |
| `#202A5A` | 주류 재고 시트 헤더 전용 네이비(치환 안 됨) |
| `#18181B` | 월말마감 "미제출" 상태 칩 |

---

## 4. 섹션 카드

모든 주요 업무 섹션(매출·현금마감·근무자·메모·상품등록·재고시트·발주리포트 등)은 **같은 카드 껍데기**를 씁니다.

**최종 렌더 값(`.branch-redesign`가 강제):**
- 배경: `--branch-ghost` (#F6F5FA)
- 테두리: **`1px solid --branch-black` (#212121)** — 지점 카드의 서명 같은 특징
- 모서리: **`border-radius: 28px`**
- 그림자: `box-shadow: 0 16px 38px rgba(33,33,33,0.035)` (일부 섹션 `0 18px 42px rgba(33,33,33,0.045)`)
- 안쪽 여백: `p-4`(대부분), 메모처럼 글이 많은 섹션은 `p-6`

**탭에서 작성하는 표준 레시피(이대로 쓰면 위 값으로 렌더됨):**
```
<section className="bg-white p-4 rounded-2xl border border-gray-100 shadow-sm space-y-3">
```
- `bg-white` → ghost, `rounded-2xl` → 28px, `border-gray-100` → 검정 1px, `shadow-sm` → 위 그림자. **반드시 `border-gray-100`(또는 `border-gray-200`)로 테두리를 준다** — slate/zinc/기타 색 테두리는 검정 치환에서 빠져 연회색으로 남는다(13번 참고).
- 표를 감싸는 스크롤 래퍼도 카드로 본다: `overflow-x-auto rounded-2xl border border-gray-100`.
- **카드 안에 카드를 모서리에 붙여 겹겹이 넣지 않는다.** 겹겹 구조가 꼭 필요하면 바깥 래퍼는 테두리 없는 투명 그룹으로 두고 안쪽 카드만 테두리를 갖는다(13번).

---

## 5. 섹션 사이 간격 · 섹션 내부 간격

- **탭 최상위(섹션과 섹션 사이): 세로 20~24px.** 발주·주류는 `space-y-5`(20px), 일일마감정산은 `space-y-6`(24px). 새 탭은 이 범위를 지킨다.
- **카드 내부 세로 리듬:** `space-y-3`(12px, 표 있는 섹션) 또는 `space-y-4`(16px, 글·폼 섹션).
- **카드 안쪽 여백:** `p-4`(16px) 기본, 글 많은 섹션 `p-6`(24px).
- **폼 그리드 간격:** 라벨+입력 그룹은 `gap-3`~`gap-4`, 칩·버튼 줄은 `gap-1.5`~`gap-2`.
- 표는 가로 스크롤 없이 한 화면에 읽히는 것을 우선한다. 폭이 부족하면 `table-fixed`·말줄임·짧은 라벨·`min-w-[…]`를 조합한다.

---

## 6. 섹션 제목 — 바닐라 알약 칩 (텍스트만)

지점 화면의 **모든 섹션 제목(`<h3>`, 메모 섹션은 `<label>`)은 바닐라색 알약 칩**으로 렌더됩니다. 이것이 이 화면의 가장 눈에 띄는 서명입니다.

**최종 렌더 값(`.branch-redesign`가 `<h3>`에 강제):**
- `display: inline-flex; width: fit-content` (글자 길이만큼만, 줄 전체를 채우지 않음)
- `align-items: center; gap: 8px`
- **`border-radius: 999px`** (완전 알약)
- **`border: 1px solid --branch-black`**
- **`background: --branch-vanilla` (#F4F2CC)** ← "제목을 채우는 색"
- `color: --branch-black`
- **`padding: 6px 12px`** [강제]
- **`font-size: 11px`** [강제] `; line-height: 1; font-weight: 900; letter-spacing: 0`

> **[강제·폰트 크기] 제목 알약은 11px이다.** index.css에 `font-size: 13px`짜리 알약 블록(`#tab-view-settle ... 2033`대)이 남아 있지만, **그 뒤의 여러 블록이 11px로 덮어써서 실제 렌더는 11px다**(뒤 블록이 이긴다). 13px는 죽은 규칙이니 참조하지 말 것. 새로 만드는 제목도 **반드시 11px**로 맞춘다. 지출내역·현금마감 등 모든 섹션 제목이 이 값이다.

**탭에서 작성하는 표준 레시피:**
```
<h3 className="text-sm font-black text-gray-900 w-fit">매출</h3>
```
`text-sm/text-gray-900`는 알약 규칙이 덮어쓰므로 신경 쓰지 않아도 되고, **`w-fit`만 지켜 폭이 글자에 맞게 한다.**

### 6-0. [강제] 한 섹션의 글자·간격 통일 — 제목↔표 간격 16px · 헤더/텍스트입력칸 폰트 = 제목 폰트(11px)

- **제목↔내용 간격:** 섹션 제목(알약)과 바로 아래 표/그리드 사이는 **16px**(`space-y-4`)를 띄운다. 기준 예시는 **현금·카드 지출내역 섹션**(`ExpenseGrid`) — 카드가 `space-y-4`라 제목 줄과 표 사이가 16px다. 제목이 표에 붙지 않게, 모든 섹션이 이 간격을 지킨다. (표를 `<table>`로 만들어 제목을 표 안 맨 윗줄에 둘 때는, 그 제목 셀의 `padding-bottom: 16px`로 같은 간격을 만든다.)
- **엑셀 컬럼 헤더 폰트 = 제목 폰트(11px):** 한 섹션 안에서 **엑셀 컬럼 헤더(thead th)의 글자 크기는 그 섹션 제목 알약과 같은 11px**로 맞춘다. 제목만 크고 헤더만 다른 크기면 한 섹션이 두 폰트로 쪼개져 어색하다. (헤더는 `font-weight: 900` 볼드·솔리드 검정은 그대로 두고 **크기만 11px**로.)
- **텍스트 입력칸 폰트(날짜·작성자 등 비숫자) = 발주 날짜 선택칸과 동일:** **11px · 볼드(700) · 시스템 폰트(모노 금지)**. 기준은 발주관리 '발주내역 리포트'의 날짜(월) 선택칸(§10, `text-[11px] font-bold`). 한 섹션 안에서 텍스트 값(날짜·작성자·이름 등)이 서로 다른 폰트/크기로 보이지 않게 통일한다. **숫자 입력칸만** §9대로 `font-mono` 우측정렬을 쓴다(크기는 같은 11px).

### 6-0-1. [강제] 폰트 크기 기준표 — 새 화면·수정 화면은 이 표만 보고 맞춘다

§6·§6-0·§9·§10에 흩어져 있던 글자 크기 규칙을 한 표로 모은 것이다(값은 그 조항들과 동일 — 어긋나면 그 조항이 우선이 아니라 **같은 값으로 맞춘다**). 관리자 화면도 `DESIGN_ADMIN.md` 상속 원칙에 따라 **이 표를 그대로 따른다**. 기준 적용 예: 법인택시 탭(2026-07-24 정렬).

| 요소 | 크기 | 굵기·색 | 비고 |
|---|---|---|---|
| 섹션 제목(알약) | **11px** [강제] | 900 · 검정 | §6. 텍스트만, 아이콘 금지 |
| 섹션 제목(**밴드**) | **14px** | 900 · 검정 | 2026-08-03 추가. 카드 맨 위 띠에 놓는 제목(`.admin-band-title` / `.branch-band-title`). 같은 띠 안의 부연이 11px라 **위계를 만들려면 제목이 커야 한다** — 같은 크기면 띠가 그냥 색칠한 줄로 보인다. **이 표에서 14px가 허용되는 유일한 자리**이고, 알약 제목은 11px 그대로다 |
| 밴드 안 필터 칸 | **11px** · 높이 **28px** · 알약(999px) | 900 · 1px 검정 · 흰 바탕 | 2026-08-03 추가(`.admin-band-filters` / `.branch-band-filters`). 아래 '입력칸·셀렉트'(h-8·rounded-lg)와 다른 값이다 — 밴드 안에서는 제목·필터가 한 줄로 균일해야 해서 예전 알약 제목과 같은 덩치로 맞춘다 |
| 표 헤더(`thead th`) | **11px** | 900(`font-black`) · `text-[#212121]` | §6-0. 제목과 같은 크기 — `text-gray-*` 금지 |
| 표 데이터 셀 | **12px**(`text-xs`) | 400~700 | §9. 표 전체에 `text-xs`를 주고 셀에서 개별 크기 지정 금지 |
| 입력칸·셀렉트(텍스트) | **11px**(`text-[11px]`) · 높이 `h-8` | 700(`font-bold`) · `rounded-lg` | §6-0·§10. 날짜/이름/월 선택 등 |
| 숫자 입력칸 | **11px** + `font-mono` 우측정렬 | 700 | §9. 금액·수량만 mono |
| 버튼(칩·알약·행동 버튼) | **11px** | 900(`font-black`) | §10. 크기 통일 — `text-sm` 버튼 금지 |
| 부제목·안내문·카드 라벨 | **11px** | 연회색(검정 40~60%) | §7 |
| KPI 요약 숫자(카드 값) | `text-lg` | 900 | 강조 숫자만 예외적으로 크게 |
| 오류·경고 배너 | **12px**(`text-xs`) | 700 · §11 오류색 | |

**금지:** 본문·표·버튼·입력칸에 `text-sm`(14px) 이상을 쓰지 않는다. 위 표에 없는 크기가 필요하면 이 표에 먼저 추가하고 쓴다. **밴드 제목(14px)은 이 표에 등재된 예외**이므로 새로 만들 때 그대로 쓰면 된다 — 다만 밴드 제목 말고 다른 자리에 14px를 쓰지 않는다.

**알려진 레거시 위반(다음에 손볼 때 11px로 맞춘다):** 관리자 모바일 하위탭 버튼(분석·전일정산·월말마감의 `text-sm font-bold border-b-2` 줄) — 법인택시 탭은 2026-07-24부터 11px/900 준수, 나머지는 미정비.

### 6-0-2. [P0] 새 탭을 만들면 알약 제목이 자동으로 안 붙는다 — 공용 클래스를 쓸 것

지점 알약 제목 CSS는 역사적으로 **탭마다 ID 셀렉터**(`#tab-view-settle`, `#labor-contract-tab`, `#monthly-settle-tab-root` …)로 개별 지정돼 있다. 그래서 **새 탭 파일을 만들면 그 목록에 없어 제목이 평범한 글씨로 렌더된다.** (비즈니스택시 탭에서 실제 발생 — 2026-07-24)

- **새 지점 탭**: `<h3 className="branch-pill-title">제목</h3>` — `.branch-redesign .branch-pill-title` 규칙이 §6 값 그대로 적용한다.
- **새 관리자 탭**: 제목은 알약이 아니라 **밴드**다 — `<h3 className="admin-band-title">`을 `.admin-band` 안에 둔다(`DESIGN_ADMIN.md` §4-0, 2026-08-03 개정). `.admin-pill-title`은 섹션 제목으로 새로 만들지 않는다(카드 중간에서 값과 나란히 놓는 라벨 정도에만 남긴다). 탭 이름 자체는 `AdminPage`의 `<main>`이 한 곳에서 그리므로 화면에서 또 적지 않는다.
- **index.css 에 새 ID 규칙을 추가하지 말 것** — 같은 사고가 다음 탭에서 또 난다.

### 6-1. [규칙] 제목에는 텍스트만 — 아이콘·이미지 금지

- **섹션 제목 안에는 글자만 넣는다.** `<svg>` 아이콘, 이미지, 장식 요소를 넣지 않는다.
- 이유: 알약 칩은 11px로 작아, 아이콘이 들어가면 칩이 커지고 제목마다 크기가 들쭉날쭉해진다. **발주·주류 탭은 이 규칙을 지켜 텍스트만** 넣는다(`거래처 추가`, `발주내역 리포트`, `주류 재고 관리표`, `재고 시트`).
- **현재 예외(고쳐야 할 위반):** 일일마감정산 탭의 제목들은 아이콘이 들어가 있다 — 매출(`CircleDollarSign`), 현금마감(`Coins`), 근무자(`Clock`), 메모(`FileText`). 규칙상 이 아이콘들은 제거 대상이다. (제거는 코드 수정이므로 별도 리뷰·로컬확인·배포 승인 절차를 따른다.)

### 6-2. [규칙] 제목 `<h3>`를 `<div>`로 감싸지 않는다

- 알약 스타일은 `#sales-section > h3`처럼 **직계 자식 선택자**로 붙는다. 제목을 `<div>`로 한 겹 감싸면 손자가 되어 알약이 사라진다.
- 제목 옆에 합계·버튼을 나란히 두고 싶으면, 그것들을 **제목의 형제**로 두고 **부모에 `flex`/`grid`**로 정렬한다(예: `sales-section-head` 클래스가 `grid-template-columns: 1fr auto`로 제목과 합계를 한 줄에 세운다). 제목 자체는 감싸지 않는다.

---

### 6-3. [표준] 제목 밴드 · 엑셀형 표 — 지점 화면 (2026-08-03 사용자 지시)

> **관리자 화면(`DESIGN_ADMIN.md` §4-0)과 구조·값이 같고 토큰만 `--branch-*`다.** 값을 손으로 다시 적지 말고 클래스만 붙인다.
> 지금 적용된 곳: **월말 업무 · 인사·기타** 탭. 일일 업무 탭들은 아직 알약 제목(§6)이며, 손댈 때 함께 옮긴다.

```tsx
<section className="branch-sheet-card">
  <div className="branch-band">
    <h3 className="branch-band-title">제목</h3>
    <div className="branch-band-filters">…필터…</div>
    <p className="branch-band-meta">부연</p>
    <div className="branch-band-actions">…버튼…</div>
  </div>
  <div className="branch-sheet-scroll"><table className="branch-sheet"> … </table></div>
</section>
```

| 항목 | 값 |
| --- | --- |
| 카드 | 1px 검정 · 모서리 **16px** · `overflow: hidden` · **padding 없음**(밴드가 폭을 꽉 채워야 한다 — 안쪽 내용이 자기 여백을 갖는다) |
| 밴드 | 바닐라 `--branch-vanilla`(#F4F2CC — 관리자와 같은 값, 팔레트 개정 2026-08-04) + 아래 1px 검정 · `min-height: 53px` · `padding: 10px 16px` |
| 제목 / 부연 | **14px·900** / 11px·700 (§6-0-1 등재값) |
| 자리 | `justify-content: flex-start` + `order`(제목 0 · 필터 1 · 부연 2 · 버튼 3, 버튼만 `margin-left:auto`). **`space-between` 금지** — 항목이 폭 전체로 벌어져 필터가 제목에서 멀어진다 |
| 표 헤더 | 엘리스 `--branch-alice`(#E6EBF3 — 관리자와 같은 값) · 11px·900·`#212121` · **밴드(또는 도구 줄) 바로 아래 붙인다** — 사이에 여백·상자 금지 |
| 표 본문 | 12px · 여백 `6px 8px` · 옅은 격자 |
| 선 | §9-1-A(오른쪽·아래에만, 전부 1px, 바깥 네 변 금지) · 헤더 고정은 §9-1-B |

**[강제 · 2026-08-04] 표는 카드 안에 "떠 있는 상자"로 두지 않는다.** 카드 본문을 `p-4` 로 감싸고 표를
`rounded-2xl border` 상자에 넣으면 관리자와 다른 "박스 속 박스"가 된다(매입집계·현금지출에서 실제 지적).
표 스크롤 래퍼는 여백·테두리·둥근 모서리 없이 밴드(또는 아래 도구 줄) 바로 다음에 둔다. 경고·안내처럼
표가 아닌 블록만 `mx-4` 여백을 갖는다.

**`.branch-band-toolbar` — 밴드의 연장 줄.** 표 조작 버튼(업체·행 추가, 자동저장 배지, 키 이동 칩)이
밴드에 못 들어갈 때(밴드는 부모 컴포넌트가, 버튼은 자식 탭이 그릴 때) 밴드와 같은 색(#F4F2CC)의 줄을
밴드 바로 아래 잇고, 그 아래에 표 머리글을 붙인다. 밴드에 직접 붙는 자리(직계 또는 다음 요소의 첫 자식)면
CSS 가 윗선을 지워 한 덩어리로 보인다.

**키 이동 칩(`SheetKeyHint`)은 푸른 계열 고정, 위치는 표 위 가운데(경계 걸침).** 표/본문 영역을
`relative` 래퍼로 감싸고 그 위에 절대배치로 띄운다 — 밴드·도구 줄과의 경계에 반쯤 걸치는 것이 표준 모양
(사용자 지시 2026-08-04 "기존 위치"). **단 카드 최상단에 두면 안 된다** — 표준 카드는 `overflow: hidden`
이라 카드 바깥으로 나간 위쪽 절반이 잘린다(실제 발생). 반드시 카드 **안쪽** 요소(표 스크롤 래퍼·본문 래퍼)를
기준으로 띄울 것. `inline` 변형은 밴드/도구 줄 안에 일반 흐름으로 넣고 싶을 때만.

**[P0 — 옛 ID 규칙이 밴드를 덮는다]** 지점 제목 CSS는 탭마다 `#labor-contract-tab h3`, `#cash-management-subtab h3` 처럼 **ID 셀렉터로 h3를 알약으로 `!important` 강제**한다(§6-0-2가 경고한 그 구조). 클래스만 붙인 밴드 제목은 ID 특이성에 밀려 **알약으로 그려진다.** 카드 모서리도 `#labor-contract-tab > section { border-radius: 28px !important }` 가 덮는다.

- 그래서 **지점 스코프의 h3 규칙 43곳 전부**에 `:not(.branch-band-title)` 을, 카드 모서리 규칙에 `:not(.branch-sheet-card)` 를 달아 두었다(index.css).
  - 처음엔 눈에 띄는 ID 몇 개만 골라 예외를 팠는데, **`#monthly-settle-tab-root h3` 가 다른 줄에 또 있어 월말마감 제목이 계속 알약으로 나왔다**(코덱스 stop-time 2026-08-03). 하나씩 쫓지 말고 **전부** 달아야 한다 — 아직 안 옮긴 탭에는 밴드 제목이 없으므로 예외를 달아도 동작이 달라지지 않는다.
- 새 탭을 밴드로 옮길 때 **제목이 알약으로 나오거나 모서리가 다르면 이 ID 규칙부터 찾아본다.**
- 모든 탭을 밴드로 옮기고 나면 그때 ID 규칙들을 통째로 걷어낸다.

**[P0 — 덮이는 것은 제목만이 아니다 · 2026-08-04]** 위 예외를 h3·모서리에만 달았더니, 같은 ID 규칙들이
**밴드 배경 · 카드 테두리 · 밴드 안 입력칸**까지 덮고 있었다. 네 가지가 실제로 화면에서 어긋났다.

| 덮은 규칙 | 증상 | 처방 |
| --- | --- | --- |
| `#cash-expenses-subtab > div { background: 고스트 !important }` (현금관리·카드지출·파트타이머급여도 같음) | **밴드가 흰색.** 카드가 그 id 를 달고 있어 밴드가 곧 `> div` 다 | 셀렉터에 `:not(.branch-band)` |
| `#monthly-settle-tab-root > div { border-color: transparent !important }` | 매출집계·매입집계 **카드 테두리가 사라짐** | `:not(.branch-sheet-card)` |
| `main > div > section { border: 0 !important }` | id 래퍼가 없는 **비즈니스택시** 카드만 테두리 없음 | `:not(.branch-sheet-card)` |
| `#monthly-settle-tab-root input[type="month"] { background: 바닐라 !important }` | 밴드 안 결산월 칸이 **흰 알약이 아니라 노란 알약** | 규칙 삭제(결산월 칸은 이제 전부 밴드 안) |

- **`:where()` 는 특이성 0** 이다. `.branch-band-filters :where(select, input…)` 은 `!important` 를 붙여도
  ID 규칙(1,2,1)에 진다. 밴드 부품이 안 먹으면 **먼저 특이성을 세어 보고**, 순서를 바꾸려 들지 말 것 —
  실제로 "파일 맨 끝에서 다시 지정한다"고 적어 둔 블록이 특이성에 밀려 아무 일도 못 하고 있었다.
- **확인 방법(추측 금지):** `npm run build` 로 나온 `dist/assets/index-*.css` 를 붙인 빈 HTML 에
  **실제 조상 구조 그대로** 밴드를 재현하고, 헤드리스 크롬(`--dump-dom`)에서 `getComputedStyle` 을 찍는다.
  로그인 없이 13개 탭 전부를 한 번에 대조할 수 있고, 고친 뒤 같은 시험으로 되돌아보기까지 된다.

**[개정 2026-08-04·2단계] 지점 팔레트 자체를 관리자와 같은 값으로 통일했다.** 1단계에서는 밴드·헤더에만 관리자 값(#F4F2CC/#E6EBF3)을 박고 토큰은 진한 값으로 남겼는데, 같은 날 사용자 지시("등록된 색을 지금 밴드·헤더 색으로 바꿔라")로 `--branch-vanilla/alice/honey` 토큰 값을 통째로 연한 값으로 올렸다(§3). 이제 밴드·헤더 규칙은 다시 토큰을 참조한다 — 값을 바꿀 일이 있으면 §3 토큰 한 곳만 고친다. 알약 제목·사이드바 활성·커서 행 강조도 함께 연해졌고, **상태 칩만** 옛 진한 값을 유지한다(§3 개정 주석).

#### 6-3-1. `.branch-sheet-head` — 머리글만 옮기는 갈래

`.branch-sheet` 는 **본문 격자까지** 정한다. 칸이 전부 편집 입력칸이거나(매입매출·파트타이머 급여대장) 이미 자기 격자·행 강조를 가진 표(근무일지·초과근무·본사 근무내역·직원현황)에 그대로 씌우면 셀 테두리·여백·정렬이 통째로 흔들린다. **바꿀 이유가 머리글 하나뿐일 때** 표에 `branch-sheet-head` 만 붙인다.

```tsx
<table className="branch-sheet-head w-full text-left text-xs border-collapse">
  <thead><tr><th>날짜</th>…</tr></thead>   {/* th 에 색·굵기·여백을 적지 않는다 */}
```

- 머리글은 `.branch-sheet` 와 **같은 값**(엘리스·11px·900·`#212121`·`padding: 8px`·`top:0` 고정)을 받는다. 본문은 손대지 않는다.
- **`<thead>`·`<tr>` 에 남아 있던 `bg-*` / `text-gray-*` / `shadow-[…]` / `border-b` 는 지운다.** 남겨 두면 위 선과 겹쳐 두 줄로 보인다(§9-1-A). CSS 가 `thead tr` 의 테두리·그림자를 `none` 으로 눌러 두긴 했지만, 코드에도 남기면 다음 사람이 어느 쪽이 진짜인지 모른다.
- `bg-*` 유틸리티를 이기려고 `background`·`color` 에 `!important` 가 걸려 있다 — 머리글 색을 이 갈래에서 개별로 바꿀 수는 없다. 다른 색이 필요하면 `.branch-sheet` 쪽으로 옮길 때가 된 것이다.

---

## 7. 헤더 바 · 구분선

제목 아래에 부제목·컨트롤이 붙는 큰 섹션(재고 시트, 발주내역 리포트, 근무자)은 **헤더 영역을 얇은 구분선으로 표 본문과 나눈다.**

- 헤더 영역: `p-4` 여백 + **아래 구분선 `border-b border-gray-100`**.
- 부제목: `text-[11px] text-gray-400`(약 11px, 연회색). 강조 단어만 `text-slate-600`.
- 헤더 안에서 제목과 오른쪽 컨트롤(월 이동·저장상태·필터)은 `flex ... justify-between gap-2~3`로 벌린다.
- 근무자 섹션처럼 제목 줄 자체에 구분선을 둘 때: `flex ... justify-between gap-2 border-b border-gray-100 pb-3`.

---

## 8. 표 (일반 표)

일일마감정산 근무자 표, 발주 매트릭스처럼 **입력/조회 표**의 공통 규칙.

**헤더(thead / th) — 최종 렌더:**
- 배경: `--branch-alice` (#E6EBF3), 글자 검정, `font-black`.
- 테두리: `rgba(33,33,33,0.12)`.
- 위에 고정: `sticky top-0 z-10`(이상). 좌측 고정열이 있으면 헤더 셀은 `z-30`, 본문 고정열은 `z-20`.
- 셀 여백: `py-2 px-2`(좁으면 `px-1`).

**본문(tbody / td):**
- **줄무늬(zebra)는 자동 적용:** `tbody tr:nth-child(even)` = ghost+alice 혼합, `odd` = 거의 흰색. 탭에서 `divide-y divide-gray-100`으로 작성해도, 최종엔 이 줄무늬가 얹힌다.
- hover: ghost+honey 혼합(연한 초록빛).
- 셀 여백: `py-1.5 px-2`. 숫자·날짜 칸은 `font-mono` + `font-bold`/`font-black`, 우측 정렬.
- 이름·사유·상품명처럼 길어질 수 있는 값은 말줄임 + `title`.
- 빈 상태: `py-10 text-center text-gray-400`.

**[규칙] 줄무늬(`nth-child`)를 이겨야 하는 색은 ID 특이성으로 이긴다.**
줄무늬 규칙이 `!important`라, 특정 행/셀에 분류색을 칠하려면 같은 `!important`로는 못 이긴다. `#purchase-sales-subtab tbody tr[data-cat="식재료비"]`처럼 **ID를 낀 선택자**로 특이성을 높여 이긴다(식재료비=바닐라, 주류비=허니, 식음료외기타=엘리스).

### 8-1. 행 삭제 · 이름 수정 (이름이 들어간 표)

이름(성명)이 들어간 모든 표 — 근무자·직원현황·파트타이머 등 — 는 아래를 지킨다.

- **행 삭제는 별도 "삭제" 컬럼을 두지 않는다.** 대신 **이름 바로 옆에 작은 `×` 버튼**을 놓아 그 자리에서 지운다. (표 오른쪽 끝 삭제 컬럼은 스캔을 방해하고, 이름과 멀어 어느 행을 지우는지 헷갈린다.)
- `×` 버튼: 평상시 연회색(`text-gray-300`~`400`), hover 시 `text-rose-600`. 접근성용 `aria-label`/`title`에 "○○○ 삭제"(또는 "N번 행 삭제")를 단다. 아이콘은 lucide `X` 또는 텍스트 `×`.
- **이름은 그 자리에서 수정(인라인)할 수 있게 한다.** 읽기 전용 텍스트로만 두지 말고, 클릭/포커스로 편집되는 입력칸으로 두거나 연필 버튼으로 편집 모드를 연다. 저장 전에는 원본을 바꾸지 않고, 취소로 되돌릴 수 있게 한다.

### 8-2. 항목 추가 UI — 기준: 발주관리 '거래처 추가' / 일일마감정산 '근무자 추가'

표·시트에 **행(항목)을 더하는 입력줄**(근무자 추가, 주류 추가, 거래처 추가 등)은 아래 한 가지 방식으로 통일한다. 여러 단계(초안 여러 줄 → "행 추가" → "등록")로 나누지 말고 **한 줄에서 즉시 추가**한다.

- **한 줄 가로 배치:** `flex flex-wrap items-center gap-1.5`. 입력칸을 세로로 쌓지 않는다.
- **왼쪽에 짧은 라벨(선택):** `text-[11px] font-black text-gray-500`(예: "근무자 추가"). 섹션 제목이 이미 있으면 생략 가능.
- **입력칸·셀렉트:** 높이 `h-8`, 글자 `text-[11px]`, `rounded-lg`, `border border-gray-200 bg-white`. 모든 칸 높이를 `h-8`로 맞춘다.
- **추가 버튼(오른쪽 끝):** **어두운 알약 + 아이콘 + 텍스트** — `flex h-8 items-center gap-1 rounded-lg bg-slate-800 px-3 text-[11px] font-black text-white hover:bg-black`, 안에 `<Plus className="h-3.5 w-3.5" /> 추가`. (lucide `Plus` 아이콘 필수.)
- **동작:** 추가를 누르면(또는 이름칸에서 **Enter**) 아래 표에 **바로 한 항목이 붙는다.** 여러 개는 반복 입력한다(항목마다 구분·사유가 다르면 쉼표 일괄입력이 아니라 한 개씩). 추가 후 입력줄은 비운다.
- **즉시 검증:** 필수값(사유 등)이 비어 있으면 **그 칸에 `ring-2 ring-rose-400`을 바로 표시**한다 — 추가를 눌러 봐야 알게 하지 않는다.
- **[금지] 엘리스블루 틴트 배경:** 입력줄 컨테이너에 `bg-zinc-50`/`bg-gray-50`/`bg-slate-50`을 쓰지 않는다. `.branch-redesign`가 이를 **ghost+alice 혼합(파란기 도는 엘리스블루)**으로 치환해 회색 대신 파란 막대처럼 보인다. 배경 없이 **테두리만**(`border border-gray-100 rounded-xl p-3`) 두거나, 카드로 떼려면 `bg-white`(→ghost, 거의 흰색)를 쓴다.

---

## 9. 엑셀형 시트 (주류 재고 · 발주내역 리포트 · 매입매출 · 급여대장 · 근무자)

> **"이 섹션을 엑셀 형식으로 만들어줘"** 라고 지시받으면 이 §9 규칙을 그대로 적용한다. 새 섹션을 엑셀화할 때 아래 항목(테두리 방식·고정열·셀 색·입력칸 투명화·비활성 잠금·행 강조)을 빠짐없이 배선한다. 기준 예시는 주류 재고 시트와 지출 그리드(`ExpenseGrid`)다.

### 9-1. 엑셀 형식 표준 레시피 — 기준: 정직원 급여대장 / 근무자 표

"엑셀 형식"으로 만들 때는 아래를 그대로 맞춘다(값 확정, 2026-07-19 사용자 승인).

- **헤더(thead th / 세로형이면 왼쪽 라벨 열)**: **[개정 2026-08-04]** 제목 밴드를 쓰는 표(월말 업무·인사·기타 전부)는 배경 **엘리스 `--branch-alice`(#E6EBF3)** + 선은 **오른쪽·아래만 1px 검정**(§9-1-A — 위·왼쪽은 밴드 아래선·카드 테두리가 이미 그린다). 밴드 없는 옛 시트(일일 업무의 근무자·주류 재고 등)만 종전대로 바닐라 `--branch-vanilla`(#F4F2CC, 팔레트 개정으로 연해짐) + 사방 1px 테두리(위·오른·아래, 첫 칸 왼쪽)를 유지한다.
  **헤더 글자는 반드시 볼드(`font-black`, 900) · 솔리드 검정**, **크기는 그 섹션 제목 알약과 같게 맞춘다(§6-0 — 현재 11px)**. (근무자·급여대장 같은 큰 데이터 시트는 종전 12px을 쓰고 있으니, 다음에 손볼 때 11px로 맞춘다.) **주의: 글자색에 `text-gray-*`를 쓰지 말 것** — `.branch-redesign`가 68% 회색으로 치환해 볼드가 흐려 보인다. `text-[#212121]`(또는 `--branch-black`)로 100% 검정을 준다. (본문 값 칸은 볼드 아님/옅은 격자로 헤더와 대비를 준다.)
  > **[관리자 화면은 헤더가 바닐라가 아니라 엘리스다]** 관리자 표는 위에 **바닐라 제목 밴드**가 붙으므로(`DESIGN_ADMIN.md` §4-0) 헤더까지 바닐라면 두 줄이 한 덩어리로 뭉쳐 제목과 표의 경계가 사라진다. 그래서 관리자에서는 헤더를 `--admin-alice`로 쓴다. **지점 화면도 밴드를 쓰는 탭은 같은 이유로 엘리스다**(2026-08-03 — 월말 업무·인사·기타). 밴드가 없는 탭(일일 업무 등)은 바닐라 그대로다. 지점용 부품은 `.branch-sheet-card` / `.branch-band*` / `.branch-sheet`(src/index.css) — 관리자쪽과 구조·값이 같고 토큰만 `--branch-*`다. **관리자 클래스를 지점 화면에 쓰면 변수 미정의로 배경이 아예 안 칠해진다**(DESIGN_ADMIN.md §1). 관리자 표를 만들 땐 `DESIGN_ADMIN.md` §4-0의 `.admin-sheet`를 쓰면 이 값이 이미 들어 있다.
- **셀 입력칸**: `sheet-cell-input` 클래스로 **납작 투명**(배경·테두리·라운드 없음). 둥근 입력박스 금지. 셀 높이는 `h-9`로 통일. **데이터 입력 칸 글자는 근무자 표 이름칸과 같은 `text-xs`(12px)로 모두 통일**한다(엑셀 안에서 폰트 크기가 칸마다 달라지지 않게).
- **격자선**: 감싸는 `<td>`가 `border-r border-b border-black/10`로 옅게 긋는다. `table`은 `border-separate; border-spacing:0`. → **선을 어느 변에 그을지는 아래 9-1-A를 반드시 따른다.**

#### 9-1-A. [P0] 선은 **오른쪽·아래에만** 긋는다 — "구분선이 겹쳐 두꺼워 보이는" 문제 방지

2026-08-03에 손익 요약표에서 이 문제로 다섯 번 되돌렸다. **한 경계에 선을 두 번 그리는 것**이 원인이고, 굵기를 올리면 더 나빠진다.

| 규칙 | 안 지키면 |
| --- | --- |
| 세로선은 **칸의 오른쪽에만**. 왼쪽에 긋지 않는다 | 이웃한 두 칸이 같은 경계를 각각 그려 **선이 두 줄**이 된다 |
| 가로선은 **칸의 아래에만**. 위에 긋지 않는다 | 위아래 행이 같은 경계를 각각 그려 두 줄 |
| **그룹 경계선도 같은 변에** 맞춘다 — 그룹의 *마지막 칸 오른쪽*(`admin-group-end` 식)에 긋는다 | 격자는 오른쪽, 그룹선만 왼쪽이면 경계마다 선이 나란히 두 개 선다 |
| 표의 **맨 왼쪽·맨 위·맨 오른쪽·맨 아래**에는 긋지 않는다 | 카드 테두리(§4)·제목 밴드의 아래 선과 겹쳐 두 줄이 된다 |
| 굵기는 **전부 1px**. 강조는 굵기가 아니라 **색**으로 한다(예: 고정열이 끝나는 선 = 1px + 진한 회색) | 2px는 그 자체가 두 줄로 보이고, 1px 구간과 만나는 자리에서 굵기가 꺾여 더 겹쳐 보인다 |
| 맨 오른쪽 칸을 `th:last-child`로 고르지 않는다 | 2단 헤더의 **아랫줄 마지막 칸**까지 걸려 멀쩡한 그룹 경계가 사라진다. 전용 클래스로 그 칸만 짚는다 |

- **선이 흐리거나 사라져 보이면 굵기부터 올리지 말 것.** 먼저 ①같은 자리에 두 번 그리고 있지 않은지, ②sticky 칸에 `border`를 쓰고 있지 않은지(아래 항목) 확인한다.

#### 9-1-B. [P0] 스크롤되는 표는 **헤더를 반드시 고정**한다 (2026-08-03 사용자 지시)

**예외 없다.** 표가 자기 상자 안에서 스크롤하든 페이지가 통째로 내려가든, 컬럼 이름이 화면에서 사라지면 그 표는 읽을 수 없다 — 어느 칸이 '금액'이고 어느 칸이 '전월비'인지 알 수 없기 때문이다.

- `thead th`에 `position: sticky; top: 0`. 헤더가 2단이면 아랫줄은 윗줄 높이만큼 `top`을 내린다(예: 윗줄 32px → 아랫줄 `top: 32px`).
- **배경은 `thead tr`이 아니라 `th`에 직접 준다.** `tr` 배경은 본문이 그 위로 지나갈 때 헤더를 덮지 못해 글자가 비쳐 보인다.
- 좌우로도 미는 표에서 고정열이 있으면 `z`를 층으로 나눈다: 헤더 셀 `z-30` · 본문 고정열 `z-20` · 고정열 ✕ 헤더가 만나는 칸은 한 층 더 위.
- 표는 `border-separate; border-spacing: 0`이라야 고정된 칸의 테두리가 함께 움직인다(위 항목). 헤더 선은 `border` 대신 `inset box-shadow`로 그린다.
- 관리자 화면은 `.admin-sheet`(`DESIGN_ADMIN.md` §4-0)가 이 배선을 이미 갖고 있으므로 **화면에서 sticky를 다시 적지 않는다.**
- **계산·표시 칸**(근무시간·초과 등): 칩 배경 없이 **납작 mono 텍스트**로, 색 의미(+초과=초록 `text-emerald-600` / −=빨강 `text-rose-500`)는 글자색으로만.
- **검증 오류**: 입력칸이 아니라 **셀(td) 배경을 붉게**(`bg-[#FDE2E2]`) 칠한다.
- **행 삭제(×)**: 이름 오른쪽에 **검정 사각 버튼 + 흰 ×**(`bg-zinc-800 text-white w-5 h-5 rounded-md`, hover `bg-black`). 이름은 인라인 수정(§8-1).
- **전역 input !important 이기기**: `.sheet-cell-input` 투명화는 `#<섹션id> .sheet-cell-input`처럼 **ID 특이성**으로 전역 `.branch-redesign input` 규칙을 이긴다. index.css를 못 건드릴 상황이면 컴포넌트에서 **전용 CSS 파일을 import**한다(근무자 표 `src/pages/branch/tabs/staffSheet.css`가 표준 예시).
- **컬럼 폭**: `w-full` 표에서 컬럼 수가 적으면 각 칸이 남는 폭을 나눠 가져 **과도하게 넓어진다.** 데이터 칸엔 `w-20`~`w-28` 등 고정폭을 주고, **긴 텍스트 칸(사유 등) 하나만 폭 없이 두어 남는 폭을 흡수**시킨다. 세로형 여러 개를 나란히 둘 땐 각 폭을 좁게 고정하고 **사이를 gap(§5)으로 띄운다**(카드를 `w-fit`으로 두면 불필요한 여백이 안 생긴다). **세로형(라벨|값)은 라벨 칸을 고정폭(예: `grid-cols-[120px_minmax(0,1fr)]`)으로 준다** — 행마다 `auto`로 두면 라벨 길이에 따라 폭이 제각각이 되어 **열 구분선(라벨|값)이 지그재그**가 된다. 고정폭이라야 구분선이 일직선이 된다.
- **테두리는 사각(둥근 모서리 없음).** **각 헤더 셀은 사방을 검정 테두리로 닫는다**(근무자 표 헤더처럼 또렷한 검정 격자).
  - **세로형(라벨|값) 컴팩트 폼**은 **격자를 균일한 검정으로 통일**한다 — 라벨만 검정·값만 옅은 회색으로 두면 가로 구분선이 "검정→회색" 두 톤으로 쪼개져 어색하다. **헤더 구분은 테두리색이 아니라 바닐라 배경**이 맡는다.
  - **큰 가로형 표(근무자·급여대장)** 는 헤더 행만 검정 격자로 또렷하게, 본문 셀은 옅은 격자(`border-black/10`)로 둬 대비를 준다(헤더가 별도 맨윗줄이라 두 톤 문제가 없다).
- **여러 섹션을 한 카드로 묶어도** 바깥 카드는 다른 섹션과 같은 검정 테두리(§4)를 갖고, 각 구획 제목은 §6 바닐라 알약을 그대로 적용한다.

날짜별로/줄별로 칸에 바로 값을 적는 **스프레드시트형 표**는 일반 표와 규칙이 다르다.

- **`border-collapse`가 아니라 `border-separate; border-spacing: 0`을 쓴다.** collapse에서는 sticky 헤더/고정열이 스크롤될 때 크롬이 테두리를 셀과 함께 그리지 못해 선이 깨진다. separate에서는 각 칸이 자기 테두리를 갖고 함께 움직인다.
- **[2026-08-03 보강] `border-separate`만으로 부족한 경우가 있다 — 고정 칸의 선은 `border`가 아니라 `inset box-shadow`로 그린다.** separate여도 크롬이 sticky 칸의 `border`를 그리다 빠뜨리는 일이 있다. 손익 요약표에서 실제로 두 증상이 같이 났다(사용자 지적):
  - 표를 아래로 내리는 순간 **헤더의 선이 사라진다.**
  - **위·왼쪽 두 방향 모두 sticky인 칸**(고정열 ✕ 헤더가 만나는 칸)은 스크롤 전에도 아래 선이 안 그려져 **헤더 밑줄이 그 칸에서 끊긴다.** `rowSpan`이 걸려 있으면 더 잘 난다.
  - → 해당 칸에 `border: 0`을 주고 `box-shadow: inset -1px 0 0 0 <색>, inset 0 -1px 0 0 <색>`(오른쪽·아래)로 대체한다. 그림자는 칸이 어디로 밀려가든 함께 그려진다.
  - **[함정] `box-shadow`는 겹쳐 쌓이지 않고 통째로 갈린다.** 어떤 칸에 다른 그림자(진한 2px 고정열 선, §9-3 커서 띠 등)를 따로 주면 **격자선까지 그 선언에 다시 적어야** 한다. 빠뜨리면 그 칸만 선이 없어진다.
  - **본문 셀에는 함부로 쓰지 않는다** — §9-3 커서 띠가 이미 `box-shadow`를 쓰므로, 선택된 행에서 서로 덮어 지운다. 위 증상이 실제로 난 곳(대개 `thead`)에만 적용한다.
  - 왼쪽·위 선은 대개 **카드 테두리와 제목 밴드**가 이미 그린다. 거기에 또 그으면 검정 선이 두 줄로 겹쳐 굵어 보인다.
- **셀 테두리·배경은 `<td>`가 그리고, 입력칸은 투명하게 비운다.** `.branch-redesign`가 input의 배경·테두리를 `!important`로 강제하므로, `.sheet-cell-input`/`.liquor-sheet-cell`을 ID 특이성으로 이겨 `background:transparent; border:0`으로 되돌린다.
- **쓸 수 없는 칸(disabled)은 회색으로 잠근다:** `rgba(33,33,33,0.06)` 배경, `rgba(33,33,33,0.35)` 글자, `cursor: not-allowed`.
- **[필수·키보드 이동] 엑셀 셀은 방향키·Enter·Tab으로 칸 사이를 오갈 수 있어야 한다.** `useSheetKeyboardNav` 훅을 쓰고 각 입력칸에 `cellProps(row, col)`을 배선한다(근무자 표·지출 그리드가 표준 예시). **"엑셀 형식으로 만들어줘" 요청에는 이 키보드 이동을 항상 함께 넣는다 — 빠뜨리면 엑셀이 아니다.**
- **[중요·IME] 방향키/Tab으로 오가는 엑셀 셀 입력칸에 `inputMode="numeric"`(또는 `type="number"`)를 넣지 않는다.** 윈도우 IME가 그 칸에 들어갈 때 자동으로 영문으로 전환되고 **그 상태가 전역에 남아**, 다음 칸으로 이동하면 **한글 칸에 영문이 찍히거나 한글이 안 쳐지는** 사고가 난다("칸을 이동하다 영문으로 기록되는" 원인). 숫자만 받으려면 `type="text"` + `onChange`에서 숫자 필터 + `onCompositionEnd`로 조합 종료 후 재적용한다(지출 그리드 `ExpenseGrid`가 표준 예시). 모바일 카드형 입력엔 `inputMode="numeric"`을 써도 되지만 **데스크톱 시트 셀엔 절대 넣지 않는다.**
- **좌측 고정열(분류·상품명·단가 등)** 은 `sticky left-0` + 흰 배경 + 우측 테두리. 마지막 고정열은 `border-r-2 border-r-slate-900`으로 고정 블록을 닫는다.
- **[표준·커서 위치 표시] 커서(포커스)가 있는 셀/행은 연한 바닐라로 짚어 준다.** 시트가 넓어 값을 여러 칸에 적다 보면 어느 행인지 놓치므로, 커서 위치를 항상 표시한다. **기준 디자인은 주류재고 시트(§9-2)** — 행 배경을 통째로 갈아엎지 말고, 그 행 각 `<td>`에 **바닐라 35% 얇은 막**(`inset 0 0 0 9999px color-mix(in srgb, var(--branch-vanilla) 35%, transparent)`) **+ 위·아래 검정 선**(`inset 0 ±1px var(--branch-black)`)을 box-shadow로 얹어 **띠처럼** 보이게 한다(`tbody tr:focus-within > td`). 칸마다 배경색이 다른 시트(입고/판매/재고)에서도 그 구분이 남아야 하므로 배경 교체가 아니라 이 '막+선' 방식을 쓴다. 고정열(`.sticky`)은 전역 box-shadow 규칙이 `!important`라 그 그림자(`0 10px 28px`)를 여기 다시 적어야 왼쪽 선이 안 끊긴다. 셀 입력칸이 투명한 표는 색을 `<td>`가 아니라 **`tr` 배경**에 얹는다 — `td`에 `!important`로 얹으면 그 행의 **검증오류 셀(빨강 `bg-[#FDE2E2]`)까지 덮어** 경고가 사라진다(tr 배경은 투명 셀 뒤로 비치고, 오류 셀은 자기 빨강을 그 위에 그린다). **행/셀이 이미 상태색을 지녀도(근무 완료 행의 바닐라 채움, 오류 셀의 빨강 등) 막은 반투명(35%)이라 상태색이 그대로 비치므로 표준 '막+선'을 그대로 쓴다** — 오류 셀은 65%가 남아 여전히 빨갛고, 바닐라 행은 조금 더 진해질 뿐이다. (근무자 표는 종전에 '선만 쓰는 변형'이었으나, 선이 얇아 행 전체가 짚여 보이지 않는다는 지적으로 2026-07-25 표준형으로 통일.) **"엑셀 형식" 시트에는 이 커서 위치 표시를 기본으로 넣는다.** → **붙여 쓰는 CSS와 함정 3가지는 §9-3.**

### 9-2. 주류 재고 시트 칸 색 (의미별 배경 분리)

세 종류의 숫자는 **배경색으로 구분**한다 — 이게 이 시트의 핵심.

| 칸 | 헤더 | 본문 | 글자 |
| --- | --- | --- | --- |
| **입고(입)** | `bg-blue-100` → 파랑 | `bg-blue-50` → 파랑 | `text-blue-800` |
| **판매(판)** | `bg-rose-100` → 분홍 | `bg-rose-50` → 분홍 | `text-rose-800` |
| **재고(재)·월초** | `liquor-stock-alice-green` | 〃 | `text-slate-800` |

- **주의(값 불일치 바로잡음):** `liquor-stock-alice-green` 클래스는 이름은 "alice"지만 실제로는 **`--branch-honey` (#E0EBDC, 초록기 — 팔레트 개정 2026-08-04)**로 렌더된다. 재고/월초 칸은 파랑(alice)이 아니라 **허니(초록기)**다. 클래스명이 오해를 부르니, 이 색을 다룰 땐 토큰 실제값으로 기억한다.
- 시트 헤더는 일반 표(alice)와 달리 **네이비 `#202A5A` + 흰 글자**를 쓴다(치환 안 됨). 헤더 칸 구분선은 `border-white/20`.
- **오늘 날짜 열 강조:** 헤더는 `bg-rose-600`, 열 전체는 위·아래 `border-rose-500`(2px)로 띠를 두른다. 오늘 아닌 열은 `border-black/10`.
- **음수 재고 강조:** 재고 칸에서 `stock < 0`이면 허니 대신 `bg-rose-100 text-rose-700`.
- **커서가 있는 행 강조(focus-within):** 배경을 갈아엎지 않고, 행 위·아래에 검정 선(`inset 0 ±1px --branch-black`) + 바닐라 35% 얇은 막을 얹어 **띠처럼** 보이게 한다. 입고/판매/재고 칸의 배경 구분은 그대로 살린다. 좌측 고정열은 `.sticky` 규칙이 box-shadow를 `!important`로 못 박아서, 그 그림자(`0 10px 28px`)를 다시 적어 줘야 왼쪽 절반 선이 안 끊긴다.

### 9-4. [강제] 숫자는 **천 단위 쉼표**로 적는다 — ERP 전체 공통

**ERP에서 화면에 보이는 모든 숫자는 세 자리마다 쉼표를 넣는다**(사용자 지시 2026-07-31). 지점·관리자 화면을 가리지 않는다.

- **왜:** 쉼표가 없으면 `1250000`과 `125000`이 한눈에 구분되지 않는다. 이 앱의 숫자는 대부분 돈이고, 자릿수를 잘못 읽으면 그대로 이체 금액이 틀린다.
- **읽기 전용 값**(합계·계산 결과·표 셀): `formatNumber(...)`로 찍는다. 직접 `String(n)`이나 `{n}`으로 찍지 않는다.
- **입력 칸**(금액을 사람이 치는 자리): 보여줄 땐 `formatWithCommas(...)`, 저장할 땐 `cleanNumeric(...)`으로 쉼표를 떼고 숫자만 남긴다.
  - `type="number"`로는 쉼표를 넣을 수 없다. **`type="text"`**로 둔다.
  - **[강제] 방향키/Tab으로 오가는 엑셀 시트 셀에는 `inputMode="numeric"`을 넣지 않는다 — §9-1의 IME 규칙이 우선한다.** 숫자 자판을 띄우려다 윈도우 IME가 영문으로 전환되고 그 상태가 전역에 남아, 다음 한글 칸(성명·비고)에 영문이 찍힌다. 쉼표를 넣는 것과 `inputMode`는 별개다 — 쉼표만 넣고 `inputMode`는 생략한다.
  - 모바일 카드형 입력(시트가 아닌 화면)에서는 `inputMode="numeric"`을 써도 된다(§9-1 단서 그대로).
  - 기준 예시: 파트타이머 급여대장의 `시급`·`팁/기타` 칸(`MonthlyPartTimeSalarySubTab.tsx`).
- **쉼표를 넣지 않는 숫자:** 연도(`2026`), 주민등록번호·계좌번호·전화번호 같은 **식별 번호**, 날짜의 일(`5`), 백분율의 소수부. 이건 자릿수를 세는 값이 아니라 하나의 표기이므로 쉼표가 오히려 방해가 된다.
- **엑셀로 내보낼 때는 쉼표를 붙이지 않는다.** 셀에는 `Number(...)`로 숫자 자체를 넣고, 자릿수 표시는 엑셀 서식이 맡는다. 문자열로 넣으면 합계·정렬이 깨진다.

### 9-5. [강제] 숫자 칸에 단위(`원`·`시간`)를 붙이지 않는다 — 표 안에서는

표 안의 숫자 칸에는 **숫자만** 적는다. 단위는 **컬럼 헤더**에 적는다(`시급 (원)`처럼). 칸마다 `원`을 붙이면 자릿수 정렬이 흐트러지고, 그 칸을 복사해 붙일 때 숫자로 취급되지 않는다.
합계·요약처럼 표 밖에서 한 번만 보여주는 자리에는 단위를 붙여도 된다(`급여합계(세전) 1,250,000 원`).

### 9-3. 커서 행 표시(행 하이라이트) — 붙여 쓰는 레시피

**엑셀형 시트를 새로 만들거나 기존 표를 엑셀화할 때 이 블록을 반드시 함께 넣는다.** 값을 여러 칸에 적다 보면 지금 몇 번째 줄인지 놓치는데, 셀 하나만 파란 테두리로 짚어서는 **어느 행인지 알 수 없다**(발주내역 리포트에서 실제로 나온 불편, 2026-07-21).

적용된 시트: 주류 재고(§9-2) · 발주내역 리포트 · 정직원 급여대장(`#fulltime-salary-subtab`) · 근무자 표(`staffSheet.css`, 표준 막+선 — 2026-07-25 선만 쓰는 변형에서 통일).

> 아직 안 넣은 엑셀형 시트: 매입매출(`#purchase-sales-subtab`) · 파트타이머 급여대장(`#parttime-salaries-subtab`). 둘 다 셀 외곽선만 있어 같은 불편이 남아 있다 — 손댈 일이 생기면 이 블록을 함께 넣는다.

`#<섹션id>`만 그 시트를 감싸는 요소의 id로 바꿔 index.css에 넣는다.

```css
/* 커서(포커스)가 있는 행에 띠를 두른다 — 바닐라 35% 얇은 막 + 위·아래 검정 선.
   배경을 갈아엎지 않으므로 칸마다 다른 배경색(입고/판매/재고, 합계 칸 등)이 그대로 남는다. */
.branch-redesign #<섹션id> tbody tr:focus-within > td {
  box-shadow:
    inset 0 1px 0 0 var(--branch-black),
    inset 0 -1px 0 0 var(--branch-black),
    inset 0 0 0 9999px color-mix(in srgb, var(--branch-vanilla) 35%, transparent);
}

/* 고정열(sticky)은 따로 이겨야 한다 — 전역 `.branch-redesign .sticky`가 box-shadow를
   !important로 못 박아 두었다. 그 그림자(0 10px 28px)를 여기 다시 적어야 고정 칸이 떠 보이는
   느낌을 잃지 않고, 위·아래 선도 다시 그어야 왼쪽 절반에서 선이 끊기지 않는다.
   맨 앞 칸의 왼쪽 4px 띠는 행이 어디서 시작하는지 짚어 준다
   (고정열이 여러 개면 이 줄만 `> td.sticky:first-child` 규칙으로 떼어낸다 — 주류재고가 그 예). */
.branch-redesign #<섹션id> tbody tr:focus-within > td.sticky {
  box-shadow:
    inset 4px 0 0 0 var(--branch-black),
    inset 0 1px 0 0 var(--branch-black),
    inset 0 -1px 0 0 var(--branch-black),
    inset 0 0 0 9999px color-mix(in srgb, var(--branch-vanilla) 35%, transparent),
    0 10px 28px rgba(33, 33, 33, 0.035) !important;
}
```

**함정 1 — 입력칸이 불투명하면 띠가 칸 안에서 안 보인다(가장 흔한 실패).** 전역 `.branch-redesign input`이 배경(ghost)과 **18px 둥근 모서리**를 `!important`로 강제하므로, 그대로 두면 칸마다 흰 알약이 떠 있고 띠는 칸 사이 실선으로만 비친다. **띠를 넣기 전에 §9-1의 입력칸 투명화를 먼저 배선한다:**

```css
.branch-redesign #<섹션id> tbody input {
  background: transparent !important;
  border: 0 !important;
  border-radius: 0 !important;
  box-shadow: none !important;
}
```

선택자는 **`tbody`로 좁힌다** — `thead`의 머리글 수정 입력칸(거래처명 등)까지 투명해지면 안 된다.

**함정 2 — 고정 칸(sticky)에는 TSX 배경 클래스로 커서 표시를 못 그린다.** `.branch-redesign .sticky`와 `html body .branch-redesign.branch-redesign .sticky`가 background를 `!important`로 덮으므로, 컴포넌트에서 `rowActive ? "bg-blue-50" : "bg-white"` 식으로 짜면 **한 줄도 화면에 나오지 않는다**(발주내역 리포트에 그렇게 들어가 있었고, 오랫동안 죽은 코드였다 — 2026-07-21 제거). **행 띠는 반드시 위 CSS로**, 컴포넌트에는 글자색 정도만 남긴다.

**함정 3 — `border-collapse: collapse` 표여도 띠는 된다.** Chromium은 collapse에서도 `<td>`의 inset box-shadow를 정상적으로 그린다(2026-07-21 실측 확인). **띠를 넣겠다고 이미 돌아가는 표를 `border-separate`로 바꾸지 말 것** — 테두리를 전부 다시 배선해야 해 위험만 크다. 단 **새로 만드는 표는 §9-1대로 `border-separate; border-spacing:0`**으로 시작한다(sticky 헤더/고정열 테두리 때문).

**막을 씌울까, 선만 그을까:** 상태색이 있어도 **막+선을 그대로 쓴다.** 막은 반투명(35%)이라 상태색이 비친다 — 검증오류 셀 빨강(`bg-[#FDE2E2]`)은 65%가 남아 복숭아빛으로 여전히 구분되고(실측 합성 ≈`#F8E7CC`), 근무 완료 행(바닐라 35%)은 조금 더 진해질 뿐이다(≈`#F6F6CA`). 근무자 표도 종전 '선만 쓰는 변형'에서 2026-07-25 표준 막+선으로 통일했다 — 선만 쓰면 행 전체가 짚여 보이지 않는다.

---

## 10. 입력 · 선택 · 버튼

**입력/선택(input·select·textarea) — 최종 렌더:**
- 배경: ghost에 흰색을 살짝 섞은 값, 테두리 `rgba(33,33,33,0.14)`, **모서리 `border-radius: 18px`**.
- 포커스: 테두리 `rgba(33,33,33,0.34)` + `box-shadow: 0 0 0 3px rgba(216,223,233,0.75)`(연한 alice 링).
- 표준 크기: 컴팩트 `h-8`(32px), 일반 `h-9`/`h-[38px]`. 폭은 내용에 맞춰 `w-[…]`.
- 탭 작성 예: `h-8 w-[124px] rounded-lg border border-gray-200 px-2 text-[11px] font-bold`.

**버튼:**
- **모든 강조/컬러 버튼은 검정 배경 + ghost 글자로 렌더된다**(`bg-[#2E6DB4]`·`bg-emerald-*`·`bg-rose-*`·`bg-slate-700`·`bg-zinc-900` 전부 → 검정). 색으로 의미를 나누지 말 것.
- 모서리 언어: `rounded-lg`(작은/표 버튼) → `rounded-xl`(중간) → `rounded-2xl`(주요 제출) → `rounded-full`(알약 토글).
- 아이콘이 도움이 되면 함께 쓰고, 삭제/저장 같은 행동엔 `aria-label`/`title`을 둔다. (제목 칩과 달리 버튼은 아이콘 허용.)
- 비활성: `disabled:opacity-40 disabled:cursor-not-allowed` 또는 `disabled:bg-gray-100`(→ alice).
- 가이드 토글 같은 알약 버튼: 켜짐 `bg-zinc-900 text-[#F4F2CC]`, 꺼짐 `bg-[#F4F2CC] text-zinc-900`(검정↔바닐라 반전).

---

## 11. 상태색 (의미가 있을 때만)

**자동저장 배지**(발주·주류 헤더 우측, `h-8`/`h-[38px]` `rounded-lg/xl` `font-black`):
- 불러오는 중 / 저장 중: `bg-amber-50 text-amber-700` (→ 바닐라)
- 저장됨: `bg-emerald-50 text-emerald-700` (→ 허니)
- **동기화 실패: `bg-rose-50 text-rose-700` — 반드시 눈에 띄게.** 저장 실패를 초록 "저장됨"으로 속이지 않는다(AGENTS.md P0-2).

**검증 오류**(제출 막는 필수 미입력): 해당 입력칸/셀에만 `branch-validation-error` → **테두리 `#C93A3A`, 배경 `#FDE2E2`, 링 `rgba(201,58,58,0.18)`**. 전체 섹션을 칠하지 않는다. 사용자가 값을 고치면 즉시 해제한다.

**현금마감 차이:** 일치 → `cash-flow-diff-ok`(허니 배경). 불일치 → `cash-flow-diff-bad`(배경 `#fee2e2`, 테두리 `#dc2626`, 글자 `#b91c1c`).

**월말마감 상태 칩**(테두리 1px 검정 공통): 제출완료 = `#EFF0A3`, 수정중 = `#D8DFE9`, 미제출 = 검정 `#18181B` + 흰 글자. **팔레트 개정(2026-08-04)에서 의도적으로 제외 — 옛 진한 값을 hex 로 유지한다**(연한 바탕 위 상태 강조, DESIGN_ADMIN §2-1과 같은 원칙).

**추가 사유 미선택**: 행 전체를 칠하지 않고, `추가 사유` 컨트롤/칩만 붉은 계열(`#FDE2E2`/`#C93A3A`)로 표시.

---

## 12. 전용 클래스로 못 박아야 하는 색들

아래 색들은 Tailwind 표준 색을 쓰면 2번 자동치환에 먹혀 사라지거나 뭉개진다. **전용 클래스로 index.css에 못 박는다.**

- **발주 대분류 칩:** `order-cat` + `order-cat-food`(바닐라)/`order-cat-side`(알리스)/`order-cat-liquor`(허니)/`order-cat-etc`(흰색). 칩이 촘촘한 자리에서는 `order-cat-soft`로 테두리만 연하게.
- **결제수단 칩:** `pay-chip` + `pay-chip-cash`(바닐라)/`pay-chip-card`(허니).
- **월말 지출 칩:** `monthly-expense-chip` + `monthly-chip-vanilla`/`monthly-chip-alice`/`monthly-chip-honey`.
- **매입매출 분류 행:** `tr[data-cat="…"]`(8번 ID 특이성 규칙).
- **조회표 행 구분선:** `sheet-rows-soft > tr`은 `rgba(33,33,33,0.07)`로 연하게, 날짜 바뀌는 행만 `row-group-start`로 `rgba(33,33,33,0.22)`. (그대로 두면 `divide-gray-*`가 검정으로 치환돼 행마다 굵은 검은 줄이 그어진다.)
- **합계/계산식 박스:** `daily-total-chip`, `cash-flow-box`는 배경을 **투명**하게 둔다. 카드 배경색을 값으로 복제하지 말 것 — 투명하면 카드 배경이 그대로 비쳐 언제나 정확히 같은 색이다.

---

## 13. 테두리 함정 — "왼쪽 위에 연회색 선이 남는" 문제와 그 방지 규칙

검정 섹션 테두리를 줬는데 **한쪽 모서리(특히 좌상단)에 연회색 1px 선이 살아남는** 일이 반복해서 있었다. 원인과 규칙:

1. **테두리는 색만 바꾸지 말고 통째로 지정한다.** CSS에서 `border-color`만 검정으로 바꾸면, 컴포넌트가 가진 `border-gray-*`의 폭/방향별 잔값이 한쪽에 남아 모서리에 연회색이 비친다. 항상 `border: 1px solid var(--branch-black)`처럼 **폭+스타일+색을 함께** 지정한다.

2. **카드 테두리는 반드시 `border-gray-100`(또는 `gray-200`)으로 작성한다.** `border-slate-200`·`border-zinc-200` 등으로 테두리를 주면 검정 치환 catch-all에서 빠져 **그 카드만 연회색으로 남는다.** (catch-all은 `bg-white`+`rounded-2xl/3xl`, 또는 `rounded-2xl`+`border-gray-100`을 검정으로 만든다.)

3. **탭 id별로 테두리를 따로 지정하지 않는다.** 예전에 `#parttime-salaries-subtab` 식으로 탭마다 개별 지정했더니 목록에서 빠진 탭은 테두리가 없었다. **후손 선택자 catch-all 하나로** 지점 화면의 흰 카드 전부에 같은 테두리를 준다.

4. **자식(`>`) 선택자 대신 후손 선택자를 쓴다.** 섹션에 조작법 칩을 얹으려 `relative` 래퍼로 한 겹 감싸는 순간, `> section`은 손자가 되어 규칙에서 빠지고 그 카드만 테두리가 사라진다. `#orders-tab-view section`처럼 깊이와 무관한 후손 선택자로 둔다.

5. **테두리 있는 카드 안에 테두리 있는 카드를 모서리에 붙이지 않는다.** 겹치면 안쪽의 연회색(또는 얇은 선)이 바깥 검정 모서리에 비친다. 겹겹이 필요하면 **바깥 래퍼는 `border:0; box-shadow:none`인 투명 그룹**으로 두고 안쪽 카드만 테두리를 갖게 한다(일일마감정산 `#settlement-finance-section`이 이 방식).

6. **그래도 한 모서리에 잔선이 남으면 outline으로 사각형을 한 번 더 덮는다:** `outline: 1px solid var(--branch-black); outline-offset: -1px;`. (관리자 디자인 프리뷰에서 검증된 방법 — border만으로 모서리가 안 잡힐 때 확실히 닫는다.)

7. **`!important`를 이겨야 하면 특이성으로 이긴다.** 지점 화면의 색·테두리 규칙 상당수가 `!important`다. 같은 `!important`로는 순서 싸움이 되니, **ID를 낀 선택자로 특이성을 높여** 이긴다(줄무늬·시트셀·분류행 모두 이 방식).

---

## 14. 사이드바(껍데기) — 셀러닷컴형 (지점 + 관리자 공통)

지점 화면과 관리자 화면의 사이드바는 **같은 형태**를 쓴다. 외부 레퍼런스(셀러닷컴)의 밝은 배경 + 섹션 그룹 + 촘촘한 목록을 **거의 그대로** 따르되, **활성(현재 탭) 표시색만 ERP 바닐라**로 바꾼다. 공통 클래스: `.ugd-nav-group`(그룹 헤더), `.ugd-nav-item`(메뉴 항목).

> **[구 규칙 폐기]** 예전의 "메뉴 버튼 큰 알약(58px)·**활성=검정 배경**·하위탭 바닐라·`branch-main-nav-button`/`branch-sidebar-subtab`"은 **폐기**했다. index.css의 옛 사이드바 규칙 복사본(3벌)은 제거했다.

**껍데기**
- 배경 ghost(밝음), 폭 **236px**, 지점은 우측 경계 `1px --branch-alice`.
- `nav` 컨테이너: `padding: 6px 0 16px; gap: 0` — 좌우 패딩 0이라 항목이 **좌우 끝까지 닿는 풀폭**.

**브랜드 블록(상단)**
- 이름(`branch-sidebar-branch-name`): **15px / 900**, 말줄임.
- 부제(`branch-sidebar-brand-sub`): **10.5px / 500**, `rgba(33,33,33,0.4)`.

**그룹 헤더 (`.ugd-nav-group`) — 검정 볼드 라벨(채우지 않음)**
- **검정 텍스트만(볼드)**: `11px / 800`, 자간 0.33px, **`#212121`**(사용자 지정 2026-07-20 — 대분류를 또렷하게), `padding: 14px 20px 6px`, **배경 없음**. (지점 `--branch-black`·관리자 `--admin-black` 둘 다 #212121이지만, 지점+관리자 공용 셀렉터라 단일 토큰을 못 써 리터럴 `#212121`로 둔다.)
- 지점: `메인 / 일일 업무 / 월말 업무 / 인사·기타`. 관리자: `메인 / 일일업무 / 월말업무 / 인사 / 이동`.
- 모바일(≤767px)에서는 숨김.

**메뉴 항목 (`.ugd-nav-item`) — 텍스트만(아이콘 없음)**
- **텍스트만, 12px / 500**, **자간 0**(레포 제약: 음수 자간 금지), `line-height: 1.45`.
- **들여쓰기 `padding: 9px 20px 9px 34px`** — 왼쪽 34px라 그룹 헤더(20px)보다 안쪽. 하위 항목이 확실히 구분된다. 세로 9px라 행 높이 ≈35px(레퍼런스와 동일). 풀폭 사각(모서리 0), `min-height: 0`, `gap: 0`.
- 평상시 글자 **`rgb(91,96,109)`**(레퍼런스 실측 그대로), hover `rgba(33,33,33,0.04)` 배경.
- **활성(현재 탭)만 색을 준다 = 바닐라**: 배경 **`#F4F2CC`(--branch-vanilla, 팔레트 개정 2026-08-04)** + 글자 `--branch-black` / **600**, 모서리 0. 나머지 항목은 배경 없음.

**관리자 세부 항목 — 중분류 없이 평탄화 (2026-07-21)**
- 예전엔 `일일 정산현황`·`월말마감` **중분류 버튼을 눌러야** 하위탭이 펼쳐졌다. 이 중분류와 전용 하위탭 스타일(`.ugd-subnav-item`, `ml-4 pl-3 border-l` 들여쓰기)은 **폐기**했다.
- 이제 세부 항목(전일 정산현황·마감 이력 점검 / 제출현황·현금관리·현금지출)을 각 그룹(`일일업무`·`월말업무`) 아래 **`.ugd-nav-item`으로 바로 노출**한다. 클릭하면 해당 `adminSection`과 세부 탭을 함께 설정하고, 활성 표시는 다른 메뉴 항목과 동일한 바닐라(`#F4F2CC`)다.

**관리자 `마감 이력 점검` 탭 — 하위탭도 평탄화 (2026-07-21)**
- 예전엔 이 탭 안에 하위탭 3개(정산 변경이력 로그 / 지점 수기 초과근무 대장 / 현금차이 이력)가 또 있었다. **하위탭을 없애고 세 섹션을 세로로 쌓아** 한 화면에서 바로 본다.
- **순서는 `현금차이 이력 → 수기 초과근무 대장 → 정산 변경이력`** — 금액 이상 → 수기 입력 → 사후 수정 순으로, 급한 것부터 위에 둔다(2026-07-21 사용자 지정).
- 각 섹션은 **카드 1장**이다 — 카드는 `.admin-sheet-card`, 제목 줄은 `.admin-band`를 쓴다(`DESIGN_ADMIN.md` §4-0, 2026-08-03 개정). **카드에 `padding`을 주지 않는다** — 밴드가 폭을 꽉 채워야 하므로 안쪽 내용이 각자 여백을 갖는다. 필터는 제목 바로 옆(`.admin-band-filters`), 실행 버튼은 오른쪽 끝(`.admin-band-actions`).
- **세 섹션의 필터는 `[지점명 드롭다운] [월 선택] [새로고침]`으로 통일**한다(공통 컴포넌트 `ModlogFilters`). 지점 목록은 별도 조회 없이 그 표의 `branchName`에서 뽑되, 현재 선택값은 목록에서 사라져도 남긴다(select가 빈 칸이 되지 않게). 월은 비우면 전체 기간이나, **현금차이만 필수** — 거기 월은 화면 필터가 아니라 전 지점 서버 조회의 조건이다.
- 표는 **카드 안에서 자체 스크롤**(`.admin-sheet-scroll max-h-[320px]`)하고 헤더는 고정된다 — 고정·배경·격자는 `.admin-sheet`가 맡으므로 화면에서 `sticky`를 다시 적지 않는다(§9-1-B). 섹션을 쌓아도 세 제목이 한 화면에 들어오게 하기 위함이다.
- 행 여백은 §8 기준값(`thead py-2 px-2` / `tbody py-1.5 px-2`)을 지킨다. 확인·삭제 버튼은 `h-6`으로 납작하게 둬 행 높이를 키우지 않는다.
- **최근 건 강조는 시간 기준이고, 행 배경이 아니라 알약으로 짚는다**(`.admin-recent-badge`, `DESIGN_ADMIN.md` §4-4 — 2026-08-03 개정). 최근 3일(오늘·어제·그저께) 안에 올라온 건에 날짜 옆으로 알약을 붙인다 — 변경이력은 `modifiedAt`, 수기 초과근무는 `createdAt`, 현금차이는 등록 시각이 없어 `마감일자` 기준. **행 배경을 칠하던 방식(`admin-log-recent`)은 없앴다** — 최근 건이 많은 날 표의 절반이 물들어 오히려 어디를 봐야 할지 흐려졌다. 같은 칸의 날짜 글자는 볼드를 빼 알약만 도드라지게 둔다.
- **행별 "확인" 버튼은 두지 않는다(2026-07-21 제거).** 예전엔 관리자가 행마다 확인을 눌러 강조를 껐는데, 그 확인 목록을 통째로 덮어쓰는 구조라 **다른 관리자가 눌러 둔 확인이 지워졌다**(Codex 리뷰 지적). 강조를 시간 기준으로 바꿔 그 저장 경로 자체를 없앴다. 대시보드 알림은 어제분만 세고 localStorage로 닫히므로 영향받지 않는다.

**폰트 크기**: 항목 **12px**(사용자 지정) · 그룹헤더 11px · 브랜드 15px · 부제 10.5px. (레퍼런스 항목은 13px이나 사용자 요청으로 12px로 낮췄다.)

---

## 15. 새 화면/수정 체크리스트

- [ ] 색은 토큰 6개 안에서만? 새 색을 만들지 않았나?
- [ ] 섹션 카드 = `bg-white p-4 rounded-2xl border border-gray-100 shadow-sm` (border는 gray 계열)?
- [ ] 섹션 제목 = `<h3 ... w-fit>텍스트</h3>`, **아이콘 없이 텍스트만**? `<div>`로 감싸지 않았나?
- [ ] 섹션 간격 20~24px, 내부 `p-4`·`space-y-3/4`?
- [ ] 표 헤더 alice, 셀 `py-1.5 px-2`, 숫자 `font-mono`? 엑셀형이면 `border-separate`?
- [ ] 버튼이 색으로 의미를 나누려 하지 않나(어차피 검정으로 렌더)?
- [ ] 저장 실패를 눈에 띄게 표시하나(초록으로 속이지 않기)?
- [ ] 검정 테두리에 연회색 잔선이 안 남게 13번 규칙을 지켰나?
- [ ] 특정 색을 유지해야 하면 전용 클래스로 못 박았나(Tailwind 표준색은 치환됨)?
- [ ] 코드를 고쳤으면 Codex 지독한 리뷰 → 로컬(`npm run dev`, :3000) 확인 → 사용자 "배포해" 대기(AGENTS.md P0-1·P1)?
