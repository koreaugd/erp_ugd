# UGD ERP 디자인 기준

## 1. 디자인 방향

UGD ERP는 지점 운영자가 매일 반복해서 쓰는 업무 화면입니다. 마케팅 페이지처럼 보이기보다, 좁은 시간 안에 입력하고 확인하기 쉬운 운영 콘솔이어야 합니다. 화면의 인상은 부드러운 배경색, 선명한 검은 섹션 테두리, 작고 밀도 있는 표, 명확한 상태색으로 정리합니다.

## 2. 색상 토큰

| 이름 | 값 | 용도 |
| --- | --- | --- |
| `--branch-ghost` | `#F6F5FA` | 지점 화면 전체 배경, 큰 섹션 바탕 |
| `--branch-alice` | `#D8DFE9` | 사이드바, 부드러운 패널, 주류 재고 강조 |
| `--branch-honey` | `#CFDECA` | 완료, 확정, 긍정 상태 |
| `--branch-vanilla` | `#EFF0A3` | 공지, 미확인, 주의 상태 |
| `--branch-black` | `#212121` | 본문 텍스트, 활성 버튼, 섹션 외곽선 |
| `#C93A3A` / `#FDE2E2` | 경고 레드 | 추가 사유 미선택, 필수 확인 필요 |
| `#2E6DB4` | 기존 액션 블루 | 저장, 등록, 포커스, 기존 주요 액션 |

색상은 의미가 있을 때만 사용합니다. 예를 들어 주류 재고의 현재고/재고 칸은 `--branch-alice` 계열로 구분하고, 미확인 또는 알림성 항목은 `--branch-vanilla`를 사용합니다.

## 3. 레이아웃

- 데스크톱은 왼쪽 고정 사이드바와 오른쪽 작업 영역으로 구성합니다.
- 사이드바 상단에는 지점명만 크게 노출합니다.
- 지점 업무 탭의 주요 섹션은 검은 1px 테두리와 낮은 그림자로 구분합니다.
- 업무 표는 가능한 한 한 화면 안에서 읽히게 만들고, 불필요한 가로 스크롤을 만들지 않습니다.
- 카드 안에 다시 장식용 카드를 겹겹이 넣지 않습니다. 단, 실제 입력 그룹이나 반복 목록은 예외입니다.

## 4. 타이포그래피

| 영역 | 크기 | 굵기 | 기준 |
| --- | --- | --- | --- |
| 화면 제목 | 24-32px | 900 | 대시보드 및 주요 탭 제목 |
| 섹션 제목 | 14-18px | 900 | 카드 헤더 |
| 일반 라벨 | 12-14px | 700-900 | 입력 필드, 상태 라벨 |
| 표 텍스트 | 10-12px | 600-900 | 직원현황, 재고, 정산 표 |
| 숫자/날짜 | 10-12px | 600-900 | `font-mono` 사용 |

표 안에서는 긴 텍스트보다 스캔성이 우선입니다. 이름, 사유, 상품명처럼 길어질 수 있는 값은 말줄임과 `title`을 함께 사용합니다.

## 5. 주요 컴포넌트

### 사이드바

- 너비는 지점명과 메뉴가 안정적으로 보이는 수준으로 유지합니다.
- 활성 메뉴는 `--branch-black` 배경과 밝은 글자로 표시합니다.
- 아이콘과 텍스트를 함께 사용해 빠르게 구분합니다.

### 섹션 카드

- 주요 업무 섹션은 `rounded-2xl` 이상 둥근 모서리, 검은 1px 테두리, 낮은 그림자를 사용합니다.
- 일일마감정산뿐 아니라 발주, 주류재고, 직원현황, 월말 관련 탭도 같은 섹션 외곽선 규칙을 따릅니다.
- 섹션 내부의 작은 입력칸은 연한 회색 테두리를 유지해 큰 섹션 테두리와 구분합니다.

### 직원현황 표

- 기본 컬럼은 `근무자번호`, `성명`, `주민등록번호 앞6자리`, `분류`, `추가 사유`, `입사일자/지점이동 날짜`, `핸드폰번호/급여변동`, `활동`입니다.
- `추가 사유`와 `급여변동` 선택 컨트롤의 기본값은 `선택`입니다.
- `추가 사유`는 `신규입사`, `지점이동`, `기존직원`, `기타` 중 선택합니다.
- `신규입사` 선택 시 입사일자와 010 제외 핸드폰번호 8자리를 입력합니다.
- `지점이동` 선택 시 지점이동 날짜와 급여변동 있음/없음을 선택합니다.
- 직원현황 신규등록은 `이름`, `주민등록번호 앞6자리`, `분류`, `추가 사유`를 먼저 받고, 조건부 상세값은 같은 행 안에서 보완합니다.
- 일일마감정산 근무자 추가는 빠른 입력을 위해 `이름`, `분류`, `추가 사유`만 받으며, 제출 시 직원현황 자동 등록 데이터에 반영합니다.
- 직원현황 기존 행은 평상시 읽기 전용으로 보이고, 연필 수정 버튼을 누른 행만 입력 컨트롤로 전환합니다. 저장 전에는 원본 데이터를 바꾸지 않으며, 취소 버튼으로 수정 전 값으로 되돌릴 수 있어야 합니다.
- 저장 후 지점 화면에서는 신규입사 핸드폰번호와 지점이동 급여변동 값을 다시 노출하지 않습니다. 수정 모드에서도 기존 값은 비워 보이고, 새 값을 입력한 경우에만 본사 확인용 데이터가 갱신됩니다.
- 추가 사유가 `선택` 상태로 남아 있는 경우 행 전체를 칠하지 않고, `추가 사유` 선택 컨트롤 또는 표시칩만 붉은 계열 배경과 테두리로 표시합니다.

### 관리자 근로계약 후보

- 관리자 `근로계약서 발송 현황`에는 `지점 등록 후보`와 `발송 내역` 탭을 둡니다.
- `지점 등록 후보`는 지점 직원현황에서 `추가 사유`가 `신규입사`, `지점이동`, `기타`인 인원만 모아 보여주고 `기존직원`은 제외합니다.
- 후보 상태는 필수값이 부족한 경우 `정보부족`, 기본 정보가 채워진 경우 `발송대기`로 표시합니다.
- 후보 목록은 지점 직원현황 저장소(`branch_own_rosters`)를 기준으로 읽습니다. 지점에서 핸드폰번호 또는 급여변동 값을 수정 저장하면 같은 저장소가 갱신되어 관리자 후보 탭 새로고침 시 변경값이 반영됩니다.

### 주류 재고 표

- 현재고와 날짜별 재고 칸은 입고 칸과 혼동되지 않도록 `--branch-alice` 계열 배경을 사용합니다.
- 입고, 판매, 재고처럼 의미가 다른 숫자는 배경색을 분리합니다.

## 6. 상호작용

- 입력값은 기존 자동 저장 흐름을 유지합니다.
- 일일마감정산 제출 검증 오류는 전체 섹션이 아니라 수정이 필요한 입력칸/행에만 `#C93A3A` 계열 테두리와 `#FDE2E2` 계열 배경을 적용합니다.
- 제출 실패 시 수정해야 할 항목을 한 번에 표시하고 첫 오류 위치로 이동하되, 사용자가 값을 고치면 해당 오류 표시는 즉시 해제합니다.
- 대시보드 `미결 확인사항`은 `기존직원`에게 입사일/추가 상세값을 강제하지 않습니다. `추가 사유`가 선택되지 않았거나, `신규입사`·`지점이동`·`기타`의 본사 확인용 필수값이 부족한 경우만 직원현황 미결로 표시합니다.
- 버튼은 아이콘이 적합한 경우 아이콘을 함께 사용하고, 삭제/저장 같은 행동은 `aria-label` 또는 `title`을 둡니다.
- 날짜 입력은 클릭 시 브라우저 date picker가 열리게 유지합니다.
- 표 편집은 행 높이가 크게 흔들리지 않도록 작은 입력 컨트롤을 사용합니다.

## 7. 반응형 기준

- 데스크톱에서는 표가 가로 스크롤 없이 읽히는 것을 우선합니다.
- 모바일에서는 사이드바가 상단/접힘 구조로 전환되더라도 주요 버튼과 입력값이 겹치지 않아야 합니다.
- 고정 폭 컬럼을 과도하게 키우지 않고, 데이터 길이에 맞춰 `table-fixed`, 말줄임, 짧은 라벨을 조합합니다.
