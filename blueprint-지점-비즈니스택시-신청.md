# 지점 비즈니스택시 신청·관리 워크플로우 설계서

> 작성일: 2026-07-24
> 목적: Claude Code 구현 참조용 계획서 (ERP `erp_ugd` 기능 확장 — 법인택시 1차분의 후속)

---

## 1. 작업 컨텍스트

### 배경 및 목적

법인택시 1차분(2026-07-24)으로 관리자는 카카오T 이용내역 조회·이상 점검·직원 등록/수정/휴직/삭제를 ERP에서 처리할 수 있게 됐다. 그러나 실제 현장에서 "우리 지점 직원을 법인택시에 등록해달라"는 요청은 지점에서 발생하며, 지금은 카톡·전화로 본사에 전달되어 누락·지연이 생긴다.

목표: **지점 화면(인사·기타)에 "비즈니스택시" 탭을 신설**하여 ① 지점이 직원 이용신청(등록 신청)을 올리고 ② 자기 지점에 등록된 인원을 확인하며 ③ 지점 소속이 아니게 된 인원의 수정/삭제를 사유와 함께 요청하면 → **관리자가 법인택시 > 신청 관리에서 승인/반려**하고, 승인 시 카카오 API가 실제 실행되는 반자동 파이프라인을 완성한다. 근로계약서 탭(지점 등록 → 관리자 처리)과 같은 검증된 패턴을 따른다.

### 범위

- **포함**
  - 지점: 비즈니스택시 탭 (이용신청 폼 · 우리 지점 등록 인원 목록 · 인원별 수정/삭제 요청 버튼(사유 필수) · 내 신청 현황(대기/승인/반려+사유))
  - 관리자: 법인택시 > 신청 관리 탭 (전 지점 신청 목록, 승인/반려(사유 필수), 승인 시 카카오 API 자동 실행)
  - 백엔드: 지점 PIN으로 자기 지점 등록 인원만 조회하는 액션 (GAS + server.ts 동일)
  - 신청 데이터 저장: Firestore 공유데이터 `kakao_taxi_requests:<지점>` (근로계약서와 같은 저장 계층)
- **제외**
  - 지점이 카카오 API를 직접 호출하는 것 — 쓰기 실행은 관리자 승인 후에만 (기존 관리자 게이트 유지)
  - 별도 "승인번호" 발급 — 카카오 구조상 존재하지 않음. 승인=카카오 등록이고, 카카오가 직원 휴대폰으로 **인증 알림톡**을 자동 발송(직원이 카카오T 앱에서 인증하면 이용 가능). 지점은 신청 현황에서 승인/반려를 확인
  - 택시 호출·그룹 생성 (카카오 API 미제공, 1차분 설계서와 동일)

### 입출력 정의

| 항목 | 내용 |
|------|------|
| **입력** | 지점 신청 폼: 이름·휴대전화(필수), 메모(선택). 수정/삭제 요청: 대상 인원(카카오 member id)·사유(필수). 관리자 처리: 승인(등록 신청이면 그룹 선택 포함)/반려(사유 필수) |
| **출력** | ① 신청 레코드: `kakao_taxi_requests:<지점>` 배열 (상태 pending/approved/rejected/done). ② 승인 시 카카오 측 실행 결과(직원 등록/삭제/수정). ③ 지점 신청 현황 표시(반려 사유 포함) |
| **트리거** | 지점 신청 = 지점의 수동 등록. 관리자 처리 = 신청 관리 탭에서 수동 승인/반려. 카카오 실행 = 승인 클릭이 트리거(자동) |

### 제약조건

- **권한 분리**: 카카오 쓰기 액션은 기존 관리자 PIN 게이트 유지. 지점용 신규 액션(`getKakaoTaxiBranchMembers`)은 지점 PIN 검증 후 **그 지점으로 매핑되는 인원만** 반환(타 지점 직원 전화번호 노출 금지).
- **지점 매핑**: member_department → ERP 지점명 매핑(별칭 포함)을 백엔드에서도 수행해야 함 — 클라이언트 별칭표(`helpers/kakaoTaxi.ts`)와 값을 같게 유지(양쪽 주석으로 상호 참조).
- **동시 쓰기**: 신청 배열은 지점별 키로 분리해 충돌을 최소화하고, 모든 쓰기는 서버 fresh 재조회(`getSharedDataFromServer`) → id 대조 → 병합 저장. 이미 처리된 신청은 재처리 금지(멱등).
- **크로스디바이스 P0**: 저장은 전부 Firestore 공유데이터 — localStorage 단독 저장 금지.
- **UI 규격**: DESIGN.md §6-0-1 폰트 기준표(제목/헤더/버튼/입력 11px·셀 12px), 지점 화면은 `--branch-*` 토큰·h3 알약 제목, 관리자는 DESIGN_ADMIN 오버라이드.
- **운영 원칙**: 수정 직후 Codex 지독한 리뷰, `npm run dev` 로컬 확인, 사용자 "배포해" 전 커밋 금지.

### 용어 정의

| 용어 | 정의 |
|------|------|
| 이용신청(register) | 지점이 직원을 카카오T 비즈니스에 등록해달라고 올리는 신청 |
| 수정/삭제 요청(update/delete) | 등록된 인원에 대한 변경 요청. 사유 필수 |
| 신청 레코드 | `kakao_taxi_requests:<지점>` 배열의 항목. id·type·상태·사유·처리이력 보유 |
| 승인 | 관리자가 신청을 확정하는 행위 — 등록/삭제는 카카오 API 즉시 실행, 수정은 수정 카드로 이어짐 |
| 인증 알림톡 | 등록 승인 시 카카오가 직원 휴대폰으로 보내는 카카오T 앱 인증 요청(별도 승인번호 없음) |

---

## 2. 워크플로우 정의

### 전체 흐름도

```
[지점: 신청 폼/요청 버튼] → [Step 1: 신청 저장 (kakao_taxi_requests:<지점>)]
   → [Step 2: 관리자 신청 관리 — 전 지점 취합]
        ↓ 승인/반려 분기
   승인(register) → [Step 3a: 카카오 직원 등록 + 인증 알림톡] → [Step 4: 상태 approved 기록]
   승인(delete)   → [Step 3b: 카카오 직원 삭제]              → [Step 4]
   승인(update)   → [Step 3c: 관리자 수정 카드 열기 → 저장]   → [Step 4]
   반려          → [Step 4: 상태 rejected + 사유 기록]
   → [Step 5: 지점 신청 현황 표시 (승인/반려+사유)]

[지점: 등록 인원 확인] → [Step 0: getKakaoTaxiBranchMembers (지점 PIN 검증·자기 지점만)]
```

### LLM 판단 vs 코드 처리 구분

| LLM이 직접 수행 (구현 시점) | 스크립트/코드로 처리 (런타임) |
|----------------|----------------|
| 지점→그룹 자동 추천 규칙 설계(승인 폼 기본값) | 신청 저장·fresh 재조회·id 대조·멱등 처리 |
| 백엔드 지점 매핑 별칭표를 클라이언트와 동기화 | 지점 PIN 검증·부서→지점 매핑 필터 |
| 반려/승인 문구·안내 문구 작성 | 카카오 등록/삭제 API 실행·결과 검증 |
| 런타임 LLM 호출 없음 — 전부 결정론적 코드 | 상태 전이 기록(처리자·시각·사유) |

### 단계별 상세

#### Step 0: 지점 등록 인원 조회

- **처리 주체**: 스크립트 (GAS `getKakaoTaxiBranchMembers` + server.ts 동일 액션)
- **입력**: 지점 `pinHash`
- **처리 내용**: PIN 검증으로 지점명 확정 → 인증완료 직원 전량 조회 → member_department(별칭 포함)가 그 지점으로 매핑되는 인원만 필터해 반환
- **출력**: 그 지점 인원 배열 (이름·부서·전화·상태)
- **성공 기준**: 타 지점 인원이 포함되지 않음
- **검증 방법**: 규칙 기반 (백엔드 필터 — 클라이언트 필터 금지, 응답 자체에 타 지점 데이터가 없어야 함)
- **실패 시 처리**: PIN 불일치·조회 실패 시 오류 메시지 표시, 빈 목록을 성공처럼 보여주지 않음 (에러 배너 + 재시도 버튼)

#### Step 1: 신청 저장 (지점)

- **처리 주체**: 스크립트 (BusinessTaxiTab → gasClient.saveSharedData)
- **입력**: 폼 값(이름·전화·메모) 또는 요청(대상 인원·사유)
- **처리 내용**: 전화 정규화·필수값 검증 → `getSharedDataFromServer` 로 최신 배열 재조회 → 새 레코드(id=uuid, status=pending, requestedAt/By) append → 저장
- **출력**: `kakao_taxi_requests:<지점>` 갱신
- **성공 기준**: 저장 후 재조회 목록에 신규 신청이 보임
- **검증 방법**: 규칙 기반 (저장 성공 응답 + 목록 갱신)
- **실패 시 처리**: 저장 실패 시 입력값 보존 + 오류 표시, 자동 재시도 없음(수동 재시도)

#### Step 2: 관리자 취합

- **처리 주체**: 스크립트 (신청 관리 뷰 — 지점 목록 loop × `getSharedDataFromServer`)
- **입력**: 전 지점의 `kakao_taxi_requests:<지점>`
- **처리 내용**: 병렬 조회 → pending 우선 정렬 표시. 조회 실패 지점은 실패로 명시(급여 변동 이력 탭과 같은 fail-visible 규약)
- **출력**: 신청 목록 화면
- **성공 기준**: 실패 지점이 "조회 실패"로 드러남 (빈 결과로 위장 금지)
- **검증 방법**: 규칙 기반 (failed 배열 표시)
- **실패 시 처리**: 실패 지점 재시도 버튼, 전체 실패 시 에러 배너

#### Step 3: 승인 실행 (관리자)

- **처리 주체**: 스크립트 (기존 관리자 카카오 액션 재사용)
- **입력**: 승인 대상 신청 + (등록이면) 관리자가 확정한 그룹
- **처리 내용**: register → `registerKakaoTaxiMember`(부서=지점명 자동 기입) 후 인증 알림톡 발송. delete → `deleteKakaoTaxiMember`. update → 해당 인원 수정 카드 열기(사유 표시), 저장 성공 시 완료 처리. 반려 → 사유 입력 필수
- **출력**: 카카오 측 반영 + 처리 결과
- **성공 기준**: 카카오 API 성공 응답 (등록은 응답의 member id 확인, 삭제는 기존 재조회 검증 규약)
- **검증 방법**: 규칙 기반 (기존 1차분의 쓰기 검증 규약 재사용 — 재조회 확인·fail-closed)
- **실패 시 처리**: 카카오 실행 실패 시 신청 상태를 바꾸지 않고(pending 유지) 오류 표시 — 실행과 기록의 불일치 금지

#### Step 4: 상태 기록

- **처리 주체**: 스크립트
- **입력**: 처리 결과(approved/rejected/done + 사유)
- **처리 내용**: 해당 지점 키 fresh 재조회 → id로 대상 신청 찾기 → 아직 pending일 때만 상태·처리시각·사유 기록 후 저장 (이미 처리됐으면 중단+안내 — 두 관리자 동시 처리 방지)
- **출력**: 갱신된 신청 레코드
- **성공 기준**: 카카오 실행 성공 건이 pending으로 남지 않음
- **검증 방법**: 규칙 기반 (저장 후 재조회로 상태 확인)
- **실패 시 처리**: 기록 실패 시 "카카오는 반영됐으나 상태 기록 실패 — 새로고침 후 수동 확인" 경고 (실행 사실을 숨기지 않음)

#### Step 5: 지점 현황 표시

- **처리 주체**: 스크립트 (BusinessTaxiTab)
- **입력**: `kakao_taxi_requests:<지점>`
- **처리 내용**: 내 신청 목록을 상태 칩(대기/승인/반려)과 반려 사유와 함께 표시. 승인된 등록 신청에는 "직원 휴대폰으로 인증 알림톡이 발송됨 — 카카오T 앱에서 인증해야 이용 가능" 안내
- **출력**: 신청 현황 표
- **성공 기준**: 반려 건에 사유가 함께 보임
- **검증 방법**: 규칙 기반 (사유 없는 반려 렌더 금지)
- **실패 시 처리**: 조회 실패 시 오류 배너 + 재시도

### 상태 전이

| 상태 | 전이 조건 | 다음 상태 |
|------|----------|----------|
| (없음) | 지점이 신청 저장 | pending |
| pending | 관리자 승인 + 카카오 실행 성공 | approved |
| pending | 관리자 승인했으나 카카오 실행 실패 | pending (유지 — 재시도 가능) |
| pending | 관리자 반려(사유 필수) | rejected |
| approved(register) | 직원이 카카오T 앱 인증 완료 | 지점 등록 인원 목록에 표시(connected) |

---

## 3. 구현 스펙

### 폴더 구조

```
/erp_ugd
  ├── gas/Code.gs                                   # getKakaoTaxiBranchMembers 액션 추가 (기존 파일 확장)
  ├── server.ts                                     # 동일 액션 로컬 구현 (기존 파일 확장)
  ├── src/
  │   ├── api/gasClient.ts                          # getKakaoTaxiBranchMembers + 신청 타입 (기존 파일 확장)
  │   └── pages/
  │       ├── BranchConfirmPage.tsx                 # 인사·기타 그룹에 "비즈니스택시" 메뉴 + 렌더 배선
  │       ├── branch/tabs/BusinessTaxiTab.tsx       # 신규: 지점 탭 (신청 폼·인원 목록·요청·현황)
  │       ├── admin/KakaoTaxiSection.tsx            # "신청 관리" 뷰 추가 (기존 파일 확장)
  │       ├── admin/helpers/kakaoTaxiRequests.ts    # 신규: 신청 레코드 타입·정렬·검증 순수 함수 (지점/관리자 공용)
  │       └── AdminPage.tsx                         # 법인택시 그룹에 "신청 관리" 메뉴 (기존 파일 확장)
  └── blueprint-지점-비즈니스택시-신청.md            # 본 설계서
```

### CLAUDE.md 핵심 섹션 목록

- 신규 CLAUDE.md 없음 — 기존 `AGENTS.md`(리뷰·배포 절차)와 `DESIGN.md`/`DESIGN_ADMIN.md`(UI 규격)를 그대로 따른다
- `AGENTS.md` 추가 한 줄: "카카오 지점 매핑 별칭표는 helpers/kakaoTaxi.ts ↔ gas/Code.gs ↔ server.ts 세 곳 동기화"

### 에이전트 구조

**구조 선택**: 단일 에이전트 (Claude Code 단독 구현, 런타임 에이전트 없음)

**선택 근거**: 웹앱 기능 확장이며 런타임 LLM 판단이 없다. 1차분과 같은 세션·같은 파일군에서 작업하므로 분리 이유가 없다.

#### 메인 에이전트 (Claude Code)
- **역할**: 백엔드 → 공용 헬퍼 → 지점 탭 → 관리자 뷰 → 배선 순서로 구현, 단계별 로컬 검증 + Codex 리뷰
- **담당 단계**: Step 0~5 전부

### 스킬/스크립트 목록

| 이름 | 유형 | 역할 | 트리거 조건 |
|------|------|------|-----------|
| `gas/Code.gs::getKakaoTaxiBranchMembers` | 스크립트 | 지점 PIN 검증 후 자기 지점 인원만 반환 | 지점 탭 진입/새로고침 |
| `src/pages/admin/helpers/kakaoTaxiRequests.ts` | 스크립트 | 신청 레코드 타입·생성·상태전이 검증·정렬 | 지점 저장/관리자 처리 시 |
| `BusinessTaxiTab.tsx` | 스크립트(컴포넌트) | 지점 신청 폼·인원 목록·요청·현황 | 인사·기타 > 비즈니스택시 진입 |
| `KakaoTaxiSection.tsx` 신청 관리 뷰 | 스크립트(컴포넌트) | 전 지점 취합·승인/반려·카카오 실행 | 법인택시 > 신청 관리 진입 |
| 신규 스킬 | — | 이 설계에서 신규 Claude 스킬은 정의하지 않음 | — |

### 스킬 생성 규칙

> 이 설계서에 정의된 모든 스킬은 구현 시 반드시 `skill-creator` 스킬(`/skill-creator`)을 사용하여 생성할 것.
> 직접 SKILL.md를 수동 작성하지 말 것 — 규격 불일치 및 트리거 실패의 원인이 됨.
> (본 설계는 신규 스킬 0개이나, 후속 확장에서 스킬을 추가하게 되면 본 규칙을 따른다.)

skill-creator가 보장하는 규격:
1. SKILL.md frontmatter (`name`, `description`) 필수 필드 준수
2. `description`의 트리거 정확도 최적화 (eval 기반 optimization loop)
3. 폴더 구조 (`SKILL.md` + `scripts/` + `references/`) 규격 준수
4. Progressive disclosure: SKILL.md 본문 500줄 이내, 대용량 참조는 `references/`로 분리
5. 테스트 프롬프트 실행 및 품질 검증 완료

### 주요 산출물 파일

| 파일 | 형식 | 생성 단계 | 용도 |
|------|------|----------|------|
| `src/pages/admin/helpers/kakaoTaxiRequests.ts` | TS | 구현 1단계 | 신청 레코드 공용 로직 |
| `gas/Code.gs`·`server.ts` 수정분 | JS/TS | 구현 2단계 | 지점 인원 조회 액션 |
| `src/pages/branch/tabs/BusinessTaxiTab.tsx` | TSX | 구현 3단계 | 지점 탭 |
| `KakaoTaxiSection.tsx`·`AdminPage.tsx` 수정분 | TSX | 구현 4단계 | 관리자 신청 관리 |
| `kakao_taxi_requests:<지점>` | Firestore 공유데이터 | 런타임 | 신청 레코드 저장 |
