# UGD ERP 관리자 화면 디자인 기준 (색상 오버라이드)

이 문서는 **관리자 화면** 전용 기준입니다. 그러나 규칙을 새로 만들지 않습니다.

> **관리자 화면은 지점 화면 기준서 [`DESIGN.md`](./DESIGN.md)의 모든 규칙을 그대로 상속한다.**
> 구조·간격·섹션 카드·**바닐라 알약 제목**·검정 1px 테두리·표(헤더/줄무늬/엑셀형)·입력/버튼·상태색·테두리 함정 방지까지 **전부 `DESIGN.md`를 따른다.**
> **관리자에서 달라지는 것은 딱 하나 — "색상 값"뿐이다.** 아래 오버라이드 표의 3색만 연한 값으로 바꾼다.

새 화면/수정 시: **먼저 `DESIGN.md`를 읽고 그 규칙대로 만든 뒤**, 색만 이 문서의 오버라이드 표를 적용한다. (그래서 `DESIGN.md`가 개정되면 관리자도 자동으로 따라간다 — 규칙을 복제하지 않았기 때문.)

---

## 1. 왜 색만 다른가

지점 화면(`.branch-redesign`)과 관리자 화면(`.admin-redesign`)은 **토큰이 완전히 분리**돼 있습니다(`src/index.css`).

- 지점: `--branch-alice / --branch-honey / --branch-vanilla / --branch-black / --branch-ghost`
- 관리자: `--admin-alice / --admin-honey / --admin-vanilla / --admin-black / --admin-ghost`

두 세트는 서로를 참조하지 않으므로, **관리자 토큰을 바꿔도 지점 화면은 영향을 받지 않는다.** 관리자 화면을 "조금 더 연하게" 만들기 위해 바닐라·엘리스블루·허니듀 3색만 아래처럼 낮춘다. 검정·고스트는 그대로 둔다.

---

## 2. 색상 오버라이드 표 (이 문서가 정하는 전부)

`DESIGN.md`의 6색 중 **아래 3색만** 관리자에서 연한 값으로 치환한다. 나머지(검정 #212121·고스트 #F6F5FA) 및 **의미/용도는 `DESIGN.md`와 동일**하다.

| 토큰 | 지점 값 (`DESIGN.md`) | **관리자 값 (연하게)** | 용도(동일) |
| --- | --- | --- | --- |
| 바닐라 `--admin-vanilla` | `#EFF0A3` | **`#F4F2CC`** | 섹션/법인 제목 알약, 주의·미확인 |
| 엘리스블루 `--admin-alice` | `#D8DFE9` | **`#E6EBF3`** | 표 헤더(thead), 부드러운 패널 |
| 허니듀 `--admin-honey` | `#CFDECA` | **`#E0EBDC`** | 완료·긍정, 보조 패널 |
| 검정 `--admin-black` | `#212121` | `#212121` (동일) | 본문 글자·테두리·활성 버튼 |
| 고스트 `--admin-ghost` | `#F6F5FA` | `#F6F5FA` (동일) | 화면/카드 배경 |

정의 위치: `src/index.css`의 `.admin-redesign { --admin-* }` 블록. 값을 바꿀 땐 **이 표와 CSS를 함께** 갱신한다.

### 2-1. 상태 칩은 연하게 하지 않는다 (판독성)

월말마감 **상태 칩**은 "의미가 있는 색"이라 연하게 만들면 구분이 흐려진다. `DESIGN.md` §11의 원래 채도를 유지한다(하드코딩, 토큰 치환 대상 아님).

- 제출완료(확정) = 바닐라 `#EFF0A3`
- 수정중 = 엘리스 `#D8DFE9`
- 미제출 = 검정 `#18181B` + 흰 글자

(`.admin-monthly-status-confirmed / -editing / -pending`, `src/index.css`)

---

## 3. 자동 치환 (관리자도 지점처럼 작동)

관리자 화면 최상위엔 `admin-redesign`(+ 기본 켜진 `admin-design-preview`) 클래스가 붙어, 지점의 `.branch-redesign`과 같은 방식으로 표준 Tailwind 색을 관리자 토큰으로 자동 치환한다. 그래서 **탭에서 표준 색으로 칠하면 위 연한 팔레트에 자동으로 맞는다.**

- `bg-white` → 고스트 / `bg-amber-50`·`bg-orange-50` → 연한 바닐라 / `bg-blue-50`·`bg-sky-50` → 연한 엘리스 / `bg-emerald-50`·`bg-green-50` → 연한 허니
- 컬러 버튼(`bg-[#2E6DB4]`·`bg-blue-600`·`bg-emerald-*` 등) → 검정 + 밝은 글자 → **버튼은 색으로 의미를 나누지 않는다**(`DESIGN.md` §10)
- `thead tr` → 엘리스, `text-gray-*` → 68% 검정, `text-*-700` 진한색 → 검정
- **주의:** 특정 색을 꼭 유지할 땐 표준 색을 피하고 커스텀 hex(`#212121` 등)로 못 박는다. 헤더 라벨 글자는 `text-[#212121]`로 100% 검정을 준다(`text-gray-*`는 흐려짐 — `DESIGN.md` §9-1).

---

## 4. 제출현황 탭 (월말마감 → 제출현황) — 배치 다운로드 배선

이 탭의 특수 규칙(나머지는 `DESIGN.md`를 따름):

- 법인(UGD·카라멘야·연하동)별로 **검정 테두리 카드 + 연한 바닐라 제목 밴드**로 묶는다.
- 표 컬럼: `지점 | 정직원급여 | 월말마감 | 매출집계`. 표 헤더는 **라벨만**(검정 글자, 엘리스 배경).
- **법인 배치 다운로드 버튼은 법인명 밴드 행(제목 오른쪽)에 둔다**(검정 알약 버튼):
  - **[정직원급여 받기]** = 그 법인 **확정** 지점 급여를 한 파일로(`downloadCorpFullTimeSalary`).
  - **[매출집계 전체]** = 그 법인 **확정** 지점 매출집계를 한 파일로(`downloadCorpSalesSummary`).
  - `월말마감`은 배치 버튼 없음. 지점별 [엑셀] 버튼은 각 행에 유지.
- 헤더의 **[전지점 매출집계]** = 모든 지점 수집현황 덤프(확정/미확정 포함, `수집상태`로 표기 — 별도 유지).
- 다운로드는 **fail-closed·서버 전용 조회**를 지킨다: ①서버 읽기 실패 시 파일 미생성, ②**법인별 배치는 `monthly_closings`를 서버에서 다시 읽어 `status==='confirmed'` 지점만** 포함(화면 캐시로만 거르면 방금 '수정중/미제출'로 바뀐 지점을 놓쳐 미확정본이 최종본처럼 나감). `AdminPage.tsx`의 `downloadSalesSummary(confirmedOnly)`/`downloadFullTimeSalary` 참고.

---

## 5. 체크리스트 (관리자 화면 수정 시)

- [ ] `DESIGN.md`를 먼저 읽고 그 규칙대로 만들었나?
- [ ] 색은 위 오버라이드 표(연한 3색) + 검정/고스트 안에서만 썼나? 새 색을 만들지 않았나?
- [ ] 제목은 **연한 바닐라 알약(텍스트만)**, 카드는 **검정 1px 테두리**인가?
- [ ] 버튼이 색으로 의미를 나누려 하지 않나(어차피 검정으로 렌더)?
- [ ] 상태 칩은 연하게 만들지 않고 원래 채도를 유지했나?
- [ ] 코드를 고쳤으면 **Codex 지독한 리뷰 → 로컬(`npm run dev`, :3000) 확인 → 사용자 "배포해" 대기**(AGENTS.md)?
