# UGD ERP 관리자 화면 디자인 기준 (색상 오버라이드)

이 문서는 **관리자 화면** 전용 기준입니다. 그러나 규칙을 새로 만들지 않습니다.

> **관리자 화면은 지점 화면 기준서 [`DESIGN.md`](./DESIGN.md)의 모든 규칙을 그대로 상속한다.**
> 구조·간격·섹션 카드·**바닐라 알약 제목**·검정 1px 테두리·표(헤더/줄무늬/엑셀형)·입력/버튼·상태색·테두리 함정 방지까지 **전부 `DESIGN.md`를 따른다.**
> **관리자에서 달라지는 것은 딱 하나 — "색상 값"뿐이다.** 아래 오버라이드 표의 3색만 연한 값으로 바꾼다.

새 화면/수정 시: **먼저 `DESIGN.md`를 읽고 그 규칙대로 만든 뒤**, 색만 이 문서의 오버라이드 표를 적용한다. (그래서 `DESIGN.md`가 개정되면 관리자도 자동으로 따라간다 — 규칙을 복제하지 않았기 때문.)

---

## 1. 왜 색만 다른가

지점 화면(`.branch-redesign`)과 관리자 화면(`.admin-redesign`)은 **토큰이 완전히 분리**돼 있습니다(`src/index.css`).

- 지점: `--branch-alice / --branch-honey / --branch-vanilla / --branch-black / --branch-ghost`
- 관리자: `--admin-alice / --admin-honey / --admin-vanilla / --admin-black / --admin-ghost`

두 세트는 서로를 참조하지 않으므로, **관리자 토큰을 바꿔도 지점 화면은 영향을 받지 않는다.** 관리자 화면을 "조금 더 연하게" 만들기 위해 바닐라·엘리스블루·허니듀 3색만 아래처럼 낮춘다. 검정·고스트는 그대로 둔다.

---

## 2. 색상 오버라이드 표 (이 문서가 정하는 전부)

`DESIGN.md`의 6색 중 **아래 3색만** 관리자에서 연한 값으로 치환한다. 나머지(검정 #212121·고스트 #F6F5FA) 및 **의미/용도는 `DESIGN.md`와 동일**하다.

| 토큰 | 지점 값 (`DESIGN.md`) | **관리자 값 (연하게)** | 용도(동일) |
| --- | --- | --- | --- |
| 바닐라 `--admin-vanilla` | `#EFF0A3` | **`#F4F2CC`** | 섹션/법인 제목 알약, 주의·미확인 |
| 엘리스블루 `--admin-alice` | `#D8DFE9` | **`#E6EBF3`** | 표 헤더(thead), 부드러운 패널 |
| 허니듀 `--admin-honey` | `#CFDECA` | **`#E0EBDC`** | 완료·긍정, 보조 패널 |
| 검정 `--admin-black` | `#212121` | `#212121` (동일) | 본문 글자·테두리·활성 버튼 |
| 고스트 `--admin-ghost` | `#F6F5FA` | `#F6F5FA` (동일) | 화면/카드 배경 |

정의 위치: `src/index.css`의 `.admin-redesign { --admin-* }` 블록. 값을 바꿀 땐 **이 표와 CSS를 함께** 갱신한다.

### 2-1. 상태 칩은 연하게 하지 않는다 (판독성)

월말마감 **상태 칩**은 "의미가 있는 색"이라 연하게 만들면 구분이 흐려진다. `DESIGN.md` §11의 원래 채도를 유지한다(하드코딩, 토큰 치환 대상 아님).

- 제출완료(확정) = 바닐라 `#EFF0A3`
- 수정중 = 엘리스 `#D8DFE9`
- 미제출 = 검정 `#18181B` + 흰 글자

(`.admin-monthly-status-confirmed / -editing / -pending`, `src/index.css`)

---

## 3. 자동 치환 (관리자도 지점처럼 작동)

관리자 화면 최상위엔 `admin-redesign`(+ 기본 켜진 `admin-design-preview`) 클래스가 붙어, 지점의 `.branch-redesign`과 같은 방식으로 표준 Tailwind 색을 관리자 토큰으로 자동 치환한다. 그래서 **탭에서 표준 색으로 칠하면 위 연한 팔레트에 자동으로 맞는다.**

- `bg-white` → 고스트 / `bg-amber-50`·`bg-orange-50` → 연한 바닐라 / `bg-blue-50`·`bg-sky-50` → 연한 엘리스 / `bg-emerald-50`·`bg-green-50` → 연한 허니
- 컬러 버튼(`bg-[#2E6DB4]`·`bg-blue-600`·`bg-emerald-*` 등) → 검정 + 밝은 글자 → **버튼은 색으로 의미를 나누지 않는다**(`DESIGN.md` §10)
- `thead tr` → 엘리스, `text-gray-*` → 68% 검정, `text-*-700` 진한색 → 검정
- **주의:** 특정 색을 꼭 유지할 땐 표준 색을 피하고 커스텀 hex(`#212121` 등)로 못 박는다. 헤더 라벨 글자는 `text-[#212121]`로 100% 검정을 준다(`text-gray-*`는 흐려짐 — `DESIGN.md` §9-1).

---

## 4-0. [표준] 섹션 제목 밴드 · 엑셀형 표 — 모든 관리자 화면 공통 (2026-08-03 사용자 지시)

> **앞으로 관리자 화면의 섹션 제목과 표 헤더는 예외 없이 이 한 벌을 쓴다.** 기준 구현은 **분석 > 손익 종합 > 손익 요약표**다.
> 값을 손으로 다시 적지 말 것 — 클래스만 붙인다. 정의는 `src/index.css`의 `.admin-sheet-card` / `.admin-band*` / `.admin-sheet` 한 곳에 있고, 거기만 고치면 전 화면이 같이 바뀐다.

```tsx
<section className="admin-sheet-card">
  <div className="admin-band">
    <h3 className="admin-band-title">섹션 제목</h3>
    <p className="admin-band-meta">부연 설명(없으면 생략)</p>
    <div className="admin-band-actions">…버튼·필터(없으면 생략)…</div>
  </div>
  <div className="admin-sheet-scroll">
    <table className="admin-sheet"> … </table>
  </div>
</section>
```

### 제목 — 알약이 아니라 **밴드**

| 항목 | 값 | 이유 |
| --- | --- | --- |
| 형태 | 카드 맨 위, **표 전체 폭**을 채우는 띠 | 알약 제목은 카드 안에 떠 있어 표와 따로 놀았다 |
| 배경 | 연한 바닐라 `--admin-vanilla` + 아래 1px 검정 선 | 그 아래 선이 표의 위 테두리를 겸한다 |
| 높이 | `min-height: 53px` | 월말업무 > 제출현황 밴드와 같은 값 |
| 제목 글자 | **14px / 900** (`.admin-band-title`) | 부연(11px)과 크기가 같으면 위계가 없어 그냥 색칠한 줄로 보인다 |
| 부연 글자 | 11px / 700, 55% 검정 (`.admin-band-meta`) | |
| **필터** | `.admin-band-filters` — **제목 바로 옆에 붙인다** (사용자 지시 2026-08-03) | 오른쪽 끝으로 밀면 "무엇을 무엇으로 걸렀는지"가 제목과 떨어져 따로 읽힌다 |
| 밴드의 가로 배치 | `justify-content: flex-start` + **`order`로 자리 고정**(제목 0 · 필터 1 · 설명 2 · 버튼 3, 버튼만 `margin-left:auto`) | **`space-between`을 쓰면 안 된다** — 항목이 폭 전체에 고르게 벌어져 필터가 제목에서 멀어진다(실제로 그렇게 났다). `order`로 못 박아 두면 화면마다 적는 순서가 달라도 같은 자리에 그려진다 |
| 필터 칸 크기 | **11px · 높이 28px · 알약(999px) · 1px 검정 테두리 · 흰 바탕** | 예전 알약 제목과 같은 덩치. 밴드가 연한 바닐라라 칩이 고스트면 띠에 묻힌다 — **흰색**이라야 '고를 수 있는 칸'으로 읽힌다(2026-08-03) |
| 버튼(새로고침·다운로드) | `.admin-band-actions` (오른쪽 끝) | 필터와 달리 '실행'이라 자리를 나눈다 |
| 아이콘 | **금지** (`DESIGN.md` §6-1) | |

- 밴드는 **가로 스크롤 칸 바깥**에 둔다 — 안에 넣으면 표를 옆으로 밀 때 제목이 따라 밀린다.
- **[P0 · 세 번 반복된 실수] 복구 수단(업로드·새로고침·월/지점 선택)은 데이터 유무와 무관하게 항상 그린다.**
  화면을 정리하다 보면 컨트롤을 제목 밴드로 옮기게 되는데, 그 밴드가 `데이터 === null ? null` 안에 들어 있으면
  **조회가 한 번 실패한 순간 다시 시도할 방법이 사라진다.** 실제로 분석 탭 → 계정 관리 → 급여 변동 이력에서
  연달아 났다(2026-08-03). **카드와 밴드는 항상 그리고, 표 본문만 상태별로 가른다.** 컨트롤을 "데이터가 있을 때만" 그리는 블록 안에 넣으면, 비었을 때 **"업로드해 주세요" 안내만 뜨고 정작 올릴 버튼이 없는 막다른 화면**이 된다(분석 탭에서 실제로 났다, 2026-08-03). 같은 이유로 지점 선택줄도 "그 지점 데이터가 있을 때만" 그리는 블록 안에 두지 않는다 — 빈 지점을 고른 순간 돌아올 길이 사라진다.
- 카드에는 `padding`을 주지 않는다(밴드가 폭을 꽉 채워야 한다). 표 아래 각주는 자기 여백(`px-4 py-3`)을 갖는다.
- **기존 `.admin-pill-title`(바닐라 알약)은 새로 만들지 않는다.** 남아 있는 화면은 손댈 때 밴드로 옮긴다.

### 표 헤더 — 바닐라가 아니라 **엘리스**

`DESIGN.md` §9-1은 엑셀형 헤더를 바닐라로 정하지만, **관리자에서는 `--admin-alice`를 쓴다.** 바로 위 제목 밴드가 바닐라라, 헤더까지 바닐라면 두 줄이 한 덩어리로 뭉쳐 제목과 표의 경계가 사라지기 때문이다. (지점 화면은 밴드가 없어 바닐라 그대로.)

- 헤더 글자: 11px / 900 / `#212121` 100% 검정(`text-gray-*`는 68% 회색으로 치환돼 흐려진다)
- 본문: 12px, 옅은 격자 — 헤더와 대비를 준다

### 선 — `DESIGN.md` §9-1-A를 그대로 따른다

오른쪽·아래에만 · 전부 1px · 표의 바깥 네 변에는 긋지 않는다(카드 테두리와 밴드가 담당). **굵게 만들어 겹침을 해결하려 하지 말 것.** 자세한 이유와 함정은 §9-1-A 표 참고.

- 헤더 선은 `border`가 아니라 **`inset box-shadow`** 로 그린다 — 헤더는 sticky라 스크롤 중 크롬이 border를 그리다 빠뜨린다.
- `box-shadow`는 **겹쳐 쌓이지 않고 통째로 갈린다.** 어떤 칸에 그림자를 새로 주면 격자선까지 그 선언에 다시 적어야 한다.

---

## 4-4. [표준] '최근 N일' 알약 — 최근 데이터 표시 (2026-08-03 사용자 지시)

> **최근 데이터를 짚어 줄 때는 예외 없이 이 알약을 쓴다. 행 배경을 칠하지 않는다.**
> 기준 구현: 법인택시 > 이상 점검 탭. 값은 `src/index.css`의 `.admin-recent-badge` 한곳에 있다.

```tsx
<span className="admin-recent-badge">최근 3일</span>
```

| 항목 | 값 |
| --- | --- |
| 배경 | 연한 바닐라 `--admin-vanilla` |
| 테두리 | 1px 검정 `--admin-black` |
| 글자 | 10px / 900 / 검정 |
| 모양 | 알약(999px) · `padding: 2px 6px` · 왼쪽 여백 6px |
| 자리 | **날짜 칸 안, 날짜 글자 오른쪽** |

- **[왜 배경을 칠하지 않나]** 예전에는 최근 행 전체를 바닐라로 칠했다. 최근 건이 많은 날 표의 절반이 물들어 **오히려 어디를 봐야 할지 흐려졌다.** 알약은 몇 건이든 같은 크기로 짚어 준다.
- **[볼드는 알약에만]** 같은 칸의 날짜 글자는 볼드·강조색을 빼고 회색 mono 로 둔다. 표를 훑을 때 볼드가 알약 하나뿐이라야 최근 건이 걸린다.
- **날짜 표기는 `YYYY-MM-DD HH:mm`(하이픈)으로 통일**한다. 점(`2026.08.03`)과 섞여 있었던 것을 2026-08-03에 맞췄다.
- 적용된 곳: 마감 이력 점검(정산 변경이력 · 현금차이 이력 · 수기 초과근무 대장), 법인택시 이상 점검.

---

## 4. 제출현황 탭 (월말마감 → 제출현황) — 배치 다운로드 배선

이 탭의 특수 규칙(나머지는 `DESIGN.md`를 따름):

- 법인(UGD·카라멘야·연하동)별로 **검정 테두리 카드 + 연한 바닐라 제목 밴드**로 묶는다.
- 표 컬럼: `지점 | 정직원급여 | 월말마감 | 매출집계`. 표 헤더는 **라벨만**(검정 글자, 엘리스 배경).
- **법인 배치 다운로드 버튼은 법인명 밴드 행(제목 오른쪽)에 둔다**(검정 알약 버튼):
  - **[정직원급여 받기]** = 그 법인 **확정** 지점 급여를 한 파일로(`downloadCorpFullTimeSalary`).
  - **[매출집계 전체]** = 그 법인 **확정** 지점 매출집계를 한 파일로(`downloadCorpSalesSummary`).
  - `월말마감`은 배치 버튼 없음. 지점별 [엑셀] 버튼은 각 행에 유지.
- 헤더의 **[전지점 매출집계]** = 모든 지점 수집현황 덤프(확정/미확정 포함, `수집상태`로 표기 — 별도 유지).
- 다운로드는 **fail-closed·서버 전용 조회**를 지킨다: ①서버 읽기 실패 시 파일 미생성, ②**법인별 배치는 `monthly_closings`를 서버에서 다시 읽어 `status==='confirmed'` 지점만** 포함(화면 캐시로만 거르면 방금 '수정중/미제출'로 바뀐 지점을 놓쳐 미확정본이 최종본처럼 나감). `AdminPage.tsx`의 `downloadSalesSummary(confirmedOnly)`/`downloadFullTimeSalary` 참고.

---

## 4-1. [해소됨 — §4-0으로 대체] 관리자 제목 양식의 결론

**결론: 관리자 섹션 제목은 §4-0의 '제목 밴드'다.** 아래는 어떻게 여기까지 왔는지의 기록이며, **값을 찾을 때는 §4-0만 본다.**

- 2026-07-22까지: 관리자용 제목 CSS가 없어 탭마다 평제목(`text-xl font-black text-[#2C3E50]`)이 제각각이었다.
- 2026-07-22: 분석 탭에 알약 제목(`.admin-pill-title`)이 들어갔다. 다른 탭 확대 여부는 미결로 남았다.
- 2026-07-31~08-03: 법인택시·근로계약서로 알약이 퍼졌다.
- **2026-08-03(최종):** 사용자 지시로 알약을 접고 **밴드로 통일**했다. 알약 제목은 카드 안에 떠 있어 표와 따로 놀았고, 표 위에 붙는 띠라야 제목과 표가 한 덩어리로 읽힌다.

`.admin-pill-title`은 **새로 만들지 않는다.** 남은 자리(카드 중간에서 값과 나란히 놓이는 라벨 등 섹션 제목이 아닌 것)에만 쓰고, 섹션 제목으로 남아 있는 것은 손댈 때 밴드로 옮긴다.

**[표 안의 상태 색은 '사다리'로 준다 (2026-08-03 사용자 지시).]** 근로계약서 진행 상태가 기준 예시다 — **손댄 만큼만 진해진다.**

| 상태 | 색 | 이유 |
| --- | --- | --- |
| 발송 대기 | **없음(글자만)** | 새로 올라온 모든 줄의 기본값. 여기에 색을 칠하면 표 전체가 물들어 **정작 봐야 할 줄이 묻힌다.** |
| 발송 완료 | 바닐라 `#EFF0A3` | 보냈고 서명을 기다리는 중 |
| 서명 완료 | 허니 `#CFDECA` | 완료 |
| 보류 | 검정 `#18181B` + 흰 글자 | 멈춤 |

- 값은 §2-1대로 **원래 채도**를 쓴다(연한 관리자 토큰으로 낮추지 않는다 — 상태는 판독이 우선).
- 색을 뺀 칸이 `<select>`면 **테두리는 `transparent`로만** 만든다. `border: 0`으로 지우면 폭·높이가 줄어 상태를 바꿀 때마다 칸이 들썩인다.
- 배선 위치: `src/index.css`의 `.admin-labor-status-*`, 매핑은 `AdminPage.tsx`의 `LABOR_STATUS_CLASS`.

---

## 4-2. 대시보드(전지점 매출 종합) 전용 규칙 (2026-07-22 신설)

관리자 대시보드는 전지점 매출 화면이다(전일 성격 내용은 '전일 정산현황' 탭으로 옮겼다).

**기간 선택 알약 (`.admin-period-chip`)** — `DESIGN.md` §10의 알약 토글을 관리자 토큰으로 옮긴 것.
- 꺼짐: 배경 `--admin-ghost` + 1px 검정 테두리, hover 시 `--admin-vanilla`
- 켜짐(`.is-active`): 배경 `--admin-black` + 글자 `--admin-ghost`

**증감 색 (`.admin-delta-up` / `-down` / `-flat`)** — [필수] 전용 클래스로 못 박는다.
- 관리자 스코프는 emerald·rose 계열 **글자색을 전부 검정으로 치환**하므로 Tailwind 색으로는 증감을 표현할 수 없다.
- 값: 증가 `#1F7A4D` / 감소 `#B3261E` / 변화없음 `rgba(33,33,33,0.55)`
- **색이 죽어도 읽히도록 컴포넌트가 `▲`/`▼` 기호를 함께 찍는다.** 색만으로 의미를 나누지 않는다.

### 2-1. [P0] 관리자에서 색이 죽거나 **뒤집히는** 두 가지 (실제로 당한 사례)

**① 실패 배너에 `bg-rose-50`을 쓰면 "실패가 성공처럼" 보인다.**
`DESIGN.md` §11은 동기화 실패를 `bg-rose-50 text-rose-700`으로 적으라고 한다. 그런데 관리자 스코프는
`.admin-redesign .bg-rose-50 → var(--admin-honey)`(index.css)로 치환한다 — **허니는 "완료·긍정" 색이다.**
즉 지침을 그대로 따르면 **지침이 금지하는 결과**(실패를 성공처럼 보이게)가 나온다. 글자색(`text-rose-700`)도
검정으로 죽어 붉은 기가 하나도 남지 않는다.
→ 관리자 오류/실패 표시는 **`DESIGN.md` §11의 오류 hex를 직접 박는다**: 배경 `#FDE2E2` · 테두리 `#C93A3A` · 글자 `#B91C1C`.
(적용 예: `SalaryChangeHistoryTab.tsx`의 조회 실패·지점 누락 배너)

**② `--branch-*` 토큰은 관리자 화면에서 값이 없다(배경이 아예 안 칠해진다).**
지점 토큰은 `.branch-redesign` 스코프에만 정의돼 있다. 관리자(`.admin-redesign`)에서
`bg-[var(--branch-vanilla)]`를 쓰면 변수 미정의라 **투명하게 렌더된다** — 칩·배지가 통째로 비어 보인다.
두 토큰 세트는 서로를 참조하지 않는다. 관리자에서는 **반드시 `--admin-*`**을 쓴다.

> 공통 교훈: 관리자 화면에서 색으로 의미를 나눌 땐 **치환표(index.css `.admin-redesign`)를 먼저 확인**한다.
> Tailwind 색 이름이 그대로 렌더될 거라고 가정하지 말 것. 의미가 중요한 색은 전용 클래스나 hex로 못 박는다.

**금액 KPI 칸 (`.admin-kpi-money`)** — 기본 `.admin-kpi-card`의 숫자는 30~46px이라 `712,300,000원`이 들어가지 않는다. 이 변형은 숫자를 21~24px로 줄이고, 누를 수 없는 칸이므로 `::after`의 이동 화살표(↗)를 지운다. 카드가 3개일 때는 `.admin-kpi-grid-3`를 함께 준다(기본은 4열).

**[필수] 집계가 불완전하면 숫자를 정상값처럼 보여주지 않는다 (`.admin-kpi-warn`).**
- 지점 조회가 하나라도 실패하면 합계는 **실제보다 작은 값**이다. 그 카드의 밑줄을 붉은 경고(`#B3261E`)로 바꿔 "N개 지점 확인 불가 — 실제보다 적은 금액입니다"를 띄운다.
- 고른 지점이 **전부** 실패면 `0원`이 아니라 **"확인 불가"** 로 적는다. 0원은 "매출이 없었다"로 읽힌다.
- 불러오는 중에는 직전 기간의 경고를 감춘다(요청 경계와 메시지를 맞춘다).
- 근거: AGENTS.md P0-2 "저장 실패를 초록 저장됨으로 속이지 않는다"와 같은 원칙. Codex 리뷰가 이 건으로 3라운드 연속 지적했다.

**선그래프 (`SalesLineChart.tsx`)** — 차트 라이브러리를 넣지 않고 인라인 SVG로 그린다.
- 선택 기간 = `#2E6DB4` 실선, 직전 동기 = `rgba(33,33,33,0.32)` **점선**. 색과 선모양을 함께 다르게 해 색약·흑백에서도 구분되게 한다.
- 두 계열은 **날짜가 아니라 순번(index)으로 겹쳐 그린다** — 저번달(30일) vs 그 전달(31일)처럼 길이가 다를 수 있다.
- x축 라벨은 최대 12개로 솎아낸다.

**[데이터 한계 — 요구를 받으면 먼저 알릴 것]** ERP 일일마감에는 **하루 총액만** 있다(현금·카드·이체·배달·총매출).
**시간대별 매출·주문 건수·객단가는 저장되지 않으므로 그런 지표·그래프는 만들 수 없다.**
그래서 '어제/오늘'처럼 하루짜리 기간은 선그래프가 점 하나가 되어, 차트만 **최근 14일 추이**로 대체한다.

---

## 4-3. 분석 > 월간 손익 분석 (2026-07-22 신설, 같은 날 db파일 기반으로 개편)

**데이터 원천은 04 에이전트의 `db파일.xlsx`(손익DB 시트)다** — ERP 월말마감이 아니다(사용자 지시 2026-07-22).
Firestore `shared_data/analysis_pnl_db` 한 문서에 통째로 저장하며, 초기 적재는 `scripts/seed-analysis-pnl.mjs`,
매월 갱신은 화면의 **'db파일 업로드'** 버튼(브라우저 파싱, 같은 컬럼 계약·같은 키)이다. 04·05 원본 폴더는 **읽기 전용**.

로컬 05 에이전트의 세 산출물을 모두 담는다: 지점별 손익계산서(`branch_detail`) + 지점 대시보드(`branch`) + 본사 종합(`hq_dashboard`/`hq_charts`).
지점 드롭다운 '전체'=본사 종합 뷰, 지점 선택=손익계산서+지점 대시보드 뷰. **§4-2의 규칙(증감색 전용 클래스·admin-kpi-money)을 그대로 쓴다.** 이 탭 고유 규칙:

- **지표 정의는 `helpers/pnlDb.ts` 머리주석이 원천**이다(parse_db.py 그대로: 식재료율=식재료÷**메뉴매출**, 인건비율=인건비÷총매출, Prime=식재료율+인건비율, 생산성=이익금÷인건비, 회전율=영수건수÷테이블수÷영업일수, 경보=식재료율>35%·인건비율>35%·**이익률<5%**). 산식을 임의로 바꾸지 않는다.
- **전사 합계·평균·순위는 본사 행 제외** — 본사 행의 총매출은 전지점 합산이라 섞으면 이중계상된다. 본사 배당/인센·이익잉여금은 순위표 아래 한 줄로만.
- **비율 증감은 의미에 따라 색을 준다** — 식재료율·인건비율·Prime 은 상승=빨강/하락=초록(`RateDelta goodWhenUp=false`), 이익률·생산성·매출은 상승=초록.
- 경보 기준 초과 값은 `.admin-rate-hot`(#B3261E) 전용 클래스(text-rose-* 는 검정으로 치환됨).
- PRIME 게이지는 `.admin-prime-gauge`(식재료=바닐라, 인건비=엘리스, 목표 60% 빨간 세로선, 축 0~100%).
- 지점 포지셔닝 산점도는 `PnlScatterChart`(평균선 사분면·버블·지점명 라벨) — 매출×이익률 / 인건비×이익률 / 회전율×객단가 3종.
- 업로드는 **'손익DB' 시트·필수 컬럼(특별지출비고 포함 22개) 검증** 후에만 저장한다. 저장 직전 **서버 신선값을 다시 읽어**(fail-closed — 못 읽으면 업로드 중단) ①행수 급감(30%↑) 확인 ②다른 컴퓨터가 방금 갱신했으면 덮어쓰기 확인 ③branchOps(테이블수·휴무일)는 신선값 기준으로 유지한다. 화면 상태를 기준으로 삼으면 로드 실패 상태의 업로드가 설정을 지운다(Codex P0).
- 하위탭 3개(손익 종합/손익 차트/지점 손익계산서, `analysisTab`)로 나눈다 — 한 탭에 다 넣으면 너무 길다(2026-07-22 사용자 지시). 표에는 인라인 막대(`.admin-cell-bar-*`)·상태 배지(`.admin-pnl-badge-*`)·구획 밴드(`.admin-statement-band`)를 써서 텍스트만 나열하지 않는다.
- **차트는 전폭 1개가 아니라 `.admin-chart-grid`(2열, ≤1100px에서 1열)에 반폭으로 배치**한다(2026-07-22 사용자 지시 — "그래프 폭이 쓸데없이 크다"). 반폭 차트는 `compact` prop(SalesLineChart·PnlScatterChart)으로 viewBox 를 좁혀 글자가 작아지지 않게 한다.
- 월별 매출·이익은 선 2개가 아니라 **그룹 막대 + 이익률 꺾은선(우측 % 축)** 콤보(`PnlComboChart`)로, 지점별 비교는 **가로 그룹 막대**(`PnlHBarChart`, 적자=빨강)로 그린다 — 05 PNG의 차트 문법. 막대 색은 alice/honey/vanilla 계열 3색(`PnlBarCharts.tsx`의 BAR_TONES)만 쓴다.
- **손익 요약표는 읽기 전용 엑셀형**(`.admin-summary-table`, 2026-07-22 사용자 지시): 헤더는 §9-1의 격자 규칙(각 셀 사방 검정 격자 + 11px 검정 볼드), `border-separate` + 헤더(top)·매장명 열(left) sticky 고정, 소계=바닐라 45% 행·정산부=엘리스 40% 행 하이라이트, **클릭한 행에 §9-3 커서 띠**(일반 행=막+선, 상태색 행=선만). 입력칸 없음 — 수정 불가.
  - **[2026-08-03 개정] 제목은 알약이 아니라 '제목 밴드'다 — 제출현황 탭(§4)과 같은 문법.** 카드 맨 위에 **표 전체 폭**으로 연한 바닐라 띠(`bg-amber-50` → `--admin-vanilla`, 아래쪽 1px 검정 선)를 깔고 제목·설명을 넣은 뒤, **컬럼 헤더 줄을 바로 아래 붙인다.**
  - 그래서 이 표의 **컬럼 헤더는 바닐라가 아니라 엘리스**다(§9-1의 바닐라 헤더에서 벗어나는 유일한 예외). 밴드와 헤더가 같은 색이면 두 줄이 한 덩어리로 뭉쳐 제목과 표의 경계가 사라진다. (원래도 `.admin-design-preview .admin-sales-overview-section th`가 `!important`로 엘리스를 덮어써 화면은 계속 엘리스였다 — 2026-08-03에 `src/index.css` 값을 실제 렌더와 같게 맞췄다.)
  - 밴드는 **가로 스크롤 칸 바깥**에 둔다(표를 옆으로 밀 때 제목이 따라 밀리면 안 된다). 카드에는 `overflow-hidden`을 주고 `p-5`는 뺀다 — 안 그러면 띠가 둥근 모서리 밖으로 삐져나온다. 각주 묶음이 자기 여백(`px-4 py-3`)을 갖는다.
  - 밴드 아래 첫 헤더 행(`.admin-sum-h1`)에는 `border-top`을 주지 않는다. 띠의 아래 선과 겹쳐 2px로 두꺼워진다.
  - **[치수를 제출현황과 같은 값으로 맞춘다]** "비슷한 디자인"은 색·구조만이 아니라 **숫자가 같아야** 나란히 놓았을 때 같아 보인다(2026-08-03 사용자 지적: 폰트·행 높이·모서리가 다 달랐다).

    | 항목 | 값 | 비고 |
    | --- | --- | --- |
    | 카드 모서리 | **16px**(`rounded-2xl`) | 손익 요약표는 `.admin-sales-overview-section`이 30px를 `!important`로 주므로 `.admin-summary-card`를 **ID 특이성**으로 되받는다 |
    | 카드 테두리 | 1px 검정 **하나만** | 보조 `outline`을 함께 두면 1px 어긋나 겹쳐 그려져 굵어 보인다 — 껐다 |
    | 밴드 여백 | `px-4 py-2.5` | |
    | 밴드 **세로폭** | **`min-h-[53px]`** | 제출현황 밴드는 `py-2.5` + 안에 든 `h-8`(32px) 버튼 + 아래 선 1px = 53px. 이 밴드엔 버튼이 없어 같은 여백으로도 37px밖에 안 서므로 **최소 높이를 못 박는다** |
    | 밴드 제목 | **14px/900** | 분석 탭 11px 통일의 **유일한 예외**(`.admin-band-title`). 설명 문구는 11px — 그래야 위계가 생긴다 |
  - **[P0 — 제출현황과 맞추는 것은 '카드와 밴드'까지다. 표 안쪽 치수는 손대지 않는다.]** 이 표는 컬럼이 17개라 **가로가 화면에 겨우 들어맞는다.** 표 안쪽을 제출현황에 맞추려다 세 번 되돌렸다(2026-08-03).

    | 건드린 것 | 결과 | 되돌린 값 |
    | --- | --- | --- |
    | 칸 여백 8px→12px, 고정열 104→120px | 폭이 약 150px 늘어 **맨 오른쪽 '이익' 컬럼이 잘리고 가로 스크롤**이 생김 | 여백은 셀의 `py-1.5 px-2` 등 TSX 클래스에 맡기고 CSS에서 덮어쓰지 않는다 / 고정열 **104px** |
    | 본문 글자 11→12px, 행 높이 →44px | 한 화면에 들어오는 지점 수가 크게 줄어듦 | 분석 탭 통일값 **11px**, 행 높이는 내용대로 |
    | 2단 헤더 아랫줄 10→11px | 폭이 늘어남 | **10px** |

    제출현황 행이 44px인 것은 칸마다 28px 버튼이 들어 있어 생긴 **결과**일 뿐 맞춰야 할 기준이 아니다.

  - **[선이 겹쳐 보이는 것은 굵기 문제가 아니라 '두 번 그린' 문제다]** 굵게 해서 해결하려 하지 말 것 — 굵은 선은 오히려 두 줄로 보이고, 1px 구간과 만나는 자리에서 굵기가 꺾여 더 겹쳐 보인다(2026-08-03에 2px로 올렸다가 되돌림). **같은 경계에 선이 두 번 그려지는 자리를 없애는 것**이 정답이다.

    | 규칙 | 이유 |
    | --- | --- |
    | 세로선은 **칸의 오른쪽에만** 그린다 | 왼쪽에도 그리면 이웃 칸끼리 같은 경계를 각각 그려 두 줄이 된다. 그룹 경계선도 `.admin-group-end`(그룹 **마지막** 칸 오른쪽)로 통일 |
    | 가로선은 **칸의 아래에만** | 위·아래 둘 다 주면 같은 이유로 두 줄 |
    | 표의 **맨 왼쪽·맨 위**에는 긋지 않는다 | 카드 테두리와 제목 밴드의 아래 선이 이미 그린다 |
    | 표의 **맨 오른쪽 칸**에도 긋지 않는다(`.admin-col-last`, `tbody td:last-child`) | 바로 옆이 카드 테두리라 검정 선이 두 줄로 붙는다. **`th:last-child`로 쓰면 안 된다** — 헤더 아랫줄의 마지막 칸까지 걸려 '이익│이익비중' 경계가 사라진다 |
    | 굵기는 **전부 1px. 예외 없음** | 2px는 예외 없이 "두 줄로 보인다"는 지적을 받았다 — 그룹 경계 → 헤더↔본문 경계 → 고정열 닫는 선 순서로 세 번 되돌렸다 |
    | 고정열이 끝나는 선(`.admin-summary-name` 오른쪽)은 `DESIGN.md` §9의 **2px 대신 1px + 진한 색**(0.55) | 굵기가 아니라 **색**으로 본문 잔선(0.08)과 구분한다. 이 표에 한정된 예외 |

    `rowspan`이 걸린 칸(구분·이익비중)은 두 헤더 줄을 합쳐 쓰므로 **자기 아래쪽이 곧 헤더의 밑줄**이다. 굵기가 전부 1px인 지금은 기본 `thead th` 규칙만으로 옆 칸들과 같은 자리·같은 굵기가 되어 밑줄이 한 줄로 이어진다 — `rowspan` 전용 규칙은 두지 않는다.

    기본 규칙을 덮어쓰는 것은 **두 칸뿐**이고, 서로 다른 칸이라 순서를 다툴 일이 없다.

    | 클래스 | 덮어쓰는 것 |
    | --- | --- |
    | `.admin-col-last`(이익비중) | 세로선을 지운다 — 옆이 카드 테두리 |
    | `.admin-summary-name`(구분) | 세로선 색만 진하게(0.55) — 고정열이 끝나는 자리 |
    | 표 왼쪽 선 | **없음** | 카드 테두리가 이미 닫는다 — 헤더에 `border-left`를 주면 선이 두 줄이 된다 |

---

## 5. 체크리스트 (관리자 화면 수정 시)

- [ ] `DESIGN.md`를 먼저 읽고 그 규칙대로 만들었나?
- [ ] 색은 위 오버라이드 표(연한 3색) + 검정/고스트 안에서만 썼나? 새 색을 만들지 않았나?
- [ ] 제목은 **§4-0의 제목 밴드**(`.admin-band` + `.admin-band-title`)인가? 평제목·알약을 새로 만들지 않았는가?
- [ ] 표는 **§4-0의 `.admin-sheet`** 인가? 헤더 색·격자·여백을 화면에서 다시 적지 않았는가?
- [ ] 필터가 있으면 **제목 바로 옆**(`.admin-band-filters`)에 붙였는가? 오른쪽 끝으로 밀지 않았는가?
- [ ] 표가 스크롤되면 **헤더가 고정**되는가? (§4-0 — `.admin-sheet`가 처리하므로 직접 sticky를 적을 일은 없다)
- [ ] 카드는 **검정 1px 테두리**인가? 위쪽 `main > section:not(...)` 규칙에 눌리면 **`#admin-layout-wrapper` ID 특이성**으로 이겼나(`DESIGN.md` §13-7)?
- [ ] 조회 실패를 0원으로 보여주지 않나? 부분 집계에 경고를 달았나(§4-2)?
- [ ] 버튼이 색으로 의미를 나누려 하지 않나(어차피 검정으로 렌더)?
- [ ] 상태 칩은 연하게 만들지 않고 원래 채도를 유지했나?
- [ ] 코드를 고쳤으면 **Codex 지독한 리뷰 → 로컬(`npm run dev`, :3000) 확인 → 사용자 "배포해" 대기**(AGENTS.md)?
