# 전자 근로계약서 발송 워크플로우 설계서

> 작성일: 2026-07-04
> 목적: Claude Code 구현 참조용 계획서 (ERP `erp_ugd` 기능 확장)

---

## 1. 작업 컨텍스트

### 배경 및 목적

지점 점장이 일일마감정산(`DailySettleTab`)에서 근무자를 추가하면 직원현황(`BranchOwnRoster`)에 자동 연동된다. 이 중 **정직원 신규입사·지점이동** 인원에게는 본사가 전자 근로계약서를 발송해야 하는데, 지금은 이 과정이 수작업(엑셀 대장 + 개별 전자계약 사이트 수동 입력)이라 누락·지연·오입력이 발생한다.

목표는 **"점장이 근무자를 추가하면 → 본사가 검토 후 버튼 한 번으로 올바른 템플릿의 전자 근로계약서를 발송하고 → 서명 완료가 자동으로 직원현황에 반영되며 → 미서명 건은 리마인드되는"** 반자동 파이프라인을 완성하는 것이다.

본사 AdminPage에는 이미 `AdminLaborContractsSection`(발송 후보 자동 감지 + 발송 내역 상태관리 골격)이 존재한다. 본 설계는 이를 재작성하지 않고 **끝까지 잇는(complete) 것**을 원칙으로 한다.

### 범위

- **포함**
  - 발송 후보 감지 필터를 "정직원 × (신규입사 | 지점이동)"으로 정확화 (기타·파트타이머 제외)
  - 발송 전 정보 완결성 검증 및 지점 보완 요청
  - 본사 수동 승인 게이트 → 전자계약 서비스 API로 실제 발송 (서버 프록시 경유)
  - 서명 완료 webhook 수신 → 계약 상태 및 직원현황 자동 반영
  - 미서명 건 리마인드 (본사 대시보드 표시 + 선택적 재발송)
- **제외**
  - 파트타이머(3.3%) 계약서 — 발송 대상 아님
  - `기타` 사유 인원 — 계약 발송 대상 아님 (직원현황 표시만 유지)
  - 계약서 법적 문안 작성/자문 — 템플릿은 본사가 전자계약 서비스에 사전 등록한 것을 사용
  - 급여/4대보험 신고 등 후속 노무 처리

### 입출력 정의

| 항목 | 내용 |
|------|------|
| **입력** | 지점 직원현황 행(`RosterEmployee`): `name, division, residentNumber, phone, addReason(신규입사/지점이동), hireDate/transferDate/entryDate, salaryChanged, rank, contractType, addReasonMemo`. 출처: Firestore `branch_own_roster/<지점>` (via `gasClient.getBranchOwnRoster`) |
| **출력** | ① 계약 레코드: Firestore 공유데이터 `labor_contracts:<지점>` 배열에 추가/갱신 (기존 키 재사용). ② 직원현황 행에 `contractStatus/contractId/contractSentAt/contractSignedAt` 필드 역기록. ③ 서명 완료 PDF 링크(전자계약 서비스 보관, URL만 저장) |
| **트리거** | 점장의 근무자 추가는 상시 발생 → 본사 화면 진입/포커스 시 후보 목록 자동 새로고침(이미 구현). 실제 발송은 **본사의 수동 "발송" 클릭**이 트리거. 서명 완료 반영은 **전자계약 서비스 webhook**이 트리거. 리마인드는 **1일 1회 스케줄** 트리거 |

### 제약조건

- **비밀키 보안**: 전자계약 서비스 API 키는 클라이언트(React) 번들에 절대 포함 금지 → 반드시 서버(Cloud Function 또는 GAS 웹앱) 경유. 프런트는 서버 프록시 엔드포인트만 호출.
- **백엔드**: 런타임 데이터는 Firebase(Firestore) 사용. `gasClient`는 `firebaseDirect` 래퍼(이름만 GAS). 신규 서버 로직은 **Firebase Cloud Functions 권장**, GAS 웹앱은 대안.
- **전자계약 서비스 요구사항**: (1) 사전 등록 템플릿에 필드 값 매핑하여 문서 생성 가능한 API, (2) 서명자에게 링크/알림톡 발송, (3) 서명 완료 webhook 제공 — 세 가지를 만족하는 서비스 필요(모두싸인·이폼사인 등 후보). **구현 착수 전 서비스·요금제·API 키 확정 필요.**
- **개인정보**: 주민등록번호는 앞 6자리만 후보 판정에 사용. 전체 주민번호는 계약 생성 시 서버→전자계약 서비스로만 전달, ERP 저장/로그 금지.
- **처리량**: 월 수십 건 규모(14+ 지점). 실시간성 요구 낮음 — 배치/수동 발송으로 충분.

### 용어 정의

| 용어 | 정의 |
|------|------|
| 발송 후보(candidate) | 직원현황에서 계약 발송 조건(정직원 × 신규입사/지점이동)에 해당하나 아직 계약 레코드가 없는 인원 |
| 계약 레코드(contract) | 발송이 개시된 건. `labor_contracts:<지점>`에 저장, 상태(발송대기/발송완료/서명완료/보류) 보유 |
| 정보부족(missing) | 발송에 필요한 필수값(주민 앞6·입사/이동일·연락처·급여변동 등)이 비어 발송 불가한 상태 |
| 서버 프록시 | API 키를 보관하고 전자계약 서비스를 호출·webhook 수신하는 서버(Cloud Function 또는 GAS 웹앱) |
| esign-adapter | 전자계약 서비스별 API 차이를 흡수하는 어댑터(스킬) |

---

## 2. 워크플로우 정의

### 전체 흐름도

```
[점장: 일일마감정산 근무자 추가]
        │  addReason=신규입사|지점이동, division=정직원
        ▼
[직원현황(BranchOwnRoster)에 연동 저장]  ← 이미 구현
        │
        ▼
[Step 1: 발송 후보 감지] ─(본사 화면 자동 새로고침)
        │
        ▼
[Step 2: 정보 완결성 검증]
        ├─ 정보부족 ─▶ [지점 보완 요청] ──(점장이 직원현황 보완)──┐
        │                                                          │
        └─ 발송대기 ◀──────────────────────────────────────────────┘
        │
        ▼
[Step 3: 본사 검토 · 발송 (수동 게이트)]
        │  템플릿 자동선택: 신규입사→T1 / 지점이동→T2
        │  서버 프록시 → 전자계약 서비스 문서 생성 + 링크 발송
        ▼
[계약 레코드 status=발송완료, 직원현황 contractStatus=발송완료]
        │
        ▼
[Step 4: 직원 전자서명 (외부 서비스)]
        │
        ▼
[Step 5: 서명 완료 webhook 수신] ─(서버)
        │  status=서명완료, 직원현황 contractStatus=서명완료, 서명PDF URL 저장
        ▼
[Step 6: 미완료 리마인드 (1일 1회 스케줄)]
        │  발송완료 후 N일 미서명 → 본사 대시보드 알림 + 선택적 재발송
        ▼
[완료]
```

### LLM 판단 vs 코드 처리 구분

이 워크플로우는 **런타임 대부분이 결정론적(코드)** 이다. LLM(Claude Code)은 주로 **빌드 시점 구현 주체**이며, 런타임 LLM 개입면은 얇다. 아래는 런타임 기준 구분.

| LLM이 직접 수행(런타임, 선택적) | 스크립트/코드로 처리(런타임, 필수) |
|----------------|----------------|
| 정보부족 사유를 지점용 자연어 보완 안내 문장으로 생성 | 후보 필터링(정직원 × 신규/이동), 필수필드 검증(missingFields 계산) |
| 급여변동 메모(자유서술) → 계약 급여 반영 여부 해석 보조 | 템플릿 선택(신규입사→T1/지점이동→T2, 규칙 기반) |
| (지점이동) 근무지·조건 변경 요약 문구 초안 | 전자계약 API 호출·필드 매핑, webhook 서명검증, 상태 전이, 리마인드 스케줄 |

> 원칙: 자연어 안내/해석 보조만 LLM. 상태 변경·API 호출·개인정보 처리는 전부 코드. LLM이 계약 발송 여부를 최종 결정하지 않는다(본사 사람이 결정).

### 단계별 상세

#### Step 1: 발송 후보 감지

- **처리 주체**: 스크립트/프런트 로직 (`AdminLaborContractsSection.loadData` 수정) + 서버 조회(`gasClient.getBranchOwnRoster`)
- **입력**: 전 지점 직원현황 배열
- **처리 내용**: `division === "정직원" && addReason ∈ {신규입사, 지점이동}` 인 행만 후보로 추출. **기존 코드 수정 필요** — 현재 `isContractCandidateEmployee`는 `기타`까지 포함하고 `division` 필터가 없음. 이미 계약 레코드가 존재하는 인원은 후보에서 제외(또는 "진행중" 표시).
- **출력**: `ContractCandidateRow[]` (지점·이름·사유·주민앞6·유효일·연락처·급여변동·정보부족목록)
- **성공 기준**: 대상 조건 인원이 빠짐없이, 비대상(파트타이머·기타·이미발송)은 제외되어 목록화됨
- **검증 방법**: 규칙 기반(필터 조건 단위 테스트) + 표본 지점 수동 대조
- **실패 시 처리**: 특정 지점 roster 조회 실패 시 해당 지점만 스킵 + 콘솔/로그 기록, 나머지 지점은 정상 표시(현행 `catch(() => [])` 유지)

#### Step 2: 정보 완결성 검증

- **처리 주체**: 스크립트 (`getContractCandidateMissingFields` 재사용·정직원 기준으로 정리)
- **입력**: 후보 행
- **처리 내용**: 사유별 필수필드 검사 — 공통: 주민 앞6, 성명, 유효일. 신규입사: 연락처(휴대폰 8자리), 급여. 지점이동: 급여변동(있음/없음), 변동시 신급여. 하나라도 없으면 `정보부족`, 전부 충족 시 `발송대기`.
- **출력**: 각 후보의 `missingFields[]` 및 상태 뱃지(정보부족/발송대기)
- **성공 기준**: 정보부족 인원은 발송 버튼 비활성, 발송대기 인원만 발송 가능
- **검증 방법**: 스키마/규칙 기반 검증
- **실패 시 처리**: 에스컬레이션 — 정보부족 목록을 지점별로 집계해 "지점 보완 요청"으로 표시. (선택) 점장 화면 알림. 사람이 직원현황에서 보완할 때까지 대기(자동 발송 안 함)

#### Step 3: 본사 검토 · 발송 (수동 승인 게이트)

- **처리 주체**: 본사(사람)의 클릭 → 서버 프록시(Cloud Function/GAS) → `esign-adapter` 스킬
- **입력**: 발송대기 상태의 후보 1건(또는 다중 선택)
- **처리 내용**:
  1. 본사가 후보 행에서 정보 최종 확인 후 "계약서 발송" 클릭
  2. 프런트 → 서버 프록시 호출(후보 식별자 + 확정 급여/유효일 전달, 주민번호 전체는 서버가 roster에서 직접 조회)
  3. 서버가 규칙으로 템플릿 선택: `신규입사→T1(표준근로계약서)`, `지점이동→T2(근무지·조건 변경 계약)`
  4. `esign-adapter.createContractDocument(template, fields)` 호출 → 문서 생성 + 서명자 링크/알림톡 발송
  5. 계약 레코드 생성: `labor_contracts:<지점>`에 `{id, branchName, name, phone, salary, reason, templateType, esignDocId, esignUrl, status:"발송완료", createdAt, sentAt}` 추가
  6. 직원현황 행에 `contractStatus="발송완료", contractId, contractSentAt` 역기록
- **출력**: 발송완료 계약 레코드 + 직원현황 상태 갱신
- **성공 기준**: 전자계약 서비스가 문서ID·서명링크 반환 && 계약 레코드·직원현황 상태가 원자적으로 갱신됨
- **검증 방법**: 사람 검토(발송 전 정보 확인) + API 응답 스키마 검증(docId 존재)
- **실패 시 처리**: API 실패 시 자동 재시도 최대 2회(지수 백오프) → 계속 실패면 계약 레코드 생성하지 않고 발송대기 유지 + 본사에 에러 토스트/로그. 중복발송 방지: 이미 `contractId` 있는 인원은 발송 버튼 비활성.

#### Step 4: 직원 전자서명 (외부 서비스)

- **처리 주체**: 전자계약 서비스(외부) + 직원 본인
- **입력**: 서명 링크/알림톡
- **처리 내용**: 직원이 링크 접속 → 신원확인 → 서명. ERP는 개입하지 않음(대기).
- **출력**: 서비스 측 서명 완료 이벤트
- **성공 기준**: 서비스에서 문서 상태 `completed`
- **검증 방법**: 서비스 자체 처리(범위 밖)
- **실패 시 처리**: 미서명 지속 → Step 6 리마인드로 이관

#### Step 5: 서명 완료 webhook 수신

- **처리 주체**: 서버 프록시(Cloud Function `esignWebhook` 또는 GAS `doPost`)
- **입력**: 전자계약 서비스 webhook 페이로드(docId, status, 서명 PDF URL 등)
- **처리 내용**:
  1. `esign-adapter.verifyWebhook(payload, signature)`로 위조 검증
  2. `esignDocId`로 계약 레코드 조회 → `status="서명완료", signedAt, signedPdfUrl` 갱신
  3. 직원현황 행 `contractStatus="서명완료", contractSignedAt` 역기록
- **출력**: 서명완료로 전이된 계약 레코드 + 직원현황 상태
- **성공 기준**: 서명 이벤트가 정확한 1건의 계약/직원 행에 반영됨
- **검증 방법**: 서명검증(HMAC 등) + docId 매칭 유일성 확인
- **실패 시 처리**: 서명검증 실패 시 무시 + 보안 로그. docId 매칭 실패 시 미처리 큐에 적재 + 본사 알림(수동 확인). webhook 유실 대비 Step 6 스케줄이 서비스 상태를 폴링해 보정(백스톱).

#### Step 6: 미완료 리마인드

- **처리 주체**: 스케줄 서버(Cloud Scheduler + Function, 또는 GAS 시간기반 트리거) 1일 1회
- **입력**: `labor_contracts:*` 전체 중 `status==="발송완료"` 건
- **처리 내용**: `sentAt` 기준 경과일 계산. N일(기본 3일) 이상 미서명이면 리마인드 대상. (선택) webhook 유실 보정을 위해 서비스 상태 폴링. 본사 대시보드에 "미서명 N건" 배지 + 목록 표시. (선택) `esign-adapter.resend(docId)`로 재알림.
- **출력**: 리마인드 목록(본사 화면) + (선택) 재발송 이력
- **성공 기준**: 지연 건이 본사 화면에 노출되고, 과다 재발송 없이 1일 1회로 제한됨
- **검증 방법**: 규칙 기반(경과일 계산) + 재발송 횟수 상한 체크
- **실패 시 처리**: 스케줄 실행 실패는 다음 주기 자동 복구(idempotent 설계). 재발송 API 실패는 스킵 + 로그.

### 상태 전이

| 상태 | 전이 조건 | 다음 상태 |
|------|----------|----------|
| (후보) 정보부족 | 필수필드 보완 완료 | (후보) 발송대기 |
| (후보) 발송대기 | 본사 발송 클릭 + API 성공 | 발송완료 |
| 발송완료 | 서명 완료 webhook(검증 통과) | 서명완료 |
| 발송완료 | sentAt + N일 경과, 미서명 | 발송완료(리마인드 플래그 on) |
| 발송완료 / 서명완료 | 본사 수동 보류 처리 | 보류 |
| 보류 | 본사 재개 | 발송대기 |

---

## 3. 구현 스펙

### 폴더 구조

```
erp_ugd
  ├── CLAUDE.md / AGENTS.md              # 기존
  ├── /src
  │   ├── /api
  │   │   ├── gasClient.ts               # (수정) esign 프록시 호출 메서드 추가
  │   │   └── firebaseDirect.ts          # (수정) 계약/직원현황 상태 갱신 헬퍼
  │   └── /pages
  │       ├── AdminPage.tsx              # (수정) AdminLaborContractsSection: 발송 버튼·필터 정직원화
  │       └── /branch
  │           ├── tabs/RosterTab.tsx     # (수정) contractStatus 뱃지 표시
  │           └── types.ts              # (수정) RosterEmployee/계약 레코드 타입 확장
  ├── /server                            # 신규 — 서버 프록시(택1)
  │   ├── /functions                     # 옵션 A: Firebase Cloud Functions (권장)
  │   │   ├── dispatchContract.ts
  │   │   ├── esignWebhook.ts
  │   │   └── checkReminders.ts
  │   └── gas/Code.gs                    # 옵션 B: GAS 웹앱 doPost/doGet + 시간트리거
  ├── /.claude
  │   └── /skills
  │       └── /esign-adapter             # 전자계약 서비스 어댑터 (skill-creator로 생성)
  │           ├── SKILL.md
  │           ├── /scripts
  │           └── /references            # 서비스별 API 스펙·필드 매핑표
  └── /docs
      └── blueprint-전자근로계약서-발송.md   # 본 문서
```

### CLAUDE.md 핵심 섹션 목록

- **전자 근로계약 워크플로우 개요**: 후보 감지 → 검토 → 발송 → 서명 → 리마인드 6단계 요약
- **발송 대상 규칙**: 정직원 × (신규입사 | 지점이동)만. 파트타이머·기타 제외 — 절대 규칙
- **비밀키/개인정보 취급 규칙**: API 키·주민번호는 서버에서만. 프런트 저장·로그 금지
- **서버 프록시 선택**: Cloud Functions(권장)/GAS 웹앱 중 채택안과 배포 방법
- **상태 정의**: 발송대기/발송완료/서명완료/보류 및 직원현황 `contractStatus` 매핑

### 에이전트 구조

**구조 선택**: 단일 에이전트 (Claude Code 메인) + 1개 구현 스킬(`esign-adapter`)

**선택 근거**: 런타임은 웹 기능 + 서버 함수로 동작하는 결정론적 파이프라인이라 다중 런타임 에이전트가 불필요하다. 서브에이전트로 분리할 만한 독립 도메인 블록이 없다. 전자계약 서비스 API 연동만 벤더 교체 가능성이 있어 **어댑터 스킬 1개**로 격리한다. 구현은 Claude Code가 TDD(`superpowers:test-driven-development`)로 서버 함수부터 작성.

#### 메인 에이전트 (CLAUDE.md)
- **역할**: 전체 기능 구현 오케스트레이션(프런트 수정 · 서버 함수 · 스킬 생성) 및 워크플로우 문서 준수
- **담당 단계**: Step 1~6 전 구간의 구현·검증

### 스킬/스크립트 목록

| 이름 | 유형 | 역할 | 트리거 조건 |
|------|------|------|-----------|
| `esign-adapter` | 스킬 | 전자계약 서비스(모두싸인/이폼사인 등) API 차이 흡수: `createContractDocument`, `getDocumentStatus`, `verifyWebhook`, `resend`, 템플릿 필드 매핑 | 서버가 발송·상태조회·webhook검증·재발송을 수행할 때 |
| `server/functions/dispatchContract.ts` | 스크립트 | 후보→계약 발송: 템플릿 선택, roster에서 주민번호 조회, adapter 호출, 계약 레코드/직원현황 갱신 | 본사 "발송" 클릭(Step 3) |
| `server/functions/esignWebhook.ts` | 스크립트 | 서명 완료 webhook 수신·검증·상태 전이 | 전자계약 서비스 콜백(Step 5) |
| `server/functions/checkReminders.ts` | 스크립트 | 미서명 경과 계산, 대시보드 플래그, 선택적 재발송, webhook 유실 폴링 보정 | 1일 1회 스케줄(Step 6) |
| `AdminLaborContractsSection`(수정) | 스크립트 | 후보 필터 정직원화, 발송 버튼/정보부족 게이팅 | 본사 화면 렌더(Step 1~3) |
| `firebaseDirect`(수정) | 스크립트 | 계약 레코드/직원현황 `contractStatus` 읽기·쓰기 헬퍼 | 상태 갱신 시 |

### 스킬 생성 규칙

> 이 설계서에 정의된 모든 스킬(`esign-adapter`)은 구현 시 반드시 `skill-creator` 스킬(`/skill-creator`)을 사용하여 생성할 것.
> 직접 SKILL.md를 수동 작성하지 말 것 — 규격 불일치 및 트리거 실패의 원인이 됨.

skill-creator가 보장하는 규격:
1. SKILL.md frontmatter (`name`, `description`) 필수 필드 준수
2. `description`의 트리거 정확도 최적화 (eval 기반 optimization loop)
3. 폴더 구조 (`SKILL.md` + `scripts/` + `references/`) 규격 준수
4. Progressive disclosure: SKILL.md 본문 500줄 이내, 서비스별 API 스펙·필드 매핑표는 `references/`로 분리
5. 테스트 프롬프트 실행 및 품질 검증 완료

### 주요 산출물 파일

| 파일 | 형식 | 생성 단계 | 용도 |
|------|------|----------|------|
| Firestore `labor_contracts:<지점>` | JSON(배열) | Step 3~5 | 계약 레코드·상태 저장 (기존 키 재사용) |
| Firestore `branch_own_roster/<지점>` 행 확장 | JSON | Step 3, 5 | 직원현황에 `contractStatus/contractId/contractSentAt/contractSignedAt` 역기록 |
| 서명 완료 PDF | URL(외부 보관) | Step 5 | 서명본 링크만 저장(원본은 전자계약 서비스) |
| 리마인드 목록 | 화면 상태 | Step 6 | 본사 대시보드 미서명 배지·목록 |

---

## 부록: 착수 전 확정 필요 사항 (사람 결정)

1. **전자계약 서비스 선정** — 모두싸인 / 이폼사인 / 기타. 요구: 템플릿 문서생성 API + 서명링크(알림톡) + 완료 webhook. → `esign-adapter` `references/`에 API 스펙 확정 후 구현.
2. **서버 프록시 방식** — Firebase Cloud Functions(권장) vs GAS 웹앱. 비밀키 보관·webhook URL 확보 방법 결정.
3. **템플릿 2종 등록** — T1(신규입사 정직원 표준근로계약서), T2(지점이동 근무지·조건 변경 계약)를 서비스에 사전 등록하고 필드 슬롯 정의.
4. **리마인드 임계일 N** — 기본 3일. 확정 필요.
